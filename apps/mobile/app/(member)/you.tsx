import { ActionButton, Body, Eyebrow, Screen, Surface, Title } from '../../components/ui';
import { useMobile, type AppearanceMode } from '../../lib/mobile-context';
export default function YouScreen() { const { appearance, identity, setAppearance, signOut } = useMobile(); const appearances: AppearanceMode[] = ['system', 'light', 'dark']; return <Screen><Eyebrow>ACCOUNT</Eyebrow><Title>You</Title>{identity.kind === 'member' && <Surface><Body>{identity.memberId}</Body><Body muted>Verified member account</Body></Surface>}<Surface><Eyebrow>APPEARANCE</Eyebrow>{appearances.map((mode) => <ActionButton key={mode} secondary={appearance !== mode} onPress={() => void setAppearance(mode)}>{mode[0]?.toUpperCase()}{mode.slice(1)}</ActionButton>)}</Surface><ActionButton secondary onPress={() => void signOut()}>Sign out</ActionButton></Screen>; }
