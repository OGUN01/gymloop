# Decisions

Append-only ADR log. One decision per entry, with rejected alternatives and why. Update an existing entry rather than duplicating it; delete an entry that turns out to be wrong (with a note on what replaced it, in the entry that replaces it).

The **Open Decisions** section at the bottom is different in kind: these are genuine unknowns intentionally left unresolved, tied to the phase that must resolve them. A future session reaching that phase must make the call deliberately — reading this list first — rather than inventing an answer under deadline pressure or rediscovering the gap mid-build.

---

## Locked stack (master prompt §4)

**ADR-001 — Monorepo: pnpm + Turborepo.** `apps/web`, `apps/mobile`, `packages/shared`, `packages/db`, `packages/api-client`. Rejected: Nx (heavier, more opinionated than this project needs), Lerna (superseded by pnpm workspaces + Turborepo for this scale), a polyrepo (would fragment the single source-of-truth doc framework across repos).

**ADR-002 — Web: Next.js App Router + React + TypeScript strict + Tailwind, on Vercel.** Rejected: Remix (smaller ecosystem for this team's needs), plain Vite SPA (loses SSR/route handlers needed for the API split in ADR-012), self-hosted Node server (Vercel's India edge presence and zero-ops deploy win for a small team).

**ADR-003 — Mobile: Expo + React Native, New Architecture mandatory, EAS Build + OTA.** Rejected: Flutter (explicitly rejected by the master prompt — different language/ecosystem from the web stack, no code-sharing with `packages/shared`), native iOS/Android (2x the engineering surface for a small team). Version pinning deferred — see ADR-020.

**ADR-004 — Database/Auth: Supabase Postgres, ap-south-1 (Mumbai).** Rejected: **Neon** — no India region (nearest is Singapore/Sydney), and India latency is a stated product requirement (a metro-gate-speed QR check-in cannot round-trip to Singapore). Rejected: self-hosted Postgres (loses Supabase's Auth, RLS tooling, and generated-types workflow that this whole doc framework is built around). Provisioned project: ref `pecxrpskmfeuyzngvewq`, org `gjjnocawiprbwdkktogn` — see `AGENTS.md` for the multi-account trap this created.

**ADR-005 — Tenancy: single database, Row-Level Security, `tenant_id` from a JWT claim.** Rejected: database-per-tenant (operationally heavy at hundreds of small gyms, migrations must run N times), schema-per-tenant (Postgres schema-per-tenant hits connection-pooling and migration-tooling friction at this scale). RLS with an indexed, JWT-sourced tenant claim is the standard Supabase multi-tenancy pattern and is what gates 6-8 (`docs/gates.md`) exist to verify.

**ADR-006 — Object storage: Cloudflare R2 + Cloudflare Images, short-lived presigned URLs.** Rejected: S3 directly (R2 has zero egress fees, which matters for member photo/video-heavy usage at scale), public bucket URLs (presigned URLs enforce the authorization check server-side before any access).

**ADR-007 — Edge/security: Cloudflare DNS, CDN, WAF, Turnstile.** Rejected: reCAPTCHA (Turnstile is privacy-friendlier and free at this scale, and the team already standardized on Cloudflare for DNS/CDN).

**ADR-008 — Payments: Razorpay, each gym connects its own account.** Rejected: a platform-custodial payment model where Gymloop holds member money (would require a payment aggregator license in India — out of scope for a software SaaS); Stripe (weak UPI/domestic-India rail support compared to Razorpay); PayU/Instamojo (Razorpay has the strongest Indian developer ecosystem and is what competitors' integrations are benchmarked against — see `docs/architecture.md`'s Quality-bars section on why competitors are the feature reference and never the bar; this is an infra choice, not the loop itself).

**ADR-009 — Jobs: `pg_cron` for scheduled scans, `pgmq`/Supabase Queues for dispatch with retry + dead-letter.** Rejected: an external queue (SQS, BullMQ+Redis) — adds an operational dependency outside Supabase for a workload (daily scans, notification dispatch) that Postgres-native tooling handles at this scale, and keeps everything in one place for the `tenant_id`-carrying structured logs (ADR-010).

**ADR-010 — Observability: Sentry (web, mobile, edge) + structured JSON logs carrying `tenant_id`.** Rejected: a self-hosted error tracker (operational overhead not justified pre-revenue); logs without a tenant dimension (would make gate 28's per-tenant incident triage impossible).

**ADR-011 — i18n: English + Hindi from day one.** Not deferred to a later phase — Indian gym staff and members routinely mix languages, and retrofitting i18n into components built English-only is expensive. Rejected: English-only for v1 with i18n "added later" (the standard version of this decision, and the reason so many Indian SaaS products never ship Hindi at all); a third language in v1 (no evidence of demand, and each one multiplies copy review).

**ADR-012 — API architecture: mutations via Next.js Route Handlers (zod + typed error envelope); reads direct via supabase-js + RLS; Supabase Edge Functions only for Razorpay webhooks and cron.** Rejected: GraphQL (adds a resolver layer with no clear win over direct RLS reads for this data shape); tRPC (couples client/server in a way that complicates the shared `packages/api-client` used by both web and mobile); Edge Functions for all backend logic (Vercel Route Handlers are colocated with the web app and simpler to deploy; Edge Functions are reserved for the two things that must sit next to the database regardless of Vercel's availability — webhooks and cron).

**ADR-013 — Explicitly rejected, do not reconsider:** Cloudflare Workers as the backend, Cloudflare D1 as the database, Neon, Flutter, no-code builders.

## Locked product decisions (master prompt §5)

**ADR-014 — Distribution: one member app for all gyms, joined by invite QR / 6-char gym code / automatic phone-number match against an imported roster.** Phone-number auto-match is the path that actually works for non-technical members and gets the UX investment. In-app branding is gym logo + one accent colour + gym name; a member in two gyms gets a header switcher. Rejected: a white-label app build per gym (an App Store review and an OTA fan-out per gym, defeating the one-shared-app distribution); gym-code-only onboarding with no phone-roster auto-match (the code path is the one non-technical members fail at).

**ADR-015 — Payments: per-gym Razorpay account, platform takes no cut, offline-first is a first-class state.** Most early gyms will have no gateway connected — that state must be fully functional (cash/UPI/card recorded by front desk, with attribution, receipts, and a working renewal pipeline), not a degraded fallback. Onboarding verifies both API keys and the webhook secret with a real ₹1 test payment before go-live. Rejected: platform-custodial payments where Gymloop holds member money (already rejected in ADR-008 — needs an aggregator licence, and revenue is subscription-only); a gateway-required launch that treats gyms without Razorpay as a degraded mode (most early gyms are exactly that state, so it must be the fully functional default).

**ADR-016 — Messaging v1: push notifications (Expo + FCM/APNs) + click-to-WhatsApp (`wa.me` deep links).** No SMS, no DLT registration in v1. Written as a provider interface with a per-gym credit wallet from day one, because Indian competitors (GymForce, Akton, GymFito, AdviceFit) already ship WhatsApp Business API — see "Market context" at the end of this file; this is table stakes arriving in Phase 1.5, not a nice-to-have. Rejected for v1: WhatsApp Business API directly (cost and Meta approval lead time not justified before product-market fit), SMS/DLT (India's DLT registration overhead is significant for a pre-revenue product).

**ADR-017 — Commercial: hosted SaaS, tiers by active-member count (~₹1,499/₹2,999/₹4,999/month), 14-day full-feature trial, manual super-admin activation at launch.** Rejected: matching competitors' ~₹89/month pricing — beating rock-bottom Indian gym software on price proves nothing; the product differentiates on the retention loop (no-show red list, follow-up outcomes, measurable recovery), not on being cheaper. In-app platform billing of gyms is Phase 1.5; v1 activation is manual.

## This session's decisions (Phase 0)

**ADR-018 — TypeScript pinned to 6.0.3, not the newer 7.0.2.** `typescript-eslint@8.69.0`'s peer range is `>=4.8.4 <6.1.0`; TS 7 breaks it. Verified via `npm view` before pinning. Fall back to `5.9.3` if a future Phase's type-generation surfaces a TS 6-specific issue — record that as a new ADR entry if it happens, don't silently downgrade.

**ADR-019 — `apps/mobile` and `packages/api-client` deferred rather than stubbed.** Confirmed with the user. An empty package trips the `knip` unused-export gate this same change arms; mobile UI is explicitly Phase 7 work in the build order. Rejected: scaffolding all five `apps/*`/`packages/*` per §4's literal layout now — contradicts §14's "no scaffolding for later." `docs/architecture.md` documents the full five-package target layout so a future session reads the gap as a decision, not a bug.

**ADR-020 — Expo/React Native version deliberately left unpinned until Phase 7.** §4's SDK 56/RN 0.85 pin was already one SDK behind current (57.0.20/RN 0.87.1) when this session checked. Pinning "whatever's current now" would only repeat that staleness by Phase 7. The version is chosen when Phase 7 starts, against whatever is current then.

**ADR-021 — Canonical status vocabularies (`docs/data-model.md`) are Postgres enums generated into `packages/db/types/database.ts`, never hand-written TypeScript constants.** The master prompt's own truth chain is Postgres enum → `supabase gen types` → generated TS types → derived zod schemas (§10). Writing them into `packages/shared/src/config/constants.ts` now would create a second source Phase 1 would then have to reconcile or delete.

**ADR-022 — `packages/shared` is kept platform-free from day 0**: its `tsconfig.json` sets `"lib": ["ES2022"]` (no DOM) and `"types": ["node"]` explicitly (TS 6's automatic `@types` discovery did not pick up `@types/node` in this workspace layout — verified by a failing typecheck before the explicit `types` array was added); a `dependency-cruiser` rule forbids importing `next/*`, `react-dom`, or `node:*` from it. It is consumed by web, mobile, and Edge Functions — a platform leak here becomes a Phase 7 blocker discovered at the worst time.

**ADR-022a — The `next`/`react-dom` portability rules match `couldNotResolve: true`, not a path against their resolved `node_modules` location.** Found by actually attempting the violation the rule was meant to catch: under pnpm's strict per-package `node_modules`, `next` and `react-dom` are not resolvable at all from `packages/shared` (it never lists them as dependencies) — a rule matching `to: { path: "node_modules/next/" }` silently never fires, because there is no resolved path for it to match. `couldNotResolve: true` catches exactly that unresolvable case and is strictly more general: it forbids *any* platform-specific package sneaking into `packages/shared` later, not only the two named at the time this rule was written. Lesson for writing any future dependency-cruiser rule in a pnpm workspace: verify a "forbid importing X" rule by actually importing X and checking the tool reports a violation — don't trust that a path-based rule fires just because it typechecks.

**ADR-023 — `packages/shared/src/config/env.ts` validates lazily (on first property access, cached) rather than eagerly at module load.** An eager `zod.parse()` throws during `next build` in CI, where no env vars are set, turning the build gate red on missing secrets rather than on broken code. Rejected: seeding CI with placeholder secret values — puts fake secrets in a checked-in workflow file and trains future sessions that a green build implies a valid environment. A documented consequence: `clientEnv()`/`env()` require a real `process.env` object (server components, Route Handlers, Edge Functions, Node scripts) — a `"use client"` component must read `process.env.NEXT_PUBLIC_X` as a literal expression directly (Next.js's client-bundle replacement only rewrites literal member expressions), not call these functions; read server-side and pass the value down as a prop.

**ADR-024 — The schema-drift CI gate compares `supabase gen types --linked` (against Supabase Cloud) against the committed `packages/db/types/database.ts`. Reversed from an earlier `--local`/Docker design — see the history below.**

**History, because the reasoning that got here matters for whoever touches this next.** The gate was first built comparing `--local` (a disposable Docker Postgres via `supabase start`) against committed, reasoning that it would be hermetic and immune to a transient network failure reaching Cloud. That design cost more than it saved, discovered across two real CI runs, not assumed:
1. First run: CI was pinned to Supabase CLI `2.116.0` while the committed file was generated locally with `2.110.0` — a real mismatch, fixed by re-pinning.
2. Second run, identical diff, even with both sides confirmed on the same CLI version: `--local` and `--linked` turned out to emit genuinely different template output for the identical schema (an extra `__InternalSupabase` block, different conditional-type parenthesization) — a structural property of the two generation paths, unrelated to version.
3. On top of both bugs, Docker added ~5-6 minutes of image-pull time to every CI run, for a job whose entire purpose is a text diff.

The deeper problem the two bugs exposed: the app's real database is **always** Supabase Cloud — `--local`'s disposable stack only *approximates* it, and turned out not to even do that reliably. `--linked` checks the invariant that actually matters (does the committed file match what's really deployed) directly, needs no Docker, and removes the whole `--local`-vs-`--linked` template-mismatch bug class permanently, since there's now only one generation path. The accepted risk — a transient network failure to Supabase's API causing a spurious red — is smaller than the bug class and slowdown it replaces, and is what a re-run is for.

`SUPABASE_ACCESS_TOKEN` is a required repo secret for this gate (previously only needed starting Phase 1's `supabase db push`). **It is set**, and gate 10 is proven: on the gate-proof PR the job authenticated, generated from Cloud, and caught exactly the hand-edit (`-export type GateProofBogusHandEdit = true;`), while passing on `main` in the same round — so it discriminates rather than failing always. A human must create it (the CLI's stored token lives in the OS credential manager and isn't extractable by an agent) from the account owning `pecxrpskmfeuyzngvewq` / org `gjjnocawiprbwdkktogn`, then `gh secret set SUPABASE_ACCESS_TOKEN -R OGUN01/gymloop`.

**The current token must be rotated** — see `docs/security.md`, "Credential rotation".

*This paragraph previously asserted the token was NOT set and the gate jammed. It was left stale after the token was added, and a second verification pass caught it — the same overclaim class the first audit found, in the same file. If you change the state of a credential or a gate, grep for every doc that describes that state before you consider the change done.* `db.yml` uses `supabase gen types --project-id pecxrpskmfeuyzngvewq`, not `--linked` — a fresh CI checkout has no prior `supabase link` state (`supabase/.temp/` is gitignored, a local session artifact) and `--linked` would fail with "Cannot find project ref" without it; `--project-id` needs no link step and was verified byte-identical to `--linked`'s output against the same project before switching.

**Docker is now used nowhere.** This paragraph previously said Docker survived in the `pgtap` job for an isolated, disposable database. ADR-030 removed that too: pgTAP runs against the Cloud project with every test file wrapped `BEGIN … ROLLBACK`, after CI applies migrations on merge. There is no `supabase start` anywhere in CI or in the documented local workflow.

**ADR-025 — The real OpenSpec CLI is `@fission-ai/openspec`, not the package published as `openspec` on npm.** The bare `openspec` name is a dead 2022 package (`openspecio`, last published 2022-05-12) unrelated to the actively maintained tool this project uses.

**ADR-026 — `no-magic-numbers`'s `ignore` list includes `[0, 1, -1]`, discovered via a real lint failure, not decided upfront.** Every `z.string().min(1)` in `env.ts` tripped the rule as originally scoped (`ignoreArrayIndexes`/`ignoreDefaultValues`/`ignoreEnums` don't cover an arbitrary call argument), and zod has no `.nonempty()` method for strings (confirmed against current docs) — there is no numeric-literal-free way to express "non-empty string" in zod. Confirmed with the user rather than silently widened, per the standing instruction to treat any apparent need for a rule exception as a finding, not a line to quietly add.

**ADR-027 — `scripts/**` is entirely exempt from the test-immutability rule.** Discovered by actually running `check-test-immutability.mjs` against this repo's own commit that added it — the rule flagged its own authoring commit (a script and its test, added together) as a violation. The rule's actual purpose (master prompt §9) is to stop an implementer quietly loosening a *product* test derived from an approved EARS spec — CI/build tooling under `scripts/` never goes through that pipeline, so a script and its own test belong in one commit. Rejected: adding a `spec:` prefix to qualify — that prefix means "human-approved specification change," which this isn't.

**ADR-028 — `turbo.json`'s `lint`, `typecheck`, and `test` tasks declare no `dependsOn`, so every gate runs independently.** They originally each declared `dependsOn: ["^build"]`. A fresh-context critic auditing the gate-proof PR caught what that actually did: when the deliberate type error broke `build`, turbo aborted the whole task graph *before ESLint ever executed* — the `lint` job went red having run zero lint rules, and the `test` job went red while all 4 tests actually passed. The `no-magic-numbers` proof was therefore worthless, and a real type error would mask every lint finding in normal development too. This directly contradicted the design principle stated in `db.yml`'s own comment ("every gate is its own parallel job — a bad commit must show every gate it breaks at once, not stop at the first failure"): running the gates as parallel CI *jobs* achieves nothing if turbo serialises them again underneath. The dependency was pointless as well as harmful — `packages/shared` and `packages/db` are consumed as TypeScript source (`main` points at `src/index.ts` / `types/database.ts`), their "build" scripts are bare `tsc --noEmit` producing no artifacts at all, and turbo had been signalling exactly that with `no output files found for task @gymloop/db#build` on every run. Verified after the fix: `lint` now catches the magic number, `test` passes 4/4 independently, `typecheck` still fails on the real type error.

**ADR-029 — `AGENTS.md` deliberately omits the master prompt §10 instruction to "archive, clear and restart" at ~70% context.** §10 mandates that line; `AGENTS.md` says the opposite ("you have a large context window; don't stop or suggest a new session on account of context limits"). Recorded as an ADR because a blind critic correctly flagged it as an undocumented spec deviation. Reason: that threshold was calibrated for smaller context windows, and surfacing context countdowns to a model with a 1M-token window triggers a documented failure mode where it truncates its own work or proposes a handoff mid-task. Rejected: keeping the line as written (it would have caused exactly that behaviour during Phase 0, which ran long); deleting it silently (which is what happened first, and is why this ADR exists).

**ADR-030 — Database strategy for Phase 1: one Supabase Cloud project (`pecxrpskmfeuyzngvewq`), no Docker, no branching. Resolves OPEN-005.** Decided by the product owner. The three conditions that make a single shared database safe are rules, not conventions:

1. **Every pgTAP test file is wrapped `BEGIN … ROLLBACK`.** A test that commits is a bug. Enforced by `scripts/check-pgtap-rollback.mjs` (the `pgtap-rollback` job in `db.yml`), which fails on any `supabase/tests/**/*.sql` whose first statement is not `BEGIN` or whose last is not `ROLLBACK`.
2. **No session and no cluster agent pushes migrations to Cloud, ever.** Migrations are written in-branch and applied only by the `migrate` job in `db.yml`, on merge to `main`, in merge order — `supabase db push --linked`, with `--dry-run` on a pull request. `AGENTS.md` rule #7 stays literally true. The `schema-drift` and `pgtap` jobs run after `migrate`; on a PR that changes `supabase/migrations` both skip with a notice and run on the post-merge push of `main`, because Cloud cannot reflect an unmerged migration.
3. **The full pgTAP suite runs after each cluster merges, never concurrently.** The workflow's `concurrency` group serialises `main` runs.

Needs no database password: with `SUPABASE_ACCESS_TOKEN` set, the CLI mints a temporary login role through the Management API (`POST /v1/projects/{ref}/cli/login-role`) for `link`, `db push` and `test db --linked` — proven from CI on the throwaway PR #3, which linked and dry-ran a no-op migration with the password variable empty. Rejected: option 2 of OPEN-005, a hermetic Docker database from committed migrations — ADR-024 already recorded that `--local` and `--linked` emit different output and cost minutes per run; option 3, a split hermetic check plus a Cloud monitor — two jobs and a Docker dependency for a Phase with no real data yet; Supabase branching — a second environment to keep consistent before there is anything to protect. **Revisit, deliberately, not now:** see OPEN-006 and OPEN-007.

## Phase 1 decisions (data model)

Taken during the overnight Phase 1 run with the product owner asleep and the run authorised end to end. Each is the safest default the orchestrator could pick against the docs as written; each is listed again under "Open decisions" so the owner can confirm or reverse it deliberately rather than inheriting it silently.

**ADR-031 — The `app_role` Postgres enum is the one role vocabulary. `ROLES` and `Role` in `packages/shared/src/config/constants.ts` are retired.** Phase 1 creates `app_role` as a Postgres enum because `staff.role`, `organization_settings.pause_approver_role`, `platform_users.role` and `audit_log.actor_role` are all columns that need it, and a column's domain is a database type. Keeping the TypeScript array as well would leave two role vocabularies that can drift — exactly what `docs/registry.md` exists to prevent, and the same failure mode ADR-021 rejected for the status vocabularies. Note the tension this resolves: `docs/architecture.md` explicitly listed "the fixed role list" as belonging in `constants.ts` *outside* the truth chain, written before it was known that roles would be a column type. That sentence is updated in the same change rather than left to contradict this. Rejected: keeping both and calling `constants.ts` "the UI's copy" (a second source is a second source); leaving `staff.role` as `text` with a check constraint so `ROLES` could stay canonical (loses the generated TypeScript union that is the entire point of the truth chain).

**ADR-032 — The tenant claim is `tenant_id` and the role claim is `app_role`; both are read through two `stable` functions in a private `app` schema, never inline in a policy and never via a subquery.** Naming the claims and the accessors once, before six agents write policies in parallel, is what stops the fan-out producing three dialects of the same predicate. The functions live in `app` rather than `public` because a function in `public` is exposed by PostgREST as an RPC endpoint and appears in `packages/db/types/database.ts`, widening both the API surface and the drift check for no gain; `app` is not in `config.toml`'s exposed `schemas`, so it is neither. They are `security invoker`, not `security definer`: they only read the JWT and never touch a table, so definer rights would buy nothing and hand a future caller an RLS bypass. A missing or empty `tenant_id` claim yields `NULL`, so the policy predicate is `NULL` and matches no rows — an unauthenticated or hook-less token sees nothing, without raising. A *malformed* claim (present but not a uuid) raises on the cast, which is the correct loud failure. Rejected: `auth.jwt() ->> 'tenant_id'` written inline in every policy (unreviewable at 30 tables, and impossible to change centrally when Phase 2's hook lands); a `user_metadata` claim (user-editable in Supabase, so unsafe for any authorization decision); a per-row subquery against a membership table (gate 8 forbids it, and it is the documented source of cross-tenant leaks in Supabase RLS setups).

**ADR-033 — Exactly two tables sit outside tenant scoping, and both are platform-owned.** `platform_users` carries no `tenant_id` at all: a super admin belongs to the platform, not to a gym, and inventing a nullable tenant column for it would make gate 6's "every table has a tenant_id" true in letter while meaning nothing. `audit_log.tenant_id` is nullable because a platform-level event (a role change among platform users) genuinely has no tenant; a null-tenant row is visible only to the platform policy. Both are named exhaustively in `docs/data-model.md`'s contract, and a pgTAP meta-test asserts that no *third* table is missing tenant scoping — so the exemption is a closed list, not a precedent. Rejected: giving `platform_users` a `tenant_id` pointing at a synthetic "platform organization" row (a fake tenant that every cross-tenant test would then have to special-case).

**ADR-034 — `supabase/seed.sql` is applied by a manually dispatched CI workflow (`.github/workflows/seed.yml`), via `supabase db query --linked -f`.** The one command is `gh workflow run seed.yml -R OGUN01/gymloop`. A seed is data, not schema, so it does not belong in the forward-only migration stream that `db.yml` applies on every merge — a demo gym must not be re-created on Cloud every time an unrelated migration lands. It cannot be run from a session either: sessions hold no database credentials, and ADR-030's rule is that CI owns writes to Cloud. `workflow_dispatch` keeps both true, makes every seeding an audited run with a log, and shares `db.yml`'s `concurrency` group so a seed can never run while a migration is applying. The file is idempotent, so a second dispatch converges rather than duplicating. Rejected: `supabase db push --include-seed` (would seed on every merge); a seed migration (forward-only means it could never be corrected, and it would run against a production project); documenting a local `supabase db query` command as the answer (works, but puts a Cloud write back in a session's hands, which is the one thing ADR-030 exists to prevent — it stays in `README.md` only as the description of what the workflow does).

**ADR-035 — Primary keys are `uuid default gen_random_uuid()`.** The Supabase best-practice guide prefers `bigint generated always as identity` for index locality, and this is a deliberate departure from it. Sequential ids in a multi-tenant product leak information across tenants — row counts, growth rate, and neighbouring gyms' volume — through any surface that shows an id, and ids here appear in URLs, QR payloads and receipt references. Postgres 17 has no built-in `uuidv7()` and `pg_uuidv7` is not available on this project (verified against `pg_available_extensions`), so time-ordered uuids are not an option; random v4 and its B-tree fragmentation is the cost of the property that matters. Revisit if a table ever reaches the scale where insert locality dominates — at 100 gyms × 500 members it does not.

**ADR-036 — DPD-007 is split: the per-table retention policy is written now, in Phase 1; the breach-notification runbook is deferred to Phase 8.** `docs/security.md` required whoever planned Phase 1 to make this call explicitly rather than assume it away. The retention policy is a property *of the schema* — it can only be written once the tables exist, and it is cheapest to write while the table list is in front of you, so it lands in `docs/security.md` in this change. The breach-notification runbook is an operational document about people and timelines, not about tables; nothing in Phase 1 makes it easier or harder to write, and Phase 8's launch checklist is where the rest of the pre-launch operational work already lives. Rejected: deferring both (the retention policy would then be written by a session that has to re-derive the whole table list); doing both now (Phase 1 has no incident-response contacts, escalation path or notification channel to write down, so the runbook would be a placeholder pretending to be a deliverable).

**ADR-037 — Table privileges are revoked and re-granted per table, in the same migration that creates the table; `delete` and `truncate` go to nobody, and `anon` gets nothing anywhere.** Row-Level Security is not a privilege system, and on this project the distinction is not theoretical. Verified by reading `pg_default_acl` on `pecxrpskmfeuyzngvewq` directly: for tables created by `postgres` in schema `public`, the default ACL is `{postgres=arwdDxtm/postgres,anon=arwdDxtm/postgres,authenticated=arwdDxtm/postgres,service_role=arwdDxtm/postgres}` — every privilege including `d` (delete) and `D` (truncate), to `anon` and `authenticated`, on every table, automatically. `truncate` is not filtered by RLS at all, so a table with perfect policies and default privileges can be emptied by any signed-in user. Hence, per table and written out in full: `revoke all … from anon, authenticated;` then `grant select, insert, update … to authenticated;` (or `select, insert` on an append-only table). Rejected: `alter default privileges in schema public revoke all on tables from anon, authenticated` as *the* mechanism — it silently changes every future object created by any tool including Studio, it is invisible where a reader audits a table, and it does not cover objects created by another role; it may later be added as a second belt but may not replace the first. Rejected: the `on all tables in schema public` form of `grant` — a later cluster running it re-grants across every earlier cluster's tables. Rejected: `force row level security` — it buys nothing here and it misleads. `force` only ever changes what the *table owner* sees; the roles that carry a user request are `authenticated` and `anon`, and both are already fully subject to every policy. Isolation is proven for `authenticated`, and that is what gate 7's suite asserts.

*Correction, recorded rather than quietly fixed (found by the Phase 1 blind critic on the tenancy migration).* This entry, `docs/data-model.md` and `design.md` all previously rejected `force` on the grounds that it "would subject `postgres` to its own policies, so the seed would write nothing". **That is factually wrong on this project.** `postgres` here holds `rolbypassrls = true` — verified with `select rolname, rolbypassrls from pg_roles` (`postgres`, `service_role`, `supabase_admin` true; `authenticated`, `anon` false) — and a `BYPASSRLS` role bypasses row security unconditionally, which `FORCE ROW LEVEL SECURITY` does not override. So `force` would not have broken the seed or the pgTAP fixtures. The decision is unchanged; only its stated reason was wrong, and it was wrong in three documents at once because it was reasoned about rather than run. The `pg_default_acl` half of this ADR *was* run against Cloud, and it held.

**ADR-038 — Tests and implementation are two commits in one push, and the red pgTAP run is bought once rather than seven times.** `AGENTS.md` rule 10 requires the test committed before the implementation that answers it, and `check-test-immutability` enforces it **per commit** — so two commits in a single push satisfy it exactly as two pushes would. What a single push gives up is a CI run observing the tests red. Under the owner's trunk-only decision there is no PR to absorb that run: on `main`, `db.yml` skips nothing, so a tests-only push applies no migration and runs pgTAP against tables that do not exist, leaving `main` red until the next push. Phase 0's hardest-won lesson is that a green tick is not evidence and a red one is not either until you read why — so the `tenancy` contract cluster pushes its tests alone first and that red run is recorded as proof the pgTAP runner discriminates rather than passing vacuously. The six fan-out clusters then push both commits together, and `main` stays green. Rejected: seven red runs on `main` (a deliberately broken trunk, on a repo whose rule is that `main` stays green, to re-prove the same one thing six more times); rejected: no red run at all (then nothing distinguishes a working pgTAP job from one that silently runs zero assertions, which is precisely the jam Phase 0 shipped twice).

**ADR-039 — A calendar-day column default is `(now() at time zone 'Asia/Kolkata')::date`, never `current_date`.** Found by the contract migration's implementer while transcribing `members.joined_on date =current_date`, and it is a real bug in the contract rather than a style preference. `current_date` evaluates in the **session** timezone; every Supabase connection is UTC. So for a gym in IST, any row inserted between 00:00 and 05:30 local time gets *yesterday's* date — and 05:00 is real gym traffic, not a theoretical edge. MNY-004 is explicit that a calendar day the gym reasons about is evaluated in the gym's timezone. Two columns are affected, `members.joined_on` and `no_show_cases.opened_on`, in two different clusters, which is why this is a contract change rather than a one-line patch. Rejected: dropping the default entirely and requiring the application to supply the date — correct in principle, but Phase 1 has no application layer, the seed would have to supply it everywhere, and a column with no default is a nullability question the table list has already answered. Rejected: a default that reads `organizations.timezone` — a column default cannot reference another row, and a trigger to do it would be behaviour in the database, which Phase 1 does not own. The literal `'Asia/Kolkata'` knowingly duplicates `DEFAULT_TIMEZONE` in `packages/shared/src/config/constants.ts`; a migration cannot import a TypeScript constant, so the duplication is recorded in the contract as the second place to change rather than pretended away. **Known limit:** the default is right for every v1 gym and wrong for a gym outside IST, which Phase 3 must handle by supplying the gym-local date explicitly.

**ADR-040 — The check-constraint rule word is fixed, because it was the one thing in the naming table left to taste.** The contract gave the pattern `<table>_<rule>_chk` and never said what `<rule>` is; its own two examples disagreed (`organizations_gym_code_format_chk` uses `_format_`, `<table>_currency_chk` does not). Six agents each naming roughly fifteen checks with no rule would have produced six dialects — exactly the failure the Conventions section exists to prevent, and the reconciliation would have been ninety constraint names after the fact. Now: a regex check is `<table>_<column>_format_chk`, a range or bound is `<table>_<column>_chk`, and a multi-column rule is `<table>_<phrase>_chk`. Raised by the contract migration's implementer as the highest-risk under-specification in the document before merge 2, which is when it would have started costing.

**ADR-041 — The currency check is `<table>_currency_format_chk`, and ADR-040's rule word wins over a literal template that contradicts it.** `docs/data-model.md` fixed the naming rule in one section (a regex check is `<table>_<column>_format_chk`, ADR-040) and, two sections later, still handed every cluster the literal template `constraint <table>_currency_chk`. `currency ~ '^[A-Z]{3}$'` is a regex, so the two lines could not both be followed. The `tenancy` implementer transcribed the literal template — correctly, since that is what the contract said — and the blind critic caught the contradiction before the other eleven `currency` columns landed in merges 2 through 7. The general rule wins: renaming one constraint now is a `sed`, renaming twelve after the fact is the ninety-name reconciliation ADR-040 exists to prevent. `docs/data-model.md` is corrected in the same change. Rejected: keeping `<table>_currency_chk` and adding it to ADR-040 as a named exception — an exception in the one convention that exists to remove taste is how the next six exceptions get argued for.

**ADR-042 — A migration is validated before it is pushed by replaying it inside `begin … rollback` against Cloud, and it is still applied only by CI.** Hard rule 7 and ADR-030 say CI applies migrations; they do not say a migration must reach `main` unexecuted. Seven migrations landing serially, each verified only by a CI round trip, means every syntax slip costs a red `main` and a fix-forward commit. Verified against the project before relying on it: `supabase db query --linked` connects as `postgres` with `rolbypassrls = true` and `rolsuper = false` — the same session shape the pgTAP job runs in — and it honours an explicit transaction: `begin; create table public.__probe_rollback(x int); rollback;` left `information_schema.tables` with zero rows for that name. So the orchestrator concatenates `begin;` + the migration + the cluster's pgTAP file and runs the whole thing through `db query --linked`; the test file's own trailing `rollback` unwinds the migration with it, and the schema is re-checked empty afterwards. Nothing is committed, which is precisely the guarantee ADR-030 already leans on for the entire pgTAP suite running against this same shared project. Rejected: pushing and letting CI find it (a red `main` per typo, and ADR-038 spends the one deliberate red run on something that actually proves a property); rejected: `supabase db push` locally (that *is* an apply, and it is the rule with the second-worst blast radius here); rejected: Docker and a local instance (ADR-030, and the daemon is not running on this machine — verified). **Limit, stated so it is not overclaimed:** this proves the migration executes and the assertions pass; it does not prove `supabase db push` will sequence the files the same way, so the CI `migrate` log stays the evidence of record. Available to the orchestrator, not to cluster agents — their brief forbids every mutating `db query`, and the distinction between "mutating" and "rolled back" is exactly the judgement call that should not be fanned out six ways.

**ADR-043 — A holdout pgTAP file that references a not-yet-merged cluster is parked in `supabase/tests-staged/`, and moved back unchanged when its migration lands.** The holdout repo's own `supabase/tests/README.md` already states the rule — "a file may only reference schema objects that have already merged. A holdout file pushed ahead of the migration it tests turns `main` red for a reason that is not a defect" — and two files broke it: the blind holdout authors for `attendance` and `comms` pushed before the `tenancy` migration had even landed. Left in place they would have held `gymloop`'s `pgtap` job red through four unrelated merges, which destroys the one property the serial landing exists to give: *this* cluster is green before the next one starts. `supabase/tests-staged/` is looked at by neither `supabase test db` (which is passed `supabase/tests` explicitly) nor `check-pgtap-rollback` (which globs `supabase/tests/*.sql` through `git ls-files`), so a parked file runs nowhere and is checked nowhere until it is moved back. **The move was made through GitHub's git-data API by blob sha — a tree entry deleted at one path and re-created at another with the identical sha — so the orchestrator never read, downloaded or rendered the contents**, which is the property AGENTS.md rule 10 actually protects. Rejected: leaving them and reading the `pgtap` log per merge to decide whether the red was "the expected red" — that is exactly the jammed-gate failure `docs/gates.md` records from Phase 0, where a job is red for a reason nobody checks and a real failure hides inside it. Rejected: deleting them and asking the authors to re-push later — a blind author's output should not be re-derived, and a blob sha round-trips it byte-identically.

**ADR-044 — The orchestrator repaired two mechanical defects in the visible pgTAP suite before it was ever committed, and this does not weaken hard rule 10.** The interrupted run left nineteen visible test files uncommitted. Replaying them against Cloud (ADR-042) showed two that could never have passed against *any* schema: (a) every `results_eq` whose expected list held bare string literals, or whose actual list cast a catalogue `name` column to `text`, aborted the whole file with `42P22 could not determine which collation to use` — `name` carries collation `"C"`, an unknown literal carries none, and pgTAP compares the two cursors as records; fixed by `collate "default"` on the actual side and `::text` on the expected side, verified by running all four shapes against Cloud. (b) `04_contract_meta`'s assertion that `organizations_tenant_all` compares `id` rather than `tenant_id` tested `pg_get_expr(...) like '%tenant_id%'`, which matches the substring inside `current_tenant_id` — so it failed against the correct policy and would have passed against a wrong one only by accident; fixed to the word-boundary regex `~ '\mtenant_id\M'`. Neither change touches what an assertion *claims*, only whether it can execute — the count of assertions per file is unchanged and no expectation was loosened. What rule 10 protects is that the implementer does not shape the tests to the implementation: the seven migrations were written by blind cluster implementers who never saw the orchestrator, and the anti-gaming signal remains the holdout suite, which the orchestrator has not read and did not touch (ADR-043 moved two of its files by sha without opening them). Recorded rather than quietly fixed because the honest description of what happened is "the orchestrator edited test files", and that sentence should be in the record with its reasons rather than discovered later in a diff.

**ADR-045 — pgTAP stays in `extensions`, and the *database* default `search_path` is what makes it reachable; the repair migration's timestamp is chosen by hand, deliberately.** The first `db.yml` run after the contract migration applied still failed every pgTAP file — visible and holdout — with `function plan(integer) does not exist`. That is name resolution, not privilege. Read out of `pg_roles` against this project: `supabase test db --linked` connects as the CLI's temporary login role `cli_login_postgres`, which carries **no `rolconfig` at all**, so its `search_path` is the built-in `"$user", public`; `postgres` carries `search_path = "$user", public, extensions`, which is exactly why the same files pass under `supabase db query --linked` and fail under `supabase test db --linked`. Three fixes were considered and two were eliminated by running them. **Rejected: install pgTAP into `public`** — it would put roughly a thousand functions into the one schema `supabase gen types` reads, and `packages/db/types/database.ts` is generated from that schema. **Rejected: `alter role cli_login_postgres set search_path …`**, the narrower fix — it returns `42501 permission denied to alter role … only roles with the CREATEROLE attribute and the ADMIN option on role "cli_login_postgres" may alter this role`, verified inside `begin … rollback`. **Chosen: `alter database postgres set search_path to "$user", public, extensions`**, verified permitted in the same way. It grants no capability that did not exist — every function in `extensions` was already callable schema-qualified and `execute` on an extension's functions is granted to `public` by default — and every role that sets its own `search_path` is unaffected, a role-level setting overriding the database default.

The second half of this decision is the file name. `supabase migration new` produced `20260906171047`, which sorts *after* the six cluster migrations still waiting to merge (`2026090611514*`–`2026090611520*`). Pushing it under that name would move the remote history head past them, and `supabase db push` **excludes** a local migration older than the head unless `--include-all` is passed — so six migrations would have been silently skipped by a green-looking `migrate` job. The file is therefore named `20260906115132_pgtap_search_path.sql`, one second after the tenancy migration it repairs and before everything still to come. This is a deliberate exception to `docs/data-model.md`'s "never invent the timestamp", whose actual purpose is stopping six parallel cluster agents from colliding; this is the orchestrator inserting one repair between two already-fixed points, serially, and the alternative is a silent skip, which is the worse failure by a wide margin. **A later phase inserting a migration into applied history must re-read this**: the rule still holds for cluster agents, and an out-of-order push without `--include-all` remains the trap.

**ADR-046 — Every pgTAP file takes the owner role explicitly, because CI's session is `cli_login_postgres`, not `postgres`.** `docs/data-model.md` stated, as verified fact, that "CI's pgTAP session connects as `postgres` … `postgres` owns every table a migration creates **and holds `BYPASSRLS`**". It does not, and the whole suite — visible and holdout — was red on that assumption. What is actually true, measured against this project after the contract migration applied: `supabase test db --linked` runs `pg_prove` as **`cli_login_postgres`**, which is a `NOINHERIT` member of `postgres` (`rolinherit = false`), holds `rolbypassrls = false`, has `has_table_privilege(…, 'public.organizations', 'SELECT') = false`, and has `has_schema_privilege(…, 'extensions'|'app'|'auth', 'USAGE') = false`. Under that role `select plan(58)` raises `function plan(integer) does not exist` — not a privilege error, because a schema the caller cannot use is simply invisible to name resolution — and not one fixture row could ever have been inserted.

The belief survived because it was true of the *other* CLI command: `supabase db query --linked` reports `current_user = postgres`. The two commands do not authenticate the same way, and every local check in this phase had been run through the one that does. **The general lesson is the same one ADR-037's correction already recorded and this phase then repeated: a claim about this database is only verified if it was run against this database, through the path that actually runs in CI.** A diagnostic committed to `main` for one round trip — a test file that raises `22P02` with `current_user` embedded in the message, because psql's stderr is what the job log shows — settled in five minutes what an hour of catalogue reading had not.

The repair is `set local role postgres;` as the first statement after `begin;` in every pgTAP file, and `set local role postgres;` in place of every `reset role;` — `reset role` returns to the *session* role, which here can do nothing, so leaving it would have broken every file at its second fixture block. The membership carries the `SET` option (`pg_has_role('cli_login_postgres','postgres','SET')` is true), and so does the path to `authenticated`, so the isolation half of every file is unaffected. Both statements are transaction-local and the closing `ROLLBACK` undoes them.

Rejected: **granting `cli_login_postgres` what it lacks.** `USAGE` on `extensions` is grantable, but `BYPASSRLS` is a role *attribute* and is neither grantable nor inheritable, so fixtures spanning two tenants would still be refused by RLS — and `alter role cli_login_postgres …` returns `42501 permission denied to alter role` in any case. Rejected: **installing pgTAP into `public`**, which would put 1,134 functions into the schema `supabase gen types` reads. Rejected: **injecting the role switch in `.github/workflows/db.yml`** by rewriting the files into a temp directory before `pg_prove` — it would reach the holdout files without editing them, which is attractive, but it makes the committed test files something that does not actually run, and it cannot fix `reset role` without doing content surgery in CI, which is worse than doing it in the open. Rejected: **supplying a database password so the CLI connects as `postgres` directly** — it would restore every assumption with no file edits, and it is the cleanest fix if the owner wants it later, but it needs a secret this run does not have and must never invent.

**The holdout files were edited without being read.** `h01` and `h02` were fetched to a file through the GitHub API, transformed by the same regex script that transformed the visible suite, and uploaded as blobs; at no point was their content printed, opened, or summarised. The four suites written during this run were transformed in their drop directory before they were ever uploaded. This is a weaker guarantee than never touching them, and it is stated plainly rather than glossed: the property AGENTS.md rule 10 protects is that the *implementation* was not fitted to the tests, and the seven migrations were written by blind cluster implementers before any of this happened.

**ADR-047 - A uniqueness or exclusion constraint that is not tenant-scoped is a cross-tenant denial of service, and four of them were in the contract.** Six blind critics reviewed the seven cluster migrations. Four of them, independently and without seeing each other, found the same shape: a foreign key references its parent's `id` alone, referential-integrity checks run with row security off, and the `with check` on the insert only proves *the row's own* `tenant_id` - so gym A can write a row into its own tenant naming gym B's member, trainer or device. That by itself is junk, not a leak. It becomes a denial of service the moment a **globally scoped** unique index or exclusion constraint stands behind it, because a constraint ignores RLS: the row gym A wrote takes a slot gym B needs, and gym B cannot see it, cannot update it, and cannot delete it - Phase 1 grants `delete` on nothing (INT-001), so the block is permanent short of `service_role`. It is also an existence oracle: a `23505` or `23P01` raised against an invisible row answers a question about another tenant.

Four instances, all now tenant-scoped in `docs/data-model.md`:

- **`pt_sessions_trainer_overlap_excl`** gains a leading `tenant_id with =` term. Gym B could otherwise fill gym A's trainer's calendar 06:00-22:00 for a month, after which every genuine booking gym A attempts raises `23P01` against a calendar that renders empty. The tenant term loses nothing: every legitimate session for a trainer carries that trainer's gym's tenant, so every real double-booking is still caught (DQA-005).
- **`memberships_member_id_live_key`** becomes `(tenant_id, member_id)`. Otherwise gym A takes the live-membership slot for gym B's member and gym B can never activate that member again.
- **`no_show_cases_member_id_open_key`** becomes `(tenant_id, member_id)`. Otherwise gym A blocks gym B's daily scan from ever opening that member's case - the precise failure NSH-003/004 exist to prevent.
- **`member_devices.push_token`** becomes unique **per gym** rather than globally. This one needs no adversary at all, which is why it is the most important of the four: a member who belongs to two gyms is entirely representable (`members` is unique on `(tenant_id, phone)`), and under a global unique the second gym's device registration fails with a constraint violation against a row it cannot see. Push is v1's primary channel (ADR-016), so that member simply never hears from the second gym. The line the contract used to carry - "a token identifies one app install, so the same token at two gyms is the same device and must not be duplicated" - is true about the *device* and wrong about the *registration*: two gyms legitimately both need a delivery target for the same handset.

Two further fixes ride the same correction. **`payments` gains `check (provider_payment_id is null or provider is not null)`**: `provider` is nullable and sits *inside* `payments_tenant_id_provider_provider_payment_id_key`, and a unique index treats rows as distinct whenever any key column is null (`indnullsnotdistinct` is false, verified on the live index) - so the duplicate-provider-reference guard the spec promises never fired, and two webhook deliveries for one captured payment would produce two `paid` rows, two receipts and a double membership extension. **`impersonation_sessions` gains `check (ended_at is null or ended_at >= started_at)`**, so the eight-year legal-hold record of a support session cannot say it ended before it began.

And **a fourth privilege tier, `read-only`**, for `messaging_wallets` and `audit_log`. The contract had both under grants that let the party they describe write them: a gym owner's own session could set its own messaging credit balance to any number, leaving no ledger row - in the one table `docs/security.md` calls "a credit ledger the gym is billed against" - and could insert an `audit_log` row naming a platform user as the actor, permanently, in the table INT-003 exists to make trustworthy. Neither has a v1 flow in which a user session writes it; both are written by `service_role`, which is never revoked from.

**Why this was decided rather than escalated.** The owner authorised this run end to end and left the standing instruction to pick the safest default where a decision is theirs. Each of these is a strictly narrowing change: the constraint still rejects everything it rejected within a tenant, and the privilege still permits everything `service_role` needs. Reversal cost is one migration per item, and no data exists. The alternative - shipping Phase 1 with four cross-tenant denial-of-service channels and a gym able to mint its own billing credits, in the phase whose entire purpose is tenant isolation - is not a safest default in any reading.

**What it cost, stated because it is the interesting part.** Blind test authors wrote their suites from the contract *as it was*, so a late contract correction strands assertions that encoded the old rule - and the holdout suites are the ones the orchestrator may not edit. The resolution keeps the property that matters: a **fresh blind author revises its own cluster's holdout file against the corrected contract**, still never reading a migration or the visible suite. That is the same guarantee as before - the tests come from the specification, not from the implementation - and it is why the contract can be corrected this late at all.

**The two corrections to an already-applied cluster ride a fix-forward migration, and that migration is the one file allowed to `drop` and to `alter` another cluster's table.** `memberships`' live-membership index and the `payments` provider check belong to `membership+money`, which merged and applied before the critics ran; hard rule 7 and ADR-030 make an applied migration unrewritable, so `20260907044750_adr_047_tenant_scoped_constraints.sql` creates the tenant-scoped index, drops the global one (create first, so "at most one live membership per member" holds at every instant) and adds the check validating rather than `not valid`. `docs/data-model.md`'s migration-file layout forbids `drop` and cross-cluster `alter`; that rule exists to stop six parallel cluster authors colliding, and a serial forward-only repair is exactly the case it cannot cover. Recorded here so the exemption is a decision and not a diff someone finds later.

**Not fixed, deliberately, and escalated instead - see OPEN-008.** The root cause under all four instances is that no foreign key in the schema re-checks the tenant. Closing *that* needs `unique (tenant_id, id)` on every parent table and composite `(tenant_id, <fk>) references parent (tenant_id, id)` on every child - roughly forty constraints across all seven clusters, and a decision about whether tenancy is enforced by referential integrity or only by policy. Tenant-scoping the four constraints removes every consequence found; it does not remove the underlying ability to write a row referencing another tenant's parent, which remains junk inside your own tenant.


**ADR-048 — The pgTAP job runs the visible suite and the holdout suite as two invocations, because the CLI's temporary login role has a connection ceiling.** `pg_prove` opens one connection per test file and the CLI authenticates each as `cli_login_postgres`, the role it mints through the Management API. Measured on run 34086777055: files 1-19 pass (`Tests=835`), then every remaining connection is refused with `FATAL: (ECIRCUITBREAKER) failed to retrieve database credentials after multiple attempts`, and the tail of the suite reports no result at all. Twelve consecutive DB runs failed this way, which is why the suite had never been green: the first eleven were read as test failures and were not. The clean fix ADR-046 named — supply `SUPABASE_DB_PASSWORD` so the CLI connects as `postgres` with a stable credential — is still the right one, and it is **not available to this run**: `.env.local` carries the key with an empty value, and ADR-046's rule is that a secret this run does not have must never be invented. So the job splits into **three** invocations with pauses between them, each well under the ceiling, and **all must pass** — a job that fails if either half fails is not a weaker gate, it is the same gate under a working connection. **Two invocations was not enough, and the correction is part of the record.** Run 34093344925 — a commit that changed only documentation — passed 17 of the 18 visible files and then could not open a connection for the 18th at all: `Tests: 0`, `FATAL: (EAUTHQUERY) unsupported or invalid secret format`. Eighteen files plus the `link` call is the boundary, so the first split was sitting exactly on it and the green run before it was luck rather than headroom. The visible suite is now two invocations of nine. This is the second time in one phase that a red run had nothing to do with the tests, which is the argument for the password fix rather than against it.

Rejected: raising `pg_prove`'s parallelism (it makes the burst worse); rejected: one file per invocation (26 role mints, straight back into the rate limit); rejected: reading a red tail as expected (that is the jammed gate `docs/gates.md` was burned by in Phase 0). **When the owner supplies the password, collapse this back to one invocation** — the split is a workaround for a credential ceiling, not a property worth keeping.

**ADR-051 — The owner supplied a database password, so CI connects as `postgres` and ADR-048's split is undone.** ADR-046 named this as the clean fix and could not take it, because `.env.local` carried `SUPABASE_DB_PASSWORD` as an empty key and a credential must never be invented. The owner supplied one on 2026-09-07, explicitly as a **temporary build credential to be rotated once the phases are done**, on the grounds that the application is not live. It is now a repository secret and is read by `db.yml` and `seed.yml`; the value lives only in the gitignored `.env.local` and in GitHub's secret store, and appears in no committed file.

Two things follow. The `pgtap` job is **one invocation again** — the ceiling that forced three (ADR-048) was a property of the minted login role, not of the suite, and it is gone with the role. And ADR-046's whole diagnosis stands but its consequence narrows: the pgTAP prologue `set local role postgres;` is now a no-op rather than a repair, because the session already *is* `postgres`. It stays, because it is what makes each file correct under either connection path, and the next person to remove a password should not have to rediscover ADR-046.

**Two obligations this creates, stated so they are not forgotten.** The credential is now in GitHub Actions' secret store and in one developer's `.env.local`: **rotate it when the build phases end**, alongside the `SUPABASE_ACCESS_TOKEN` rotation `docs/security.md` already owes. And it grants `postgres` — superuser-adjacent, `BYPASSRLS` — to anything that can read the secret, so a workflow that runs untrusted code (a `pull_request_target`, a third-party action) must never be given it. Today only `db.yml` and `seed.yml` see it, both triggered by pushes to `main` and a manual dispatch.

**ADR-049 — Three more instances of the ADR-047 class, found by the blind final critic, in tables ADR-047 had already been through.** ADR-047 said a uniqueness constraint that is not tenant-scoped is a cross-tenant denial of service. It fixed four. The final critic found a fifth, and two adjacent privilege holes:

- **`invoices_payment_id_key`** is globally unique on a `not null` column whose foreign key references `payments (id)` alone. Gym A inserts an invoice *into its own tenant* naming gym B's payment id — referential integrity runs with row security off, so nothing stops it — and gym B can then never invoice that payment: `23505` against a row it cannot see, update or delete. Six blind critics missed it because it is a table-level `constraint … unique` inside `create table`, not a `create unique index`; every ADR-047 instance was of the latter shape and the search was shaped by the examples. It becomes `unique (tenant_id, payment_id)`.
- **`messaging_wallet_ledger` becomes read-only to `authenticated`.** ADR-047 made `messaging_wallets` read-only so a gym could not set its own credit balance — and left the ledger append-only, in a contract that says the balance *is* the sum of the ledger. The fix was defeated by the table it did not move.
- **`webhook_events` becomes read-only to `authenticated`.** `signature_valid` and `payload` are client-supplied on insert, so a gym could write the record of a payment webhook it verified itself. Only `service_role` writes it in any v1 flow — the same argument ADR-047 made for `audit_log`.

The lesson worth keeping is about the search, not the schema: four independent critics found four instances of one shape and stopped, because the shape they had learned was `create unique index`. `04_contract_meta.sql` now carries the rule as a catalogue-iterating assertion — every unique or exclusion constraint in `public` must lead with `tenant_id`, with the two deliberate exceptions named — so the fifth instance is the last one a human has to notice.

**ADR-052 — Every foreign key to a tenant-scoped parent becomes composite, closing OPEN-008 at the cause rather than the consequences.** Decided by the product owner on 2026-09-07, who was shown both options and chose the structural one.

OPEN-008 recorded that no foreign key in this schema re-checks the tenant: every one references its parent's `id` alone, and Postgres runs referential-integrity probes **with row security off**. A gym can therefore write a row *into its own tenant* — satisfying every policy and every `with check` — whose `member_id`, `staff_id`, `membership_id` or `payment_id` belongs to another gym. By itself that is junk rather than a leak: nothing of the other gym becomes readable. It is the *cause* under ADR-047 and ADR-049, because a constraint also ignores RLS, so the junk row can take a unique slot the other gym is then denied forever. Those two ADRs tenant-scoped nine constraints and removed every consequence anyone could find. This removes the ability, and stops the class recurring in tables Phase 2 has not written yet.

Measured against the live catalogue rather than estimated: **88 foreign keys, of which 50 across 23 tables become composite.** The 38 that do not are exactly three shapes, each outside tenancy by construction — a table's own `tenant_id → organizations (id)`, which *is* the tenant check; the three references to `auth.users`; and `impersonation_sessions.actor_user_id → platform_users`, a table with no tenant column. Every tenant-scoped parent gains `unique (tenant_id, id)` so it can be a target.

**One deliberate exemption, and it is the interesting one.** A composite key is `match simple`, under which a row is exempt from the check if *any* key column is null. That is exactly right for an optional reference — a null `coupon_id` means no coupon, and no check should fire — because every `tenant_id` in the pair is `not null`. But `audit_log.tenant_id` **is** nullable, by ADR-033, precisely so platform-level events can be recorded. A composite key there would stop enforcing on the platform rows that most need an intact reference, and would do it silently. `audit_log` keeps its single-column key. Writing `match full` to force the issue was rejected: it would reject every legitimately null optional reference across the other 49.

**Why now, and not in Phase 5 when someone notices.** There is no data in these tables — the demo gym is seeded and disposable — so this is one migration and a test round. Once a paying gym exists it is a rewrite of every key under load, and every code path written before it would need re-auditing against a guarantee it was not built to expect. The alternative the owner declined is a Phase 2 rule that every Route Handler validates parent tenancy before insert: cheaper today, and a guard that can be forgotten in exactly one code path — which is the argument ADR-037 already made for privileges over guards, and it applies unchanged here.

**What it costs, stated because a blind author found it rather than the author of the rule.** Two assertions in the suite proved ADR-047's property *behaviourally* — that a tenant-scoped constraint stops one gym denying another a slot — and the only way to prove it was to perform the very write this ADR now forbids: gym B taking the live-membership slot for gym A's member, gym A booking gym B's trainer. Those writes are now rejected by the foreign key before the constraint is ever consulted, so the property they proved is no longer reachable behaviourally and is carried by catalogue shape alone. That is a real, if small, loss of coverage, and it is the correct trade — a defect that cannot be expressed is better than one that can be expressed and caught — but it should be visible rather than discovered later as a gap. The same blind author's sweep also found that **every cross-tenant write in the entire suite, visible and holdout, had been written deliberately by a test author to document the hole**, each with a comment saying so. Four independent blind authors found this gap, described it accurately, and asserted it as the contract, because it was the contract. This ADR is those four findings being acted on.

**Proven applied and green in run `34110039349`**: all four `db.yml` jobs, `Files=26, Tests=1591, Result: PASS`, with the demo gym seeded. The migration's own replay against Cloud is the violation check that matters — adding a foreign key validates every existing row, and this is the first migration in the project to run against real data, so a single cross-tenant reference among the seeded rows would have aborted it. There was none.

**What this does not do.** It does not make cross-tenant *reads* possible or impossible — RLS owns that and always did. It does not replace the tenant-scoped constraints of ADR-047/049; those stay, because defence in depth is the point and because a constraint is what turns a bad row into a denial of service. And it does not address OPEN-013, the absence of intra-tenant authorization, which remains the loudest thing Phase 2 must face.

**ADR-050 — A pgTAP suite that shares a database may never assert over a whole table.** The visible suite went green on run 34090125689 (`Files=18, Tests=793, Result: PASS`) and then failed on the very next run, with no test and no schema change between them. What changed was that the demo gym had been seeded (ADR-034) into the same Cloud project. Fourteen assertions failed, all of one shape: they run as `postgres` or as a **platform role** — which is deliberately not tenant-filtered — and then count or list the *entire* table, so `super_admin sees both gyms in organizations` wanted 2 and got 3, and `sees both gyms' members` wanted 2 and got 32. Every one of them was right about the schema and wrong about the world.

The rule this establishes is narrow and worth stating once: **an assertion may only make a closed-world claim about rows it scopes to its own fixtures.** A tenant-scoped assertion running as `authenticated` is unaffected — RLS already scopes it, which is why the great majority of the suite passed untouched. The exposed ones are exactly those that escape RLS. The property they exist to prove — "a platform role is not tenant-filtered: it sees gym A's rows *and* gym B's" — is provable by containment against a table with other rows in it; only the equality form needed the empty database.

Two things make this more than a bug fix. First, `docs/decisions.md` OPEN-006 already says this project will later hold a real gym's rows, so every one of these assertions was a time bomb with a known fuse — the seed just lit it early, which is the useful outcome. Second, it is the answer to a question ADR-030 left implicit: a suite that only passes on a fresh database is not a suite, and running against a shared project is what surfaces the difference. **The seed was worth running for this reason alone**, independently of gate 11.

**How the holdout half of this correction was made, stated to the standard ADR-044 set for itself.** Three assertions in `h01_tenancy_isolation.sql` carried the same defect and were fixed. They were **not** transformed by sha or by a mechanical regex, as ADR-043 and ADR-044 did — they were edited by a **dedicated blind holdout author**: a separate agent, its own context, whose absolute constraint throughout was never to open a main-repo migration or a main-repo visible test file, and which reported per-file `plan(N)` and a description of the rule it had corrected, never the content of an assertion. That agent read the holdout files, which is not a violation but its job; the property AGENTS.md rule 10 protects is that the *implementation* is not fitted to the tests and that the holdout is not fitted to the implementation, and both hold. **The orchestrator has never read a holdout assertion**, in this round or any other.

Two things weaken that slightly and are better said here than found later. First, the holdout author was **told the class of defect** — that a platform-role session counting a whole table assumes an empty database, that the seed now exists, and which tables the visible suite had failed on. That is information about the world and about the contract, not about the implementation, but it is more than a blind author would have derived alone. Second, in an earlier round it was given a CI run id and read that run's log filtered to the holdout step, to diagnose two of its own files; it reports not having read the visible half beyond the summary line the orchestrator had already quoted to it. Both are judgement calls made in favour of getting the defect fixed, and neither touched a migration.

Rejected: **seeding a separate project** — OPEN-006 already proposes that for Phase 5 and it does not fix these assertions, it only hides them again. Rejected: **raising the expected counts to today's numbers** — a count that encodes how much demo data happens to exist is the same defect wearing a new constant, and it breaks the next time the seed changes.

## Phase 2 decisions (identity & tenancy)

**ADR-053 — The model policy governs the phase's own reasoning, not every sub-agent it dispatches; mechanical work runs on Sonnet.** `docs/roadmap.md` says build phases run on Claude Opus 5 at `xhigh` and adds "do not lower effort or substitute a smaller model than Opus 5, and do not propose it." That sentence was written when a phase meant one session doing all of its own thinking, and it is the right rule for that. Phase 1 ran nineteen sub-agents, and several of them were transcription: taking a fully-specified shape and emitting it, sweeping a doc for a stale sentence, listing what exists. The owner observed during Phase 1 that those could have run on Sonnet and said so directly. **The split, now binding:** Opus 5 keeps every role whose value is in what the agent *notices* rather than what it was told — blind test authors (visible and holdout), blind critics, any implementer writing a security boundary (RLS, the access-token hook, the role matrix), and the orchestrating session itself. Sonnet takes the roles whose value is in doing exactly what a complete specification says — inventory, factual research, documentation sweeps, registry updates, emitting a migration from a shape that is already fixed. **The test this draws the line on:** if the brief is incomplete and the agent has to fill the gap with judgement, that gap is the deliverable, and it is Opus's. If a wrong output would be *obviously* wrong on inspection, it is Sonnet's. Rejected: keeping the blanket rule — it spends the phase's budget on transcription and, more to the point, the owner asked for the split. Rejected: reading it as licence to move a blind author or a critic to Sonnet — Phase 1's most valuable findings (ADR-047, ADR-049) came from critics noticing what nobody had specified, which is precisely the capability being paid for. This ADR is the deliberate amendment `docs/roadmap.md`'s own correction note demands: the policy changes *because someone decided to*, and the decision is recorded here rather than left to drift.

**ADR-054 - The access-token hook lives in `app`, runs as `security definer`, and swallows its own exceptions.** Three decisions in one function, each against the documented default.

*Schema.* Supabase's documentation puts the hook in `public`. Here it must not be: `supabase/config.toml` exposes only `public` and `graphql_public` to the Data API and `packages/db/types/database.ts` is generated from `public`, so a hook in `public` would be a callable PostgREST RPC, a permanent `schema-drift` diff, and a function on the public API surface whose entire job is deciding who is a super admin. The URI is `pg-functions://postgres/app/custom_access_token_hook`, and `scripts/__tests__/auth-config.test.ts` asserts the schema is `app` so a later edit cannot quietly move it.

*Privilege.* The documented alternative is `security invoker` plus table grants to `supabase_auth_admin` on every table the hook reads. Research against the primary sources could not establish how `supabase_auth_admin` interacts with RLS on those tables - the docs show the grants and say nothing about the policies. `security definer` under an owner holding `BYPASSRLS` removes the question: five table grants become one `execute` grant, `execute` is revoked from `public`, `anon` and `authenticated`, and the surface is one function callable by one role.

*The exception handler, which is the important one.* **A Postgres auth hook fails closed** - Supabase propagates the exception into an HTTP error and issues no token, with a two-second budget, and the hook is global. An unhandled exception in this one function locks every user out of sign-in **and** token refresh simultaneously, project-wide, with a CI round-trip as the floor on recovery. So the whole body sits inside `exception when others then return event;`: any failure yields a token with no Gymloop claims, which under Phase 1's arithmetic is a session that reads zero rows and raises nothing. Between "everyone is signed in and sees nothing" and "nobody can sign in", the first is a browser refresh away from recovery. This is not a licence to swallow errors generally - it is one handler at one boundary whose failure mode is a total outage, and it carries a comment saying so, or a later reader deletes it as sloppy. **Because a swallowed exception is silent, the hook cannot be validated by the product working**: it is proven by calling it directly with synthetic events. Measured before the migration was pushed, inside `begin ... rollback` against Cloud - a staff user, an expired member and a super admin each received exactly the claims the contract specifies, and six malformed events (`{}`, a null `user_id`, an unparseable uuid, an unlinked user, JSON `null`, a bare array) each returned the event unchanged without raising.

**ADR-055 - The role matrix: four gates, read and write as separate policies, and an invariant derived from the grants. Closes OPEN-013.** Phase 1 gave every tenant-scoped table one policy, `tenant_id = app.current_tenant_id()`, so any session carrying a gym's tenant claim read every row in that gym - every member's phone number, every payment, `staff.role`, `razorpay_accounts`. Phase 2 replaces that with a per-table read gate and write gate drawn from a 36-row matrix, plus a member-scoped read policy on the fourteen tables a member may see.

*Four gates and no fifth*: `app.current_app_role()`, `app.is_staff()`, `app.is_gym_admin()`, `app.is_front_office()`. Written as functions rather than inline role lists so that changing which roles count as staff is one edit rather than thirty-five, and closed as a vocabulary because a table needing a fifth distinct gate is evidence the table is wrong - v1 explicitly defers a per-permission matrix, and `app.can_do_x()` is how one gets built by accident.

*Read and write are separate policies, and this reversed the first draft.* One `for all` policy with the read gate on `using` and the write gate on `with check` looks right and is not: an `UPDATE` by a caller who may read but not write passes `using`, fails `with check`, and Postgres raises `42501`. Three requirements say such an update affects **zero rows**, and zero-rows-not-an-error is Phase 1's documented semantics (`docs/data-model.md`: *"a policy that raised would let a caller tell 'nothing here' apart from 'wrong tenant'"*). **Both blind agents reported this independently, in the same terms, neither having seen the other's output** - the blind arrangement producing exactly the signal it exists for. The spec was not edited to match the implementation; the design was changed to match the spec, because editing it would have been tests following code, the one failure hard rule 10 exists to prevent. Cost: 147 policies rather than 86.

*The invariant.* An earlier draft claimed "every table carries `_platform_select` and `_platform_write`" - 36 and 36. A blind author found the hole: four tables grant `authenticated` `select` only (ADR-047, ADR-049), and a `_platform_write` there permits what the grant denies, which is the very contradiction the gym-side rule was justified by. The rule now covers both sides once - **a write policy exists on a table exactly when `authenticated` holds `insert` or `update` on it** - giving 36 select and 32 write. Less tidy, and stronger: the meta-test derives the read-only set from `has_table_privilege` rather than from a list, so it keeps holding when a later phase changes a grant. `impersonation_sessions` is deliberately not among the four: its grant permits writes and a super admin creates sessions through it; what it lacks is `_tenant_write`, because a gym may read the record of being impersonated and may not author it. The read-only five are five for two different reasons, and conflating them is how the wrong table becomes writable.

*Also settled here*: `app.current_app_role()` returns `text` and never casts. The first draft had it raise `22P02` on an unrecognised label, by analogy with ADR-032's malformed-`tenant_id` rule. The analogy is wrong - a malformed uuid cannot mean anything, whereas an unknown role has an obvious safe meaning, *this session holds no privileges* - and the cast made one forged claim behave differently table by table, raising where a policy compared the role directly and returning zero rows where a gate function swallowed it. One consequence had to be handled downstream: `audit_log.actor_role` is the enum, so the role-change audit row resolves the label through the enum's own catalogue rather than casting, since a plain cast would raise on a forged claim and turn an audit write into a failed UPDATE - and an inline list of the seven labels would be a second role vocabulary, which is what ADR-031 deleted.

**ADR-056 - `is_active` becomes load-bearing, deactivation revokes live sessions, and the residual window is stated rather than designed away. Closes OPEN-009.** Two halves with different mechanisms. *The claim*: an inactive identity resolves to no claims at all, and resolution **stops** rather than falling through - a deactivated super admin must not silently become a member. *The tokens already issued*: a claim is a copy of a row taken at issue time, so a trigger deletes the user's `auth.sessions` rows when an identity stops being active or its `role` changes, invalidating the refresh token and making the access token in hand the last one that user holds. Verified against Cloud before being relied on (ADR-042's discipline applied to a privilege rather than a syntax): `postgres` holds `DELETE` on `auth.sessions` and `rolbypassrls`, and `auth.refresh_tokens` cascades from `sessions(id)`, so the refresh token goes with the session. `jwt_expiry` drops from the 3600 default to **900**, making the residual window fifteen minutes (**measured, not intended**: run `34180847888` read back `jwt_exp = 900` from the project after the patch, and a blind critic was right to flag that this ADR had stated it as fact before any run had applied it); the trade is that every refresh executes the hook, so this is a load decision as much as a security one. **The residual window is real**: between the trigger firing and the current access token expiring, a deactivated user still holds valid claims. Any requirement claiming otherwise would be false, and a test asserting otherwise would be testing a fiction.

*What "active" means was wrong in the first contract and is worth recording.* It said `is_active` for all three identity tables. `members` has no such column - its lifecycle column is `status`, and it also carries `erased_at`. Both blind agents caught it. A member is active when `status not in ('cancelled','blocked')` **and** `erased_at is null`: a `paused` or `expired` member signs in normally, because renewing is what they sign in to do and locking them out would break the loop the product is built around. `members` has no `role` column and so writes no role-change audit row; that asymmetry is real, not an omission.

*`platform_support` and `super_admin` finally differ.* The platform write policy is gated on `super_admin` across all 32 writable tables, so support reads everywhere and writes nowhere - including its own `platform_users` row, which under Phase 1 it could have updated to promote itself.

**ADR-057 - The first `super_admin` is created by a workflow that disables itself. Closes OPEN-001.** Gym self-signup requires super-admin approval, so the platform cannot bootstrap its first admin through it, and whatever does must not become a general-purpose way to create admins later. `.github/workflows/bootstrap-platform-user.yml` is `workflow_dispatch`-only, takes an email, and inserts one `platform_users` row **`where not exists (select 1 from public.platform_users)`**. The emptiness check is inside the statement rather than in a script: once the first row exists the workflow is permanently inert and a second dispatch is a no-op, not a second admin. It never creates an authentication identity - the person must already have signed up through the ordinary email flow - so it labels an existing `auth.users` row and cannot mint credentials. Every subsequent platform account is created by an existing `super_admin` through the ordinary policy, which is auditable and revocable. Inputs are escaped as SQL literals rather than bound, because `supabase db query` runs through the Management API and has no `:'var'` binding. `scripts/__tests__/auth-config.test.ts` asserts the `where not exists` clause survives and that the workflow never inserts into `auth.users` - deleting the guard is the failure that assertion exists to catch.

**ADR-058 - The hook is enabled by a three-field Management API patch, never by `supabase config push`.** `config push` exists in CLI 2.110.0 and is the obvious tool. It pushes the **whole** of `supabase/config.toml` - 414 lines of Supabase's local-development defaults, including `site_url = "http://127.0.0.1:3000"`, a local SMTP block, and storage and realtime sections never reconciled against what the Cloud project has - and there is no `--dry-run`. Enabling one hook that way would silently set a production project's site URL to localhost and break every email link the first time staff email sign-in is used. `.github/workflows/auth-config.yml` instead patches exactly `hook_custom_access_token_enabled`, `hook_custom_access_token_uri` and `jwt_exp`, and **refuses to run if `app.custom_access_token_hook` does not exist on the project** - because a Postgres auth hook fails closed, so pointing Auth at a missing function locks every user out of sign-in, and that ordering must not rest on someone remembering it. `config.toml` carries the same three values as the repo's record of the project, with a comment saying it is the record and not the mechanism. **The known unknown is now closed by measurement.** An open Supabase issue reports this endpoint refusing a `send_email` hook with "Auth Hooks can only be configured on Team or Enterprise Plans" on a project where the Dashboard had already enabled it, and whether the same gate applied to `hook_custom_access_token_*` was not established. **It does not**: run `34180847888` returned `HTTP 200` and read back `hook_custom_access_token_enabled = true`, `hook_custom_access_token_uri = pg-functions://postgres/app/custom_access_token_hook`, `jwt_exp = 900` on this project's plan. The guard in that workflow needed one fix first — it grepped for a JSON string while the CLI drew an ASCII table on the runner, so it refused a correct action and said something false while doing it; it now asks the database for a sentinel word, which survives every renderer. If it is refused for that reason the fallback is the Dashboard toggle, done once by the owner and recorded - never a wholesale `config push` smuggled in as a workaround.

**ADR-059 - Phases 3 to 6 ship vertical slices with real screens, and the Gauntlet Loop is calibrated to whether a mistake is silent.** Owner decision, 2026-09-08, taken at the end of Phase 2 on the grounds that *"all of these are taking time - we want an app"*. Two changes, and one admission.

*Vertical slices.* The master prompt's build order puts every screen in Phase 7, so the owner would see nothing clickable until five phases after Phase 2. Nobody decided that; it fell out of a build order in which the phase was the planning unit. From Phase 3, each phase ships **schema plus endpoint plus a screen** - Phase 3 ends with a staff login, a member list and a working QR check-in the owner can use. Phase 7 stops being "build the UI" and becomes "make it excellent", which is what its blind visual comparison was always for.

*Rigor calibrated to the failure mode.* The full blind arrangement - a visible test author, a holdout author in a separate repo, an implementer, and a fresh-context critic, none reading the others - stays in force wherever **a mistake is silent**: every RLS or policy change, the whole of Phase 5 (money), and any change to identity or the claim contract. It is relaxed for Phases 3, 4 and 6, where a defect is loud: one implementer, tests still written before the implementation, the EARS spec still human-approved first, every CI gate still enforced. The argument that Phases 1 and 2 earned the cost is not an argument that Phase 6 will - an RLS hole leaks another gym's members with nothing going red, and a broken check-in screen fails in front of you.

*The admission, because it is the larger number.* **Roughly a third of Phase 2's agent round-trips were contract churn.** `design.md` was edited four times *after* agents had been dispatched against it - deleting the `platform_users` special case, adding the role clause to the member policies, binding impersonation to `auth.uid()`, moving `reason` onto both audit rows. Each edit was correct and two closed real security gaps; each also forced re-work in three places at once, and twice an agent reported a failure that was only my having moved the contract underneath it. **The rule from here: fix the contract, then fan out.** A blind agent's finding that changes the contract mid-flight is evidence the contract was not ready to fan out from, and the cost of that is paid by every agent already working. This is a process defect, not a defect of the method - the blind arrangement itself found the unreachable-impersonation defect that would otherwise have reached Phase 7 as an unbuildable button.

Rejected: **dropping the holdout suite and the critics outright** to reach a demo fastest. The two findings that mattered most this phase - a requirement with no reachable implementation, and a test that passed against it vacuously - both came from roles this would have deleted, and both were in the identity layer every later phase authenticates against. Rejected: **keeping the current process unchanged**; it is not wrong, it is miscalibrated, and the owner is right that a week of invisible backend is a bad trade when the alternative costs assurance only where assurance is cheap to re-establish.

**ADR-060 - Four process cuts, taken at the end of Phase 2.** Owner decision, 2026-09-08, when asked what else costs time without earning it. Full v1 product scope is explicitly **kept** - leads, CSV import, the add-on catalogue, the messaging wallet and the super-admin console all stay, because competitors ship them (ADR-016/017).

**1. The holdout suite moves into this repo, at `supabase/tests-holdout/`. The second independent author stays; the second repository goes.** The separate repo exists to stop an implementer shaping code to tests it can see - but hard rule 10 already forbids the implementer reading `supabase/tests/**` at all, so the second *repo* adds almost nothing over a second *author*. What it costs is concrete: a separate push each round, ADR-043's parking rules, a CI checkout, and - the expensive part - the orchestrator cannot read a holdout file to diagnose a failure, so every red assertion becomes a round trip through its author. That happened five times in Phase 2. **Every finding the holdout side produced this phase came from a second person reading the contract independently, not from the tests being secret**: the missing `_platform_write` rule on grant-read-only tables, and the missing role term on the impersonator policy. Independence is the property worth paying for; secrecy was the part being paid for twice. ADR-043 is superseded. **Executed at the start of Phase 3, not now** - Phase 2 is one green run from done and rebuilding the test wiring underneath it would risk that for no gain.

**2. The pgTAP suite gets shared fixtures and stops asserting the matrix twice.** 1,928 assertions now take **688 wallclock seconds**, and every round trip pays that before anything else. Most of it is 35 files each building two gyms from scratch, plus the role matrix being asserted once visibly and once in the holdout. Target: under four minutes.

**3. One planning document per phase.** `proposal.md`, `design.md` and `tasks.md` merge into `plan.md`. The EARS spec stays a separate file, because it is what tests derive from and what a blind author reads. Phase 2's proposal and design overlapped by roughly half.

**4. ADRs get shorter** - decision, why, what was rejected. Phase 2's run to 600 words where 300 carries the same content. This entry is already twice the length the rule asks for, which is the point.

Rejected: **dropping the second author as well as the second repo.** The two findings that mattered most in Phase 2 - a requirement with no reachable implementation, and a test that passed against it vacuously - came from independent readers, and both sat in the identity layer every later phase authenticates against. Rejected: **cutting product scope** - the owner considered it and declined, and the market note in ADR-016/017 supports that.

## Phase 3 decisions (core domain)

**ADR-061 — Exactly-once check-in is a `before insert` trigger on `attendance` holding a transaction-level advisory lock, not a Route Handler and not an RPC.** ATT-004 says a repeat inside the gym's window is refused; the window is `organization_settings.checkin_dedupe_seconds`, per gym, so it is not a constant and can be neither an index expression nor a check constraint. That leaves a read followed by a write — under which both reads pass before either writes and the duplicate is silent. `app.enforce_check_in()` takes `pg_advisory_xact_lock` keyed on (tenant, member) **before** it reads the window, so a concurrent check-in for the same member blocks until the first transaction's row is visible or gone. The function is `volatile`, and that is load-bearing rather than a default: a volatile PL/pgSQL function takes a fresh snapshot per statement, where a `stable` one would reuse the snapshot the INSERT took *before* it blocked on the lock and look straight past the row it exists to find. Cost: two check-ins for the *same* member serialise and no others do.

*Where it attaches is the decision, not which mechanism it uses.* `attendance` grants `insert` to `authenticated` and `attendance_tenant_write` admits any front-office session, so a screen inserting through `supabase-js` is supported architecture, not abuse — a rule living in a Route Handler therefore has a documented way round it. On the table, every writer meets it: this app, a direct PostgREST insert, a future Edge Function on `service_role`, and Phase 7's offline replay. Rejected: a unique index or exclusion constraint over a time bucket (the bucket width is another table's column, and index expressions must be immutable); `SERIALIZABLE` (PostgREST is READ COMMITTED, the level is not reachable per request, and it becomes a retry loop the caller must write); a `security definer` RPC as the *sole* guard (same hole as the handler, one indirection further away).

*A duplicate and a retry are different questions.* A repeated **scan** is a second attempt inside the window and is refused (`GL014`). The same **client event id** twice is one attempt arriving twice — a network retry or a replay — and is absorbed: the window check excludes rows carrying the same `client_event_id`, so the resubmission reaches `attendance_tenant_id_client_event_id_key`, and the Route Handler answers 23505 with the row that already exists. Erroring there would make a correctly-retrying client look broken.

*It is `security invoker`, and the first draft was `security definer` — which was an information oracle.* A `before insert` row trigger fires ahead of `ExecWithCheckOptions`, so **every `authenticated` session reaches the function**, including roles the matrix denies the tables it reads; under definer rights owned by `postgres` (`rolbypassrls`) those lookups ran past RLS, and the refusal messages leaked what the reads could not. A `member` reads their own `attendance` rows, which carry `qr_session_id`, and `qr_sessions_tenant_select` is `is_front_office()` — so replaying that id returned `GL011`/`GL012` carrying `expires_at` and `revoked_at`, and `GL014` carried `checkin_dedupe_seconds` from `organization_settings`, which the matrix puts on the list of what a member may not see. It also contradicted `docs/data-model.md`'s own semantics, *"a policy that raised would let a caller tell 'nothing here' apart from 'wrong tenant'"*. The fix is one word, not a branch: under invoker a denied probe finds nothing and gets the same `GL010` as an id that never existed, and no policy is restated inside the trigger. Verified from the live catalogue that the happy path survives — `attendance_tenant_write` is `is_front_office()`, `qr_sessions_tenant_select` is `is_front_office()` exactly, and `attendance`/`memberships`/`organization_settings`/`members` `_tenant_select` are all `is_staff()`, with `is_front_office()` ⊆ `is_staff()` — so no role that may insert attendance is unable to read anything the trigger reads. `pg_advisory_xact_lock`, `md5` and `make_interval` are all `EXECUTE`-to-`PUBLIC`, so the lock needs no privilege of its own. **Fifth instance in two phases of the same shape: a rule whose correctness is held by a different component than the one being protected** — here inverted, the trigger holding a privilege the policies were meant to hold.

*What the tenant guarantee actually is, stated because "closed by a policy" and "never read from the row" differ.* The lookups still filter on the caller-supplied `new.tenant_id`; what stops it naming another gym is RLS on each table read, `attendance_tenant_write`'s `with check`, and ADR-052's composite foreign keys. It is the policy, not the absence of the read.

*Two consequences worth stating rather than discovering.* The window query excludes `a.id <> new.id`, because a `before insert` trigger fires for every proposed row of an `insert … on conflict do update` — which is the shape `supabase/seed.sql` uses to stay re-runnable, so without it the second seed run would find each attendance row duplicating itself. And a gym with **no** `organization_settings` row has configured no window and gets none: falling back to the column's own default would put a second copy of `120` in the code, which is exactly the constant a single-gym test cannot distinguish from correct.

**ADR-062 — `attendance_front_desk_has_assist_chk` tests `assist_reason !~ '^\s*$'`.** Phase 1 wrote `<> ''`, which accepts three spaces; the row is the audit trail for one person marking another present, and whitespace is the shape a form submits when somebody tabs past the field. **The first fix had the same hole one turn further in**: bare `btrim()` strips *spaces only*, so `btrim(E'	
') = ''` is false and a tab-and-newline reason still passed — the holdout author found it by testing a tab where the visible suite tested spaces. The rule is "contains a non-whitespace character", which a regex states totally and a trim cannot; the regex also takes no second argument that can be forgotten. Measured against Cloud under the final definition before the constraint was dropped and re-added (which validates every existing row): 585 rows, 68 with a reason, **0** matching `'^\s*$'`. Scope deliberately stops there — `attendance_corrections_reason_chk` has the same weakness and belongs to whichever phase builds corrections. The generalisation, since this was the third time in the phase-pair that one end of a range was bounded and the other left free: **a predicate that enumerates what to strip is weaker than one that says what must be present.**

**ADR-063 — A request schema lives in `packages/shared/src/api/`, and the gate code is minted in `apps/web/lib/`.** `checkInRequestSchema` is in `shared` because mobile posts the same body in Phase 7 and because `shared` already depends on zod — adding a validator to `apps/web` would have been a second one. `newGateCode`/`hashGateCode` could not follow it there: `packages/shared` forbids Node builtins by construction (`shared-not-to-node-core`) and the code is hashed with `node:crypto`. Rejected: hashing in Postgres with `pgcrypto`, which would add an extension to make a Route Handler's job the database's.

*Two things beyond the brief, named so they are not mistaken for scope creep found later.* `POST /api/gate-code` exists because `qr_sessions` stores only a hash, so a code cannot be conjured by a seed row or a SQL insert — without an issuing endpoint the scan half of check-in has nothing to scan and cannot be demonstrated. And `GATE_CODE_TTL_MS` is a constant rather than per-gym configuration because `organization_settings` has no column for it; **it is not the de-duplication window**, which is per gym and must never be given a constant to fall back on.

## Phase 3 decisions (core domain)

**ADR-064 - "Paused" is a condition that is derived, not a status that is flipped.** Approving a pause stamps `membership_pauses.approved_at`; **nothing sets `memberships.status = 'frozen'`, and nothing should.** The implementer found the gap and correctly declined to invent half a mechanism for it.

The obvious repair is a job that flips the status when a pause starts and flips it back when it ends. That needs a scheduler this project does not have, and it creates two representations of one fact which can disagree - a pause row that says paused and a status column that has not caught up yet, or vice versa after a failed run. Instead, **a member is paused when an approved pause covers the date being asked about**: `approved_at is not null and rejected_at is null and <date> between starts_on and ends_on`. NSH-002's consumer is the no-show scan (Phase 4), which is already a per-gym dated query and can ask that question directly. Rejected: flipping the status on approval - a pause may start in the future, so approval and effect are different moments.

**Consequence, stated because it is load-bearing:** `memberships_member_id_live_key` treats `active` and `frozen` as live, and a paused member stays `active`, so a pause does not free the slot for a second membership. That is correct - a paused member has not stopped being a member - but it means `frozen` is currently written by nothing. Phase 5 should either use it or delete it rather than leaving a status the schema offers and the product never sets.

**ADR-065 - `pause_approver_role` may not name a role the matrix forbids from writing pauses.** `organization_settings_pause_approver_role_chk` permits `trainer`; `membership_pauses_tenant_write` is `is_front_office()`, which is `(gym_owner, gym_manager, front_desk)`. A gym that configures a trainer as its pause approver therefore has an approver **RLS refuses to let approve** - the setting and the policy contradict each other, and the setting loses silently. Verified against the live catalogue.

The matrix wins, because it is the security boundary and the setting is a preference. The check constraint narrows to the three roles that can actually write, which turns a silent misconfiguration into a rejected one at the moment somebody tries to save it. Rejected: widening `membership_pauses`' write gate to include trainers - a trainer approving a member's freeze is a commercial decision, and ADR-055's whole argument is that the matrix is not widened to fit a setting.

**ADR-066 - The recurring defect of this project, named so it can be looked for.** Five instances across Phases 2 and 3, each found by a blind reader rather than by the author. The shape is **a privilege boundary crossed for convenience, in a component that runs before the boundary it was meant to respect.** That framing is the visible-suite author's, and it is better than the one this ADR started with ("a rule whose correctness is held by a different component"), because it says what to *look for* rather than what to regret: **every `security definer` function and every `before` trigger, checked as a pair.**

The five:

1. **`<t>_member_select` trusted the hook** to never mint a `member_id` claim alongside a staff role. Closed by naming the role in the policy.
2. **`impersonation_sessions_impersonator_write` had no role term**, so a `front_desk` token carrying an impersonation claim could end the session. Closed the same way.
3. **The exactly-once check-in guard was specified as a Route Handler or an RPC.** `attendance` grants `insert` to `authenticated`, so a direct `supabase-js` write bypasses both. Closed by attaching the guard to the table.
4. **`impersonation_sessions_platform_write` gated the caller and not the `actor_user_id` column**, so an admin could create a session attributed to somebody else. Closed with `actor_user_id = auth.uid()`.
5. **`app.enforce_check_in()` was `security definer`**, owned by a `BYPASSRLS` role, and being a `before insert` trigger it ran ahead of the policy. So *any* `authenticated` session could reach four tables past RLS: a member replaying a `qr_session_id` out of their own readable `attendance` row got `expires_at` and `revoked_at` back in an error message, from a table the matrix gives them zero rows of, and `GL014` printed `checkin_dedupe_seconds` from a table they may not read at all. Closed by **deleting `security definer`** - every role that may insert attendance already holds all four reads (`is_front_office()` ⊆ `is_staff()`), so the elevation bought nothing and cost the isolation.

**Neither half of #5 is a defect alone.** Reading past RLS is what `security definer` is for; running before the policy is what `before` triggers do. The pair is the defect, and that is why it survived an implementer, two blind test authors and me until somebody looked at the two facts together.

**The pattern has two directions, and the search that finds one does not find the other.** This refinement is the visible-suite author's, after the sixth instance turned up looking nothing like the fifth.

- **Elevating past a boundary that was already sufficient.** `app.enforce_check_in()` as `security definer`: the policy was correct, and the function was handed rights it did not need, in a component that runs before the policy. **Found by grepping `security definer` against `before` triggers** — a mechanical search, and it is now in `docs/data-model.md` as a rule.
- **Leaving a rule outside a boundary that never claimed it.** `pause_approver_role`: the policy was correct *and complete for what it claimed to govern* — `is_front_office()` really is who may write a pause — and the rule it did not claim lived in a Route Handler the policy could not see. **No grep finds this.** It is found only by reading each policy against the rules its table is supposed to carry, and asking what the policy does *not* say.

Both are a privilege boundary and a rule disagreeing about who enforces what. The first over-reaches; the second under-claims. A review that only runs the mechanical search will keep finding the fifth kind and never the sixth.

**The rule that follows:** a `security definer` function must justify its elevation against *what its callers already hold*, in a comment, at the point of definition. Where the callers already hold every read, invoker is not merely safer - it is the only version that cannot become an oracle. And the fix is deleting a word, never adding a permission check inside the elevated code: that check is a second copy of the policy, and the copy is what goes stale.

**ADR-067 - The seventh and eighth instances, both inside ADR-066's own fix, and a second rule that follows.** `app.enforce_pause_decision()` was written to close instance #6. A blind pgTAP suite written from `openspec/.../pause-decision/spec.md` by an author who had not read it found two more defects in it, and neither is a variation of the first — they are new shapes worth naming.

- **The trigger was `before update` only.** Every check was hung on the *transition* from pending to approved, and an `insert` has no transition: it arrives at the destination. `membership_pauses` grants `insert` to `authenticated` and its write policy is `is_front_office()`, so one statement — self-requested, self-approved, wrong role — met no rule at all. **The generalisation: "on the table, not in a caller" is not finished until it says *which statements*.** A trigger is a boundary with an event list, and an event list is a place a rule can be left outside of, exactly like a caller. `04_contract_meta.sql` had *pinned* the narrow event list with an assertion arguing that widening it would be someone tidying the rule into symmetry — a guard written around the defect, defending it.
- **A decided pause could be re-decided.** The guard read `if new.approved_at is null or old.approved_at is not null then return new;`, written from the spec sentence "apply the approval rules only to a pause that has not already been decided". It does exactly that: it stops applying the rules, **and then allows the write.** A second, equally-qualified manager could overwrite `approved_by_staff_id`, so the record of who authorised money the gym would not collect became the record of whoever wrote last. **The rule: in a `before` trigger, falling out of a branch is a decision to permit.** "Not governed by this rule" and "allowed" are the same statement there, and a spec that says a rule does not apply is never saying the write is fine — it is saying some *other* rule decides, and if no other rule does, the trigger has just authorised it.

Both were found because the spec was written first and the tests were written from the spec by someone who could not see the code — the arrangement ADR-059 says to reserve for rules whose failure is silent. Both of these fail silently.

**ADR-068 - Two more shapes, both found inside ADR-067's fix, and the thing they have in common.** The Phase 3 critic returned NO-GO on the migration written to close ADR-067, and three of its five defects were introduced by that migration. The pattern is not carelessness; it is that each fix moved a rule to a new place without re-asking what the rule was reading.

- **Widening a trigger's event list changes what the guard compares against.** `before update` becoming `before insert or update` forces every `old.` to become `new.`, because OLD is null on an insert — and at that instant a check comparing the caller's input against a **recorded fact** silently becomes a check comparing the caller's input against **the caller's other input**. ADR-067's fix did this to the separation-of-duties rule on freezes: `new.approved_by_staff_id <> new.requested_by_staff_id`. A manager refused on the straight self-approval sent one UPDATE moving `requested_by_staff_id` onto a colleague and approving it, and the record then named an employee who never asked. **The generalisation: a rule of the form "A must differ from B" is a control only if at least one of A and B is outside the writer's reach in that statement.** So the fix is not a better comparison; it is making the requester immutable (restoring the recorded fact) and refusing an insert that arrives already decided (where no recorded fact can exist).
- **Authorization by absence, and a carve-out that tests the claim instead of the session.** `if app.current_staff_id() is null then return new;` reads "if I cannot identify you, you are trusted." It was written for `postgres` and `service_role`; its actual extension is every `authenticated` session whose token carries no `staff_id` — and Phase 2's claim contract mints exactly that for **impersonation**: `app_role = 'gym_owner'`, a `tenant_id`, no `staff_id`. Such a session passed `is_front_office()`, met no pause rule at all, and could rewrite who authorised a freeze. `docs/security.md` says an impersonating token has the gym's reach and not more; it had more than a real owner's. **The rule: a trusted-context carve-out must test the property it means** — "no policy applies to this session" — not a symptom that correlates with it. The predicate is `row_security_active('public.<table>')`: false for the table owner and for any `BYPASSRLS` role, true for an ordinary signed-in one, naming no role at all.

  **The same carve-out had a second failure in the opposite direction, and the blind holdout author found it by accident.** Testing the *claim* meant `postgres` and `service_role` fell OUT of the exemption whenever `request.jwt.claims` happened to be populated — which PostgREST does on every request including a `service_role` one, and which every pgTAP file here does and never clears. So an Edge Function writing a decided pause would have failed in production, and a fixture write after a claim block was judged against a stale identity. It failed closed, surfacing as a `GL020` naming a staff member with nothing to do with the write, which is why nobody had seen it. The author noticed because two assertions went from green to red when an *unrelated, earlier* claim block changed — **a test whose result depends on what ran before it is telling you something about the system, not about the test.** `current_user <> 'authenticated'` was the first fix here and is also wrong, more subtly: it hardcodes a role name that `pg_roles` already knows the answer about.

**And one non-defect worth recording, because the obvious fix was wrong.** The critic's D4 — every check-in rule reachable by UPDATE, since all of them hang on a `before insert` trigger — was first fixed by `revoke update on attendance from authenticated`. Three suites said no: `06_attendance_checkin` asserts that grant under INT-001, and `06_attendance_rls` exercises `set checked_out_at`, because **a check-out is a legitimate later write to a visit that already happened**. Narrowing the write policy to `for insert` fails the same way and additionally destroys the `with check` that refuses moving one's own row to another gym. The answer was to freeze the columns that carry *what the check-in meant* and leave writable the ones that record *what happened afterwards* — with `tenant_id` deliberately left out of the frozen set, because the policy's `with check` already refuses that and a `before` trigger raising first would answer ahead of the policy, which is ADR-066 exactly. **A hole closed by deleting a feature is not closed; the tests are what said so.**

**ADR-069 - `num_failed()` is not verification, and a bare `select` in a pgTAP file can invent passing tests.** Local replay against Cloud (ADR-042) runs a file through `supabase db query -f`, which returns only the LAST result set — so the loop has been reading `select num_failed()` and calling a file green. CI runs `supabase test db`, which is psql in tuples-only unaligned mode piped into `prove`, and prove checks the whole TAP **stream**: the count against `plan()`, and the numbering.

`h17_pause_decision_holdout.sql` passed locally with `num_failed() = 0` and failed CI with `Tests: 48` against `plan(38)` and 39 "Tests out of sequence" errors. Nothing was failing. Its setup writes were bare top-level statements — `select pg_temp.attempt($$…$$);` — and the helper returns the string `ok` on success. psql printed that value on a line of its own, and **a bare line reading `ok` is valid TAP**, so prove counted ten tests nobody wrote.

Two things follow, and the second matters more than the first:

- A discarded helper result must not reach stdout: `do $$ begin perform …; end $$;`, or better, consume it in the assertion that was going to check the state anyway (`select is(pg_temp.attempt($$…$$), 'ok', …)`), which also turns a silently-failing setup write into a visible red instead of a later assertion failing for a confusing reason.
- **No CI gate was added for this**, deliberately. `prove` already catches it exactly and structurally; a second heuristic check ("no bare top-level select of a non-assertion function") is the kind that eventually lies, which this project has already paid for twice — `check-pgtap-rollback` reading `end;` inside a dollar-quoted block as a top-level `END`, and the auth-config guard grepping JSON while the CLI drew an ASCII table. The gap was in the *local* method, not in the gates, and the fix is to stop treating `num_failed() = 0` as proof that a file is green.

**ADR-070 - Round two: a rule can be outside the writer's reach on one statement and inside it on another, and a frozen-column list is stale the day it is written.** The Phase 3 critic's second pass verified every fix from the first pass is real *in the database* — then found five more defects, three of them created by those fixes. That is now three rounds running where the fix produced the next defect, which is the most reliable fact about this phase and worth more than any individual bug.

- **Immutability is per-statement, and INSERT is a statement.** ADR-068 made `requested_by_staff_id` immutable so that "the approver must differ from the requester" compared against a recorded fact. It did so on UPDATE only — and INSERT is where the column *enters*, so it was still whatever the caller typed, or null. A manager inserted a pause naming a colleague as requester and approved it in the next statement: two statements, one person, no rule crossed, and the record names an employee who never asked. **ADR-068's own sentence is what this violated**, so the sentence needs its second half: a rule of the form "A must differ from B" is a control only if one of A and B is outside the writer's reach in that statement — *and you must check every statement that can set it, not only the ones that can change it.* The fix already existed one function away: `GL016` refuses a supplied `assisted_by_staff_id` that is not the acting staff member.
- **A denylist of frozen columns goes stale, and both of ours already had.** `attendance` named nine columns and left four writable — `id` (the identity `POST /api/check-in` returns and `attendance_corrections` points at), `created_at`, and `offline_recorded_at`/`replayed_at`, Phase 7's provenance, forgeable onto a row that was never offline. `membership_pauses` named seven and left `id` and `created_at`. Neither list rotted over time; **both were incomplete on the day they were written**, which is what a denylist does — it asks an author to enumerate a set that grows without them. Comparing rows as `to_jsonb(new) - <allowlist>` against `to_jsonb(old) - <allowlist>` inverts the default so a column added tomorrow is frozen unless its migration deliberately says otherwise.
- **Half a rule is not the rule.** The born-decided guard named `approved_at` while the comment above it stated the general case, so a pause could be created already *rejected* — permanently recording a refusal against a request nobody made.
- **Base64 is not a signature.** The member cursor's `id` was interpolated raw into a PostgREST `or=(…)` filter, where `,` `.` `(` `)` are grammar, and `decodeCursor` checked only `typeof id === 'string'`. A hand-crafted cursor appended its own `WHERE` fragment — proved against the live API. It crossed no tenant (RLS still filtered, the caller was already staff of that gym), and saying so plainly matters: **the shape is the defect and the blast radius was luck.** A comment in that file asserted encoding "stops a caller hand-crafting one", which was never true of any encoding.
- **Two more comments that stated what the code does not do**, both introduced by the previous round's fix: a migration header describing the carve-out as `current_user <> 'authenticated'` when the code twelve lines below used `row_security_active()` (and ADR-068 records the former as the *wrong* answer), and a shared-package docstring claiming the table refuses to let the requester change — true of UPDATE, false of INSERT, and the sentence a reader would have trusted instead of re-deriving the hole above. This project has now paid for a false comment three times. **A comment that says a gap is closed is load-bearing: it stops the next person looking.**

**ADR-071 - A rule that reads a nullable column is not a rule until the null case is written down.** Both defects of the Phase 3 critic's third round were this, in different clothes, and both were introduced by the migration that closed the second round.

- **`null is distinct from null` is false.** The requester guard was written as `new.requested_by_staff_id is distinct from v_actor_staff_id`, which fires when a session with a staff id names somebody else — and is silent when a session with *no* staff id names nobody. That session is the one the spec singles out: `app.custom_access_token_hook` mints `app_role = 'gym_owner'` and a `tenant_id` for a live impersonation and deliberately no `staff_id`. The same NULL logic then finished the job downstream — `GL021` asks `<uuid> is not distinct from old.requested_by_staff_id`, also false against NULL — so a manager approving the row in good faith satisfied the two-person rule against nobody. **The fix is to name the null case as its own clause, not to adjust the comparison.**
- **A column governed only inside a branch is ungoverned outside it.** Every rule about `approved_by_staff_id` sits below `if new.approved_at is null then return new;`, so for as long as there was no approval the column was anyone's to write: a rejection could name a false approver, and the settled guard then froze it beyond correction. Fixed with a `check` constraint rather than another branch, because **this is the third consecutive round in which a rule attached to a branch was got round by arriving where the branch did not run** — the requester guard, the approver columns, and the trusted-context exemption bleeding into a rule it was never meant to exempt. A constraint holds for every writer including `postgres` and `service_role`, which the trigger exempts by design.

**And the defect was already in our own demo data.** `seed-scenarios.sql` named an approver on its *rejected* pause — the exact forgery, sitting in the fixtures while three critic rounds looked past it. Fixtures are not exempt from the rules they set up; a wrong row there is a rule nobody will think to check.

**Fourth false comment of the phase**, and the one that matters most: the migration introducing the requester guard said "a null actor is refused too, which also closes an impersonating session forging a request." It did not — one round after ADR-070 named that exact failure mode. **A comment claiming a gap is closed must be verified like code, because its whole function is to stop the next person looking.**

**ADR-072 - A rule that only refuses belongs after the policy; only a rule that must modify the row needs to run before it.** ADR-071's fix added the requester rule to `app.enforce_pause_decision()`, which was a `before insert` trigger — so it began answering for rows `membership_pauses_tenant_write` owned. Four assertions in three suites went red, each asserting that row security is what refuses a write it owns: a cross-tenant insert, an insert with no claims, and a trainer writing in their own gym.

**This is ADR-066 reintroduced by a migration that cites ADR-066.** Every migration in the sequence kept `tenant_id` out of its comparisons for exactly this reason, and then a new rule was added to the same `before` trigger without asking the same question of it. Worth being plain about why that is easy to do: the discipline had been applied *per column*, and it is really a property of *when the trigger runs*.

Two repairs were rejected and the reasons are the content:

- **Gate the rule on the tenant matching.** Closes three of four. The trainer is refused by the policy on *role*, in their own gym, so the trigger still answers first.
- **Gate it on `is_front_office() or is_platform()`.** Closes all four and is precisely what ADR-066 forbids — a second copy of the policy inside the function, and the copy is what goes stale. **It would have passed CI and been wrong**, which is the reason to write this down rather than just fix it.

The repair is the trigger's timing. The function only ever raises; it never modifies `new`. Under `after insert or update` it fires once row security has admitted the row, so a caller the policy refuses gets `42501` from the policy and a caller it admits meets every rule unchanged — raising in an AFTER trigger aborts the statement just the same. `app.enforce_check_in()` correctly stays `before insert`: it stamps `tenant_id`, `branch_id` and `assisted_by_staff_id` onto the row, so it must run first, and its refusal messages were made safe a different way (ADR-066, `security invoker`).

**The process failure is worth more than the defect.** Every earlier round replayed all 39 pgTAP files against the candidate migration before pushing; this round replayed only the four the change targeted, and CI found what the shortened sweep missed. The four broken assertions were in suites the change "obviously" did not touch. **The full sweep is not optional — a trigger is a global object, and "which files does this affect" is the question a regression suite exists to answer for you.**

**ADR-073 - Never cap the output of a failure list.** The pre-push sweep replays all 43 pgTAP files and prints one line each. It reported five failures. I read it through `grep -v 'failures": 0' | grep -v '^tests:' | head -5` — and because a `git commit` had been chained into the same command, its two lines of output consumed two of those five slots. I saw three failures, fixed three, pushed, and CI found the two I had hidden from myself.

**The sweep was right; reading it was wrong.** That is the more dangerous shape, because the instinct afterwards is to distrust the check.

The rule, and it generalises past this repo: **a `head` on a list of successes is a convenience; a `head` on a list of failures is a filter that hides exactly what you are looking for.** Cap output when the risk is noise. Never cap it when the risk is a miss.

Two habits follow. Print the count alongside the list, so a truncated read is visibly inconsistent with the total — the same property that caught `check-pgtap-rollback` skipping untracked files, where "40 files checked" against 41 on disk was the tell. And do not chain unrelated commands into a diagnostic, because their output shifts the window you are about to truncate.

Related, from the same afternoon: this project's sweep splices an unapplied migration into each test's transaction. That is sound, but it means **the sweep can only be trusted as far as its own plumbing** — read its total, not its first few lines.

**ADR-074 - `revoke ... from public` does not revoke what Supabase granted by name.** Phase 4 shipped `public.run_no_show_scan_all()` with `revoke all on function ... from public`, a comment saying the grant was revoked from `authenticated`, and a registry entry repeating it. The ACL disagreed with all three: `{postgres=X,anon=X,authenticated=X,service_role=X}`. Supabase's default privileges grant EXECUTE to `anon` and `authenticated` **explicitly**, and revoking PUBLIC leaves an explicit grant standing.

Proved over the live API before it was believed: a front-desk session ran the nightly job and opened seven real cases; a **member** — not staff at all — got 200; a trainer got 200. Only `anon` was stopped, and only because it lacks `usage` on schema `app`, which is luck rather than design.

**Phase 2 had already got this right and Phase 4 copied the intent instead of the statements.** `20260907184313` revokes `app.custom_access_token_hook` from `public`, `anon` and `authenticated` in three separate statements. The rule: **revoke by name, then verify with `has_function_privilege` — the ACL is the only thing that knows.** A comment claiming a grant was revoked is the fifth false comment this project has shipped (ADR-070, ADR-071), and this one was repeated in `docs/registry.md`, which is how a reader would have confirmed it and been wrong twice.

**ADR-075 - The scan read expiry off a column nothing writes, three paragraphs after explaining why not to.** `app.run_no_show_scan()` opened with a careful argument for deriving *paused* from `membership_pauses` rather than from `memberships.status` (ADR-064) — and then filtered `status in ('active','frozen')` with no reference to `ends_on`. `grep -rn "'expired'"` across migrations, `apps/` and `packages/` returns only the enum's own definition: **nothing in this product ever writes that label**, because a status flip needs a scheduler ADR-064 says does not exist.

So a lapsed membership reads `active` for ever, the scan opens a churn case for it, and — because absence keeps growing — that case climbs to the **top** of the red list: the first person the front desk is told to ring every morning, about a membership that ended weeks ago. The demo data already contained one.

The generalisation, which is the part worth carrying into Phase 5: **an argument for deriving one fact from evidence is an argument for deriving every fact of that kind.** Having made the case for pauses, the same paragraph should have been asked of expiry, of cancellation, and of anything else a status column claims. A rule that is right about one column and silent about its neighbour reads as deliberate.

**ADR-076 - A correction is not a decision.** `app.enforce_follow_up()` derived the case's status from every insert, including one carrying `corrects_follow_up_id`. So staff logging "will return — ring Friday" and then correcting a wrong outcome a minute later — through the append-only log's only mechanism for fixing anything — silently moved the case out of `follow_up_due`, nulled `next_follow_up_at`, and dropped it from the index built for "which follow-ups are due". Nobody rings on Friday.

Two properties make this the worst kind of defect this project has produced: it is **caused by staff doing exactly the right thing**, and its symptom is a member who is never called, which nothing in the product surfaces. The spec had not asked the question; the answer it fell into was the harmful one. **When a spec is silent about what a corrective action does to derived state, that silence is a requirement waiting to be written, not a detail to be inferred.**

**ADR-077 - The scan is scheduled by `pg_cron`, not by a deployed Edge Function.** OPEN-007 is closed. The critic found the loop correct, reachable and never started: no `cron.job`, no schedule in `config.toml`, no workflow deploying the function.

`pg_cron` over the Edge Function for three reasons in order of weight. **It needs no public endpoint** — and ADR-074 is exactly what a public endpoint costs when one grant is written wrongly. It needs no deployment step and no secret, so there is no state in which the migration is applied and the job is not running. And it cannot depend on Vercel, which is `docs/architecture.md`'s own stated reason for wanting an Edge Function here — while depending on less.

`supabase/functions/no-show-scan` is kept as the operator's manual re-run path, because a re-run after an incident is a real need and not one an operator should need `psql` for. `pg_cron` is authoritative; the function is a door, not a schedule.

**ADR-078 - A checker whose failure mode is silence is worse than no checker.** The pre-push sweep replays all 43 pgTAP files and prints one line each. It reported "42 files, all green" for a run in which one file had failed and another had been swallowed — and I pushed on it six times. **The DB workflow was red on `main` for six consecutive commits while commit messages claimed "43/43 green, every gate."**

The mechanism, because it is subtle and general: `supabase db query` returns only the **last result set that has rows**. On a passing file `finish()` returns no rows, so a `num_failed()` placed before it is what comes back. On a **failing** file `finish()` emits its `# Looks like you failed N tests` diagnostic row, which shadows `num_failed()` entirely. So the script could observe passes and nothing else: a failing file produced no matching output, `printf` had already written its name with no result beside it, and the *next* file's name landed on the same line and donated its result.

That one bug explains every anomaly of the evening — "42 results for 43 files", a file that appeared to vanish from the run, and six red CI runs believed green. The count is now stashed in a temp table before `finish()` and selected back after it, so it is the last rows on the wire either way.

**Three rules follow, and the third is the one that would have caught it:**

- Detection and diagnosis stay separate paths (ADR-073's second half, restated because it recurred).
- Count what you got against what you expected; a missing line is a result.
- **Test a checker in both directions.** Point it at something known to be broken and confirm it says so. Every check written in this project until now was verified only against a passing input, which proves it can say yes and nothing about whether it can say no.

**ADR-079 - `Number.isInteger` is not "an integer Postgres will accept".** The red list's cursor guard accepted anything typed `number`, so `Infinity` reached PostgREST. The fix used `Number.isInteger` and its comment asserted that `1e21` is not an integer. **`Number.isInteger(1e21)` is `true`** — it is a mathematical integer whose *string form* is `"1e+21"`, and the string is what reaches the database, which rejects it as `22P02`. `2147483648` passes the same guard and gives `22003 integer out of range`. The page renders `errorMessage` verbatim, so both blank the red list and show a Postgres error to a front desk.

The check that means something is the **column's own domain**: `days_absent` is a Postgres `integer`, so the cursor's number must be one — `isInt4`, bounds included. The general form: *a validity check on a value crossing into another system must be written against that system's type, not against the sending language's nearest equivalent.*

Contributing cause, worth as much as the rule: **there was no test file for `apps/web/lib/red-list.ts` at all.** The roster had one and its cursor survived two critics; the red list had none and its cursor was wrong twice.

**ADR-080 - A case is closed when its membership ends, and "live" means all three states.** ADR-075 stopped the scan *opening* a case for a lapsed membership and nothing closed the ones already open. Two were live in the demo gym, a third arrived the next day on its own — no manual writes, just a membership reaching its end date. Because `days_absent` grows without bound they climb to the top of the red list and stay there, so the front desk is told every morning to ring the person at the top about coming back to a membership that no longer exists. It is also an inconsistency the product does not mean to have: the same member is in scope or out of it depending only on *when* their membership expired relative to the case.

**The wording was settled by a blind test author refusing to guess.** The requirement first said "an open case"; the author pointed out that `no_show_cases_tenant_id_member_id_open_key` treats `open`, `contacted` and `follow_up_due` as one open case, asserted only the literal reading, and reported the ambiguity instead of choosing. It means all three — the red list renders everything that is not `returned` or `closed`, so a `contacted` case for a lapsed member sits at the top exactly as an `open` one does, and one somebody has already rung about is the more embarrassing to keep suggesting.

Two details worth the lines: the case is **closed, never deleted** (NSH-005's reasoning — it is the record that somebody tried), and `returned_at` is deliberately **not** stamped, because they did not return, their membership ended, and reusing that column would make the two indistinguishable in the only place the difference is recorded.

**ADR-081 - The Edge Function is deployed by CI, on push.** A blind critic found `supabase/functions/no-show-scan` documented in two places as an operator's manual re-run path and **never deployed** — `supabase functions list` returned `{"functions":[]}`. It found this by asking the platform rather than reading the prose, which is how every instance of this shape has been found (ADR-070, ADR-071, ADR-074).

Two ways to make prose and reality agree: delete the claim or make it true. Made true, because Phase 5's Razorpay webhook is an Edge Function that must be deployed for the product to take money online at all — the pipeline is needed regardless, and building it now means the webhook arrives into something that already works.

**On push rather than manual dispatch**, unlike the seed: a function whose deployment is something somebody remembers to do is a function that drifts from the repo, and "deployed" becomes a claim nobody can check — which is the defect this ADR exists to close. `deno check` runs in the deploy job as well as in CI, so a path-filtered push cannot outrun the typecheck.

**ADR-082 - The trusted-context carve-out belongs to rules that adjudicate a CLAIM, not to rules that keep the books.** Every enforcement trigger in this project opens with `if not row_security_active(...) then return; end if;` (ADR-068), and Phase 5's first draft copied that opening onto all five of the manual-payment rules at once. A holdout suite took **ten assertions off it in one run**, and every failure traced to the same mistake.

The distinction the carve-out is actually drawing:

- **Attribution and the provider rule read the JWT.** A trusted context has no `staff_id` claim, and the Razorpay webhook — `service_role` — writes provider identifiers because that is its entire job. Refusing those writes would refuse the correct behaviour. The carve-out is right.
- **A receipt number and a refund ceiling are not about who is asking.** Carved out, every online payment would have no receipt number, because the webhook that records it is exactly the caller the carve-out exempts — **a receipt book with a hole in it for every online payment is not a receipt book**. And a refund limit that the one path which will process refunds at scale does not obey is not a limit.

The general form, which is worth more than the instance: **a carve-out for trusted callers is sound exactly when the rule's subject is something a trusted caller legitimately lacks.** Where the rule's subject is an invariant about the data — a sequence, a total, a date arithmetic — the trusted caller needs it *more*, not less, because no policy is standing behind it.

Two consequences fell out of the same round, both already-named shapes recurring:

- **The refusals moved from `before` to `after` (ADR-072, ADR-066).** They modify nothing, so nothing needed them early — and running early made them adjudicate rows row security was about to refuse: a member inserting a payment directly got this file's `GL034` rather than the policy's `42501`, so a refusal message leaked where a permission denial belonged. Committed in a file whose own comments quote ADR-066 at the point of the mistake. **Quoting a rule is not applying it.**
- **A blind test author found the receipt-number format by writing an assertion that could not be satisfied and asking why.** "Both years' first receipt numbers are equal" contradicts `payments_tenant_id_receipt_number_key` (unique per gym, not per year), which the same author had read from the catalogue and written into its own header. The reconciliation is the answer: the financial year has to be *in* the rendered number, because the counter restarts every April and a bare ordinal collides with last year's on the first payment of each one. Neither suite would have found it alone — the visible one asserted the restart, the index refused it, and the contradiction was the finding.


**ADR-096 - A retired membership does not grow, and the payment against it is still a payment.** `app.grant_periods()` read a membership's price, currency, dates, count and length and **never its status**. A critic measured it on the exact sequence this phase prescribes as the repair for a mis-sold membership — refund, cancel, sell a new one — with the payment then naming the retired row: `ends_on` went from 14347 to **26667**, `periods_granted` from 150,000 to 300,000, on a `cancelled` membership. Money recorded and receipted, dates moved, member still refused at the gate, nothing surfaced anywhere.

**It is reachable precisely when the product tells someone to do the repair**, because that is the moment a member holds two memberships and one of them is retired. Not from the console — the membership page offers only a live or lapsed membership to renew — but `POST /api/payments` takes any `membershipId` and `GL042` only checks the member matches.

The payment is still recorded, receipted, attributed, refundable and counted by `GL036`. **Refusing it would be a different and worse rule**: the money changed hands and a receipt is the gym's record of that; refusing after the fact leaves cash in a drawer with nothing to show for it. `pending` and `frozen` are deliberately not retired — a pending membership is activated by its own first payment, which is how a dateless one comes alive, and a frozen one is a paused live membership.

**Both blind authors then found the same thing about the rule as written, independently, and neither scored it**: the refusal is deferred, not durable. Money paid while retired stays on record, so reviving the membership makes the next payment grant everything the total bought. One called it the named harm arriving late; the other bounded it to its two possible answers with a `diag` and reported it. The requirement now answers it — the money counts, because a member who paid ₹3,000 has paid ₹3,000, and detaching it would mean they bought nothing and cannot get it back. **The gym is never out of pocket**: ₹3,000 buys three periods at ₹1,000 whenever it is counted.

What that exposed is not about money at all, and is recorded as **OPEN-030**: `memberships.status` has no state machine while `payments` has one. A front desk can write `cancelled → active`, and can write `expired` — a status ADR-064/075 say nothing in this product ever writes. Three rules now depend on which status may follow which, and nothing says.

**ADR-098 - A membership's status moves only where it can go, and the rule cannot live where the seed can switch it off.** OPEN-030, closed. `payments` has had `app.payment_transition_allowed()` and `GL039` since Phase 5 round three; `memberships` had nothing, and a critic moved a retired membership back to life from an ordinary front-desk session in one statement. `docs/data-model.md` has listed the legal edges since Phase 1 and called `expired` and `cancelled` terminal — **the gap was never a missing contract, it was that nothing enforced the one that existed**, and a routing-table document asserted a rule the database did not keep.

Written as an explicit edge list rather than a deny-list, for the reason its payment sibling gives: a label added to `membership_status` later should be unreachable until somebody decides where it belongs. Measured across all 25 ordered pairs — 13 permitted, 12 refused, every refusal `GL047`.

**It has a trigger of its own, and that is the design rather than an accident.** The obvious home is beside the other rules in `app.enforce_membership_terms_frozen()`, and a blind author caught what that would cost: **both seed files disable that trigger around their own statements.** That window is deliberate and narrow and was argued for DATES — the seed builds months of history in one pass and a payment can only ever move `ends_on` forward. It was never argued for transitions. Folded in there, the rule would have been silently off for the whole seed and would have passed every assertion anyone wrote, because nothing else exercises that window. The author's assertion disables the trigger exactly as CI does and checks the revival is *still* refused: **ADR-078's instinct — test the checker in both directions — applied to a trigger's enablement rather than its logic.**

**Two contract defects the other author found, one operational.** "Reviving a retired membership" said *changes a retired membership's status*, which the self-write scenario then permits — they overlapped on exactly `cancelled → cancelled`. That is not pedantry: **`app.grant_periods()` writes `status = m.status` on every grant**, so a rule refusing a status WRITE rather than a status CHANGE breaks every renewal in the product. And `pending → cancelled`, which the contract declares legal, is unreachable for a dateless membership — `memberships_dated_unless_pending_chk` answers `23514` first, and that row is exactly a sale called off before the money arrived. Recorded with OPEN-026's family rather than fixed alongside this.

**What this makes permanent**, stated because it is a real cost and not a side effect: a status written in error can no longer be typed back. The repair is to sell the member a new membership, which the freed one-live-membership index allows, and which a blind author asserted end to end rather than assuming.

**And it forced a decision ADR-096 had deferred without knowing it.** ADR-096 kept money paid against a retired membership on record *because a revival might yet count it*. Retirement is now terminal, so that revival never comes and the money would strand — the harm ADR-096 named. The answer is not to refuse the payment: a manual payment is cash already in the drawer before any row is written. It is that the payment stays a complete, refundable record, and the remedy is a refund plus a payment against a live membership. Both requirements sit in one change so neither can drift from the other.

**ADR-113 — A new gym can approve its own pauses.** Under ADR-111, the three
Phase 6 onboarding presets initially set `pause_approver_role = gym_owner`.
Onboarding creates an owner and no manager; the existing approval rule compares
roles exactly, so the earlier proposed manager default stranded approval in a
new gym. Owners may select a manager later when that identity exists. This
corrects the accepted planning default before any Phase 6 test author starts;
it changes no existing gym settings or Phase 5 behavior.

**ADR-112 — Preserve migration order when existing versions are ahead of the clock.**
The refund unit's CLI-generated version `20260910074537` sorts before the
already-applied `20260915100000` migration. Under the owner's ADR-111 delegation,
create the file with the CLI, then assign the next second after the largest
existing version when the generated version would precede it. The resulting
`20260915100001` is an ordering identifier, not a claim about wall-clock time.
This supersedes the timestamp-only naming convention for this existing future
history. Rejected: editing applied history or using `--include-all` to insert a
new migration before dependencies already applied. CI remains the only applier.

The refund unit reuses the existing enforcement functions and row policies.
Its public security-invoker RPC is required because separate HTTP insert/read
calls cannot provide the specified exact concurrent replay. Its private audit
trigger is elevated only to append the specified audit row: authenticated users
correctly have no audit insert privilege. New public types are generated by the
CLI after CI applies the migration, as required by the migration skill.

**ADR-111 — Owner delegates remaining Phase 5 and Phase 6 product decisions.** On 2026-09-10, after reviewing the continuation drafts, the owner instructed: "you take decise" and "complete it with best". The agent may settle remaining requirements without asking for approval again. Adopt the documented net-price, refund retry/refusal-order and Phase 6 defaults, preserving the agreed phase sequence. Freeze each unit before dispatching authors; independence, tests-first commits, CI-only migrations and all gates remain in force. A newly found technical ambiguity is resolved and recorded serially before agents continue against it. This is product-decision authority, not a claim that the phases are already complete.

**ADR-110 — Limit this change to the first paid grant.** The proposed membership-creation span cap was withdrawn before resuming implementation. It was justified by the demo gym's live rows, while its INSERT rule also bound independently authored historical fixtures. That population mismatch broke existing suites. OPEN-029's unpaid direct-creation gap remains open; this change fixes only the separate doubling defect when the first payment arrives.

For a fully dated membership with no granted periods, the first grant starts at the later of its recorded start and gym-local today and sets exactly the sold duration times the periods bought. The ordinary fully dateless path and subsequent renewals retain their existing behavior. Partial payments move no dates before they complete a period. The two half-dated pending shapes remain deferred under OPEN-026.

A fresh review on 2026-09-10 found that the pending migration filled a missing start date on the ends-only shape while its activation test still read the old null start. A second payment would then activate it, silently changing the deferred behavior. The contract was narrowed explicitly before independent tests were updated. Rejected: repairing half-dated rows incidentally as part of first-grant arithmetic; that requires deciding their meaning separately.

Completed on 2026-09-10: fresh critic GO, local and CI 47 files / 4243 assertions
passing, seed dry run green, and a browser payment producing exactly one 30-day
period and receipt `2026-27/000007`. The temporary payment was removed and the
original demo membership restored in the same session; the receipt number stays
spent. Evidence: `docs/evidence/2026-09-10-first-paid-period.md`.

**ADR-109 - The browser verification for the three membership migrations, and what it left behind.** Three migrations changed money-path triggers on `memberships` — a function body restored, `GL042` reordered, a trigger renamed — and none of them had been exercised through the product. Done at the desk in a real browser session against the demo gym, on membership `00000006-…-0001` (Aarav Deshpande, 30-day plan at ₹1,500):

| | before | after |
|---|---|---|
| `paid` payments | 1 | 2 |
| `periods_granted` | 1 | 2 |
| `ends_on` | 2026-09-09 | 2026-10-10 |
| receipt | — | `2026-27/000006` |
| attributed to | — | the signed-in staff, from the JWT claim rather than the form |
| `member_id` | unchanged | unchanged |

**Exactly one period for one period's money**, dated from `greatest(ends_on, today)` because the membership had lapsed the day before — which is the arithmetic ADR-093 specified and the first time it has been seen through the product since the migrations landed.

**What the session left, swept and recorded rather than claimed clean.** ADR-095 is a "cleaned up and verified" that had swept three of six tables and missed an approved pause which then removed a member from churn detection. So all seven tables a desk session can write were counted for rows created in the window: **`payments` 1, everything else 0** — `refunds`, `memberships`, `attendance`, `qr_sessions`, `membership_pauses`, `members` all zero.

**The one row stays, deliberately.** It is a renewal — legitimate, in-product demo state, and arguably better demo data than a ledger whose newest payment is months old. Reverting it would mean deleting a `paid` payment and rolling back `periods_granted` and `ends_on`, all three of which the freezes refuse below `pg_trigger_depth() >= 2`; doing it would require disabling money-path triggers as the table owner, **which is a larger and riskier act than the row itself.** It is recorded here so the next person to read the demo data knows why Aarav has two payments and runs to October.

**ADR-108 - GO, on the ninth round, and the closing argument is a route nobody had attacked.** Every round from the second onward attacked UPDATE forms. `GL042` lives in the UPDATE branch of `app.enforce_membership_terms_frozen()`; its INSERT branch checks only `periods_granted = 0`. So the obvious unexamined route is **DELETE then re-INSERT at the same `id` with a different `member_id`** — which lands the named harm exactly: the payment still names membership X and member A, and membership X now belongs to B. `service_role`, which every round has counted as an in-force session, holds `DELETE` and `TRUNCATE` on `memberships` where `authenticated` holds only INSERT, SELECT and UPDATE.

**It is closed, and by the foreign keys rather than by any rule.** `payments_membership_id_fkey` is `NO ACTION` and not deferrable, so no membership a receipt names can be deleted at all:

    delete from public.memberships where id = <membership with a paid payment>
    -->  23503  violates foreign key constraint "payments_membership_id_fkey"

**The harm requires a receipt, and the route cannot reach any membership that has one.** That is a better argument than the rule itself gives, and it took nine rounds to be made because everybody including me was looking at the trigger.

Three more flanks closed in the same pass, all by measurement: no function anywhere carries `session_replication_role` in its `proconfig` (`pg_proc where proconfig::text ilike '%replication%'` → zero rows), so it cannot be smuggled into a caller through a `security definer`; every foreign key on `memberships` and every one pointing at it is `confupdtype = 'a'`, so no `on update cascade` can drag `member_id` along behind a renumbered member; and `GL042` sits above both the `row_security_active` and `is_gym_admin` early returns.

**The verdict on the process, which the critic was asked for directly and gave straight: stop.** Nine rounds, roughly twenty-eight defects, **not one in a migration**. The last three rounds produced only prose about prose — corrections to corrections, in a document whose subject is corrections to corrections. The three migrations were right on the first measurement and are still right.

**What the nine rounds actually bought, stated honestly because the number is embarrassing and the lesson is not:** two real defects in behaviour (`GL042` ordered after `GL043` by a silent re-emission; `GL047` ordered ahead of everything by a trigger *name*), one real scope correction (the contract was specifying a set its author did not control), and about twenty-five corrections to descriptions of those three things. **The implementation was never the risk. Describing it accurately was**, and the cost of describing it accurately exceeded the cost of building it by roughly three to one.

The general form, and the only thing here worth carrying to the next requirement: **write the smallest normative sentence that covers the harm, quantified over something you control, and put everything else under a heading that says it is an observation.** Four of the nine rounds died on sentences that were true of the implementation and false of the world.

**ADR-107 - The normative claim held. Seven rounds, and the only thing that ever broke was prose about numbers.** A seventh critic aimed the four mechanisms most likely to be fatal at the narrowed sentence — `before` triggers rewriting the row via `app.stamp_membership()`, `INSERT … ON CONFLICT DO UPDATE`, `MERGE`, and **execution at `pg_trigger_depth() >= 2` with `GL044`/`GL045`'s depth gates open**, driven from a temporary trigger on `public.members` — across 27 measured single-row statements, five roles including `service_role` and a trusted context with row security off, both SET orders, and every rule combination. **`GL042` answered every time, with the row read back unmoved.**

Structurally, why it cannot break for a row: `GL042` is the first statement of the UPDATE branch and is ungated; `memberships_stamp` never writes `member_id`; `memberships_terms_frozen` sorts before `memberships_transitions_after_terms` **and both are `after` ROW triggers, which is the half of the comparison that makes name order mean anything** — the same `level` qualifier ADR-103 had to add to ADR-101's rule after omitting it cost the `payments` row of the fire-order table, omitted again here in the paragraph that cites it; neither trigger is `DEFERRABLE` and neither carries a `WHEN` clause. The one single-row exception is the seed's `disable trigger` window, which the requirement discloses in its opening paragraph — and the honest statement of it is a **class, not that instance**: **two distinct privileges above the application** can switch this rule off, and calling them one privilege by two routes was itself a measured falsehood. `alter table … disable trigger` needs **ownership of the table**; `set session_replication_role = 'replica'` needs **superuser class** — `pg_settings.context` is `superuser`, `pg_parameter_acl` grants it to nobody, and `public.memberships` is owned by `postgres`, which is not a superuser on this platform. Measured on a role created solely to own the table: **it could disable the trigger and was refused the GUC with `42501`.** Both are closed to every application role (`authenticated`, `service_role` and `anon` each `42501` on the GUC, and `42501 must be owner of table memberships` on the trigger), so neither leaks here — but a reader auditing *who can turn this rule off* would have taken `relowner` for the predicate, which is wrong in both directions. The phrase carried "Measured, both" over the one thing in it that had not been measured.

**And the multi-row case was pinned down further than the contract needed.** The mechanism is **row processing order**, not the SET list — same two rows, same session, same statement:

    where id in (low, high)                          Bitmap Heap Scan  -->  GL042
    where id in (low, high), enable_bitmapscan = off Index Scan        -->  GL047
    where tenant_id = … and id::text like 'bbbb%'    Seq Scan          -->  GL042

**Only one of those plans is the planner's own choice for a given statement; the
others were forced with `enable_*` GUCs**, and the table read as three natural
observations until a critic reproduced it on a fixture of their own and said so.
It matters because the spec a hundred lines away records the same
`where id in (a, b)` statement answering `GL047` unqualified — reconcilable only
once you know one of the two was forced.

So "the answer flips with the query plan" is measured, not inferred. **In every multi-row refusal both rows were read back at their original owner: the statement never moved a membership, it only changed which code came back.** That is the distinction between an undecided detail and a hole, and it had been assumed for six rounds.

**What this round found instead: three numbers, and every one of them in a sentence about checking numbers.**

  * *"the four superseded `select plan(N)` lines"* — **five**. The sentence quotes `700 → 709 → 722 → 741 → 757 → 782` in its own next clause, and six values make five transitions. It is the sentence whose entire purpose is to say a bulk edit was checked by `git diff` rather than memory.
  * *"six `not null` columns among those a caller can write"* — the phrase does not survive measurement, and the correction to it first said *"no grounded reading yields six"*, **which is an unmeasured universal negative sitting inside this round's own sentence about measuring things**. A critic produced a reading that does yield six: the not-null columns less the surrogate key, the two timestamps and the three the rules write — `tenant_id, member_id, plan_id, price_paise, discount_paise, currency`. The correction to 19/12/4 is right; a claim quantifying over *all possible readings* quantifies over a set the writer does not control, which is ADR-103's own rule turned on the sentence enforcing it. Measured: `authenticated` holds INSERT and UPDATE on **all 19** columns, **12** are `not null`, **4** of those carry no default. This was the **third number in a clause whose other two had just been corrected for being measured false.** Correcting the numbers a critic names and not the one beside them is how that clause was wrong in three consecutive rounds.
  * `splice.py`'s summary line still said *"Pass/fail for one pgTAP file"* — true when it was the whole harness, false once the runner split out, **and the same commit that split it wrote the contradiction into the other file's docstring**.

**The rule these three share, and it is the one worth keeping from seven rounds:** *a correction is not a measurement.* Every one of these survived a round in which the text around it was being actively corrected, because the fix was applied to what a critic pointed at rather than to what the sentence claimed. **Re-measure after the fix, never as part of it** — the only method that has worked on this class, and the one that caught ADR-105 still carrying "six CHECKs" after ADR-106 had corrected the spec.

**ADR-106 - The narrowed claim was true for a row and false for a statement, and restating it prominently is where the qualifier fell off.** ADR-105 reduced this requirement to one sentence: `GL042` answers ahead of `GL043`, `GL044`, `GL045`, `GL046` and `GL047`. A sixth critic could not break it across **30-plus single-row statements** — every combination, both SET orders, five roles including `service_role` and a trusted context, `UPDATE … FROM`, a data-modifying CTE, `MERGE`, and the money-freeze branch armed — and then broke it in one line at *statement* level:

    update public.memberships set member_id = <C>, status = 'pending'
     where id in (<a>, <b>);            -->  GL047
       -- <a> alone                     -->  GL042
       -- <b> alone                     -->  GL047

**Postgres does not order the row triggers of a statement**, so a multi-row statement is refused by whichever rule its first row reaches — and **the answer flips with the query plan**: the same SET list under `where tenant_id = …` takes a sequential scan and answers `GL042`. Attack one of the same family produces `GL043` for a genuine `GL042` violation, which is verbatim the harm this requirement exists to prevent: a desk re-pointing a mis-sold membership told *"change the plan instead."*

**The fact was never unknown.** `docs/registry.md` has carried it since round eleven — *"a mixed multi-row statement is refused with whichever rule its first row reaches"* — and `payment-record/spec.md` itself said so fifty lines upstream, as an aside. What happened is that ADR-105's restatement presented itself as authoritative — *"that is the whole of the normative claim"* — and carried no row qualifier. **Restating a claim more prominently is an opportunity to drop one of its qualifiers**, and a bolder sentence is trusted more, so the loss costs more. Fixed with the words "for a row", plus a scenario that says explicitly this requirement does not decide the multi-row case.

That is a **different dimension** from the five rejections ADR-105 escalated, so it implies no further escalation: those were about mechanisms outside this project's control, this is about a qualifier inside a sentence about mechanisms that are.

**Two numbers in the same commit were measured false**, both of them evidence *for* the escalation and both wrong in the direction that understated it: "six CHECK constraints" is **seven** (measured: `pg_constraint`, `contype = 'c'` on `public.memberships` — 7), and "10 fire on UPDATE beyond the two written by this project" is **13 total, nine internal, against four of ours** — a number ADR-105 states correctly two paragraphs away. The spec and its own ADR disagreed, in the same commit, for the sixth time this phase. **And fixing it in the spec left the same wrong number standing in ADR-105 one file over** — caught by re-measuring rather than by trusting the fix, which is now the only method that has ever worked on this class: 7 CHECKs, and 13 triggers firing on UPDATE of which 9 are internal and 4 are ours, verified independently after the correction rather than as part of it. That is now the single most reliable defect in this work: **text written in the same commit as the thing it describes.**

**ADR-105 - Five critic rounds on one dimension means the requirement was specifying the wrong thing. Escalated per AGENTS.md, and the bar was moved rather than lowered.** `AGENTS.md` says: *"If a critic rejects the same dimension three times, that's evidence the requirement is under-specified — escalate to the human, never silently lower the bar."* This requirement was rejected on the same dimension **five** times, for nineteen defects, and **not one of them was in a migration**. The owner is asleep and authorised the call to be made and recorded; this is it.

**What kept failing was an attempt to state, normatively, which of every mechanism Postgres can refuse a statement with answers first.** Five sentences, each falsified by measurement:

  1. *"`GL042` in every case"* — the live-membership index answers `23505`.
  2. *"…unless the target member holds a live membership"* — four more families inside the scenario's own column list.
  3. *"…CHECK constraints, unique indexes, foreign keys and the row-security policy are not rules"* — `not null` (`23502`) and `before` triggers missing, in the same breath as the sentence *"such a list is always one item short"*.
  4. *"…ahead of nothing enforced earlier in the statement"* — **a foreign key pointing AT this table is an `after` trigger ON it.**
  5. *"…and the resulting row satisfies the table's CHECK constraints"* — `not null` again, plus `42501`, plus a `22003` out-of-range.

**The surface is not enumerable, and measurement says so plainly.** `public.memberships` carries **22 triggers — 4 written by this project and 18 internal constraint triggers — of which 13 fire on UPDATE, 9 of them internal**, every one sorting before `memberships_*` under byte ordering. Add **seven** CHECKs, the `not null` columns, a policy that *filters* rather than refuses, and every type-range error the column set admits. **The set grows whenever anyone adds a column, a constraint, or a table that references this one.** A normative sentence quantifying over it cannot be kept true by anybody, which is why five careful attempts were not enough and a sixth would not be either.

**And nothing depended on it.** What a caller needs is *the move did not happen*, and every measured route — nine SQLSTATEs and one silent `UPDATE 0` — either refuses or changes nothing. ADR-092's grep passes on all of them.

**So the contract now claims only what this project controls: `GL042` answers ahead of `GL043`, `GL044`, `GL045`, `GL046` and `GL047`.** Five codes in one function plus one in the trigger beside it — a closed set, checked by 28 measured statements across five rounds and never once broken. That narrow claim is also the one that mattered: the original defect was `GL043` answering where `GL042` should, telling a desk to "change the plan instead" when the true repair is a refund and a new sale.

**Everything else moved into a block headed "Observed refusal behaviour — NOT a contract"**, and the ~60 assertions in sections 34-36 stay exactly as they are, reclassified from promises into regression pins. That is ADR-100's own prescription for an undecided pair — *either the contract decides which answers, or an assertion pins that nobody may rely on it* — applied to the whole class instead of pair by pair.

**This is not lowering the bar and the distinction matters.** Nothing that was refused is now permitted, and a sixth critic verified it rather than taking the claim: every deleted scenario's outcome was re-measured and still refuses. No assertion was deleted or weakened either — checked across all six rounds, where the only removed lines in `supabase/tests/22_payment_record.sql` are the **five** superseded `select plan(N)` lines, as the count went 700 → 709 → 722 → 741 → 757 → 782 — one removal per transition, and six values make five transitions, which is the arithmetic the sentence originally got wrong **while quoting the sequence that settles it**. Measured per commit (`--unified=0`, removed lines only): `e60983d`, `7b28b3c`, `df004e7`, `ca23bb1`, `33d07ed`, each removing exactly one line and that line the superseded plan. The migrations are untouched.

**But "nothing was deleted" was false when this ADR first said it.** The span replacement that moved the mechanism material into the non-normative block **swallowed two scenarios** — "Selling a member a second membership" and "Selling somebody else a membership" — in the very commit whose thesis was that nothing had been. The property never lapsed, because the holdout suite asserts it and quotes the deleted sentence verbatim, so this was an honesty gap rather than a hole; a critic found them missing and they are restored. **A claim that nothing was removed is exactly the claim a bulk edit is least able to make about itself**, and the check is `git diff`, not memory. What changed is the *kind* of claim being made about behaviour nobody consumes. **A specification should quantify over a set its author controls** — the inside of a function, not the union of every way a database can say no.

**The cost of learning it, recorded because it is the honest number:** five rounds, nineteen defects, roughly a third of this work's total effort, and every defect a sentence. Three were reintroductions of a family an earlier ADR had already named by name. **The two migrations were mechanically correct on first measurement and survived every attack in all five rounds.** The strongest evidence this project has produced that the blind arrangement earns its cost — and, read the other way, that a description can be harder to get right than the thing it describes.

**Two harness findings from the same round, both instances of one rule.** The plan-count check ADR-104 introduced lived in `scratchpad/`, an OS temp directory outside the repository that no future session and no CI job can read — *a checker nobody else can run is a procedure somebody remembers*, which is the exact failure ADR-104 was written about, one level up. It now lives at **`scripts/pgtap/sweep.py`** with the splicer at `scripts/pgtap/splice.py`. And three more runnable copies of the pre-ADR-104 splice (`critic_sweep.py`, `critic17_sweep.py`, `h16_sweep.py`) were still on disk, deleted under the rule ADR-104 wrote when it deleted `sweep.sh`: **a superseded tool that still runs is not superseded.** Worth adding what ADR-104 missed: **CI already catches a plan shortfall independently** — `db.yml`'s `pgtap` job runs `supabase test db`, which is pg_prove, which fails on a TAP plan mismatch. The local runner is the pre-push equivalent, and the two agreeing is the point.

**ADR-104 - `num_failed()` cannot see a plan mismatch, so the sweep could not either.** `scratchpad/sweep.py` has reported one number since ADR-073: `num_failed()`, captured after `finish()` so a failing file cannot shadow it. That is the right fix for the bug it was written for and it is blind to a different one — **a file that runs fewer assertions than it planned has zero failures.**

Caught by accident, and only because two harnesses disagreed. `tapcount.py` reported `plan 1..757 ran 756 ok 756 notok 0` on the visible suite; `sweep.py` reported `failures: 0`. Neither was wrong. One assertion is written

    with u as (update … returning 1), c as (select count(*)::int as n from u)
    select is(c.n, 0, '…') from c;

and `tapcount.py` anchors on lines beginning `select <fn>(`, so it never wrapped that one — its TAP row was emitted and not counted. Measured directly against pgTAP's own counter, `pg_catalog.currval('__tresults___numb_seq')`: **757 emitted, 0 failed**. The suite was fine; both harnesses were partly blind, in opposite directions.

**The plan/ran capture and the comparison were split across two files, and this paragraph named neither correctly for three rounds.** As the repository stands: **`scripts/pgtap/splice.py`** rewrites one file, stashing `num_failed()` and pgTAP's own counter `__tresults___numb_seq` into a temp table before `finish()` and selecting them back after it; **`scripts/pgtap/sweep.py`** is the runner, and its GREEN requires both `failures = 0` and `ran` equal to the number in the file's `plan(...)` line. This ADR originally credited the tool with a check the orchestrator was performing by hand — *the same gap in kind as the one this ADR is about*: a procedure somebody remembers is not a checker. **It then named the fix wrongly twice more.** It pointed at `scratchpad/sweepall.py`, a path that has never existed in this repository, and it asserted of `sweep.py` the precise opposite of what `sweep.py`'s own docstring, `splice.py`'s docstring, `docs/gates.md` and ADR-105 eleven lines above all say (this file is newest-first; an earlier draft said "below", getting the distance right and the direction wrong — another new fact asserted inside a correction) — that `sweep.py` is the one that "only rewrites one file and has no opinion about pass or fail", which is `splice.py`. A critic found it standing after two consecutive rounds had corrected the *same claim* in the script docstrings and in ADR-105, because each fix went to the file the previous critic pointed at. **Three places named those two files; two were corrected; this was the third.** Worth adding what this ADR missed entirely: **CI catches a plan shortfall independently** — `db.yml`'s `pgtap` job runs `supabase test db`, which is pg_prove, which fails on a TAP plan mismatch, and that workflow's own comment records a real observed *"planned 58 but ran 28"*. The local runner is the pre-push equivalent, and the two agreeing is the point.

Worth noting what did NOT catch it: every CI job, both critics, and the sweep itself. It surfaced because a second tool with a different blind spot happened to be run on the same file, which is not a strategy. The plan-count check is.

**ADR-103 - Three critic rounds, and the same sentence was wrong three different ways. The lesson is to enumerate the inside of a rule, never the outside.** The requirement "a membership belongs to the member it was sold to" needed one sentence saying which rule answers when a statement violates several. It took four:

  1. *"`GL042` answers in every case."* — false: the live-membership index answers `23505` first.
  2. *"…in every case, where the target member holds no live membership."* — false: four more families inside the scenario's own column list, including a status write onto a null-dated `pending` row, which `seed-scenarios.sql` creates.
  3. *"…ahead of every rule; CHECK constraints, unique indexes, foreign keys and the row-security policy are not rules."* — false: `not null` (`23502`) and `before` triggers were missing, and this list was written in the same breath as the sentence *"enumerating exceptions to a rule about shape produces a list that is always one item short"*.
  4. *"…ahead of every rule enforced by an `after` trigger on this table, and ahead of nothing enforced earlier in the statement."* — false: **a foreign key pointing AT `memberships` is an `after` row trigger on `memberships`.** Four foreign keys point at it (`attendance`, `membership_pauses`, `payments`, `memberships.renewal_of_membership_id`), carried by eight constraint triggers — of which **four are `after delete` and cannot fire on an UPDATE at all**. This ADR first said "eight of them exist" inside a paragraph about ordering against `GL042` on an UPDATE: literally true and materially misleading, which a fourth critic measured with `pg_get_triggerdef`. Measured: changing `member_id` and `id` together answers `23503` from `attendance_membership_id_fkey`; the control answers `GL042`. That route is neither "earlier in the statement" nor "the table's own shape", so it defeated both formulations at once.

**Every attempt tried to enumerate what the rule does NOT beat, and that set is open-ended.** The fix is to enumerate the inside: `GL042` answers ahead of the four other rules in its own function and ahead of `GL047`, and the contract decides nothing else. Five codes in one function plus one beside it is a closed set a reader can check. "Everything else" is not, and three rounds were spent proving it.

**The general form, which is ADR-092's grep turned around:** *a normative sentence should quantify over a set the writer controls.* The inside of a function is controlled; the set of ways Postgres can refuse a statement is not, and it grows when anybody adds a constraint or a child table.

**And a second, sharper instance of the same disease in the same requirement.** Which row's status decides whether the partial live-membership index applies was written down wrong three times: draft one said *the target member's* state, draft two said *the source membership's* state. Both false. **A partial index is evaluated against the tuple being STORED — and that is only half of it.** The stored tuple decides whether this row falls inside the predicate; the TARGET member's other rows decide whether there is anything to collide with. A fourth critic caught the correction itself over-stating this as "not the target member's other rows", which is plainly false — same source row, same written status, only the target differs: a member holding a live membership gives `23505`, one holding nothing gives `GL042`. A critic measured the only pair that separates the three readings:

    cancelled row --> live-holder, status = 'active'   in one statement  -->  23505
    active    row --> live-holder, status = 'cancelled' in one statement  -->  GL042

**Every assertion written across three rounds was green under all three readings, because none of them wrote `status`.** That is the part worth keeping: an assertion set can be honest, controlled, and complete against the contract, and still be unable to distinguish the contract from two wrong versions of it. The discriminating case is the one where the two things being conflated are made to disagree — here, a statement that changes the row's status as well as its owner. **When a rule is keyed on a row's state, assert it with a statement that CHANGES that state**, or the assertion is measuring the fixture rather than the rule.

**Also corrected: ADR-101's own mechanical rule was one word short.** "Enumerate every other same-timing trigger and sort by name" omits **level** — Postgres orders by name only within a timing *and* level, and every after-row trigger fires before every after-statement trigger whatever it is called. That omission put the `payments` row of the fire-order table in the wrong order: `payments_identity_frozen` (the `GL038` money freeze) fires **before** the two extension triggers, not after them, because those two are `for each statement`. The rule and the table both now carry level.

**Three rounds, ten defects, and not one of them was in a migration.** The two migrations this work shipped were mechanically correct on first measurement and stayed correct under every subsequent attack. Every defect was in a sentence — a scenario, a class statement, a registry cell, a table of measurements — and three of them were in text written in the same commit as the thing it described. That is now four times this phase, and it is the strongest argument yet for the blind arrangement: **the implementation was never the risk; the description of it was.**

**ADR-102 - The local sweep and CI's pgTAP job contend for the same Cloud project, and the failure looks like a broken test.** There is one database (ADR-030: no Docker, no branching), and both the 47-file local sweep and the `DB / pgtap` job run every test file against it. Run them at the same time and fixtures collide on locks. Observed:

    ERROR: 57014: canceling statement due to statement timeout
    CONTEXT: while inserting index tuple (0,31) in relation "organizations_pkey"

**The `CONTEXT` line is the whole diagnosis** — a statement timeout while inserting a primary-key tuple is a lock wait, not a slow query and not a failing assertion. Without it this reads as "the holdout suite broke", and the reflex is to go looking at the holdout suite.

It surfaced as `ERROR h22_payment_record_holdout KeyError('rows')` in the sweep report, because the harness expects a result set and got the CLI's error envelope. That is the right way for it to surface — **it is a non-green line, and ADR-073 says read every non-green line** — but the message names the harness rather than the cause.

**The rule: do not start a local sweep while a `DB` workflow run is in flight**, and if one starts mid-sweep, re-run the affected files afterwards rather than reading the timeout as a result. `gh run list -L 5 --json name,status` answers it in a second. The two big files (`22_payment_record.sql` at 741 assertions and `h22_payment_record_holdout.sql` at 937) are the ones that will lose the race, because they are the longest transactions and hold their fixtures the longest.

Not a defect in anything under test, and recorded because the failure mode is misleading rather than because it is subtle.

**ADR-101 - A trigger's NAME decided which rule answered, and the fix ADR-100 shipped could not reach it.** ADR-100 decided that `GL042` answers ahead of every other rule that can refuse the same statement, and implemented it by reordering clauses inside `app.enforce_membership_terms_frozen()`. A fresh-context critic measured the sentence false by a route that mechanism structurally cannot touch: `update public.memberships set member_id = <other>, status = 'active' where id = <cancelled>` answers **`GL047`**, from a front desk and from a gym manager, in either SET-list order. The controls say it is genuinely `GL047` winning — the re-point alone answers `GL042`, and a *legal* transition plus a re-point answers `GL042`.

**Postgres fires same-timing row triggers in trigger-name order.** `memberships_status_transitions` sorts before `memberships_terms_frozen` because `'s' < 't'`, so `GL047` preceded all five rules in that function. Clause order inside a function decides nothing across triggers, and **ADR-100 never asked which triggers existed** — it reasoned about a function as though it were the whole rule set.

**The fix is the name, not the placement.** `GL047` sits in its own trigger deliberately: both seed files disable `memberships_terms_frozen` around their own statements, a window argued for dates and never for transitions, so folding the rule in would leave it silently off for the whole seed — which is exactly what ADR-098 cost a round to learn. Renaming keeps that property and fixes the order: `memberships_transitions_after_terms` sorts after (`'tr' > 'te'`) and says why it is called that. Dropped and recreated rather than renamed, so the migration does not depend on what the trigger is currently called. Nothing else refers to the old name.

**The part worth keeping is that I wrote the grep and did not run it on my own change.** ADR-100's closing sentence is *"for every pair of rules that can fire on the same row, either the contract decides which answers or an assertion pins that nobody may rely on it"* — and OPEN-034, written in the same commit, enumerated five undecided pairs across the payment and refund families while omitting `GL042`/`GL047`, **the only pair this session had itself introduced, one commit earlier**. A general rule written about other people's code is not applied to your own by being written down. The mechanical form: **when a change adds a trigger, enumerate every other trigger on that table of the same timing AND the same level, and sort them by name** — that is a query, not a judgement. **The `level` half was missing from the first version of this rule and cost the `payments` row of the table below**, which recorded `payments_identity_frozen` firing after the two extension triggers when it fires before them: name order applies only within a timing and level, and every after-row trigger precedes every after-statement one whatever it is called.

**A second critic round rejected the fix, and both new defects were in the sentences written to fix the first one.**

  * **The `23505` scenario was false.** `memberships_tenant_id_member_id_live_key` is **partial on `('active','frozen')`**, so it bites only when the membership *being moved* is live — not when the target member holds one. Measured on a real `cancelled` row in the demo gym pointed at a member who does hold a live membership: **`GL042`**, not `23505`. The two scenarios had been written as though the target's state alone decided the answer, and that partition is wrong for every non-live source — the exact case they were rewritten around. **The visible suite's own fixture comment had this right before the spec did**, which is the second time this round that an assertion was more accurate than the sentence above it.
  * **Scoping the other scenario with one condition was still wrong, and would have stayed wrong.** Adding "the target holds no live membership" left four more families inside the scenario's own column list: a status write onto a null-dated `pending` row (`23514` — and `seed-scenarios.sql` creates exactly such a row), `ends_on` before `starts_on`, a negative price or discount, a nonexistent plan or member (`23503`), another tenant's `tenant_id` (`42501`). **Enumerating exceptions to a rule about shape produces a list that is always one item short.** The contract now defines the excluded set by **when it runs** rather than by what it contains: `GL042` answers ahead of every other rule enforced by an `after` trigger on this table, and nothing enforced earlier in the statement — column and table constraints, `not null`, unique indexes, foreign keys, the policy's `with check`, and any `before` trigger. **The list-of-four that first replaced the enumeration was itself one item short, twice over**: it omitted `not null` (`23502`) — measured: `set member_id = <other>, currency = null` answers `23502` where the control `set member_id = <other>` alone answers `GL042`, and the same for `price_paise` and `status` — and `before` triggers, of which `memberships` carries two (`memberships_stamp`, `memberships_touch_updated_at`), and it was written in the same breath as the sentence *"enumerating exceptions to a rule about shape produces a list that is always one item short"*. A list of mechanisms is still a list. The fix is that the phase of the statement each thing runs in is checkable, and membership of a list is not — which is the same move ADR-101 made when it replaced "reorder the clauses" with "sort the trigger names", and the same one ADR-092 made for scenarios.
  * **And the fire-order table in this very ADR recorded the pre-fix order** for `memberships` — the one row out of six about ADR-101's own change, in the one place ADR-101 tells a future session to look up trigger order. Every other row was verified correct. A record written in the same commit as the change it records is the most likely one to describe the intention rather than the result; this is the fourth time this phase.

**Two more contract defects from the same critic, both corrected in `openspec/specs/payment-record/spec.md`:**

  * **"In every case" was false in the *typical* case.** The new scenario said a re-point plus anything else is refused with `GL042` in every case. Measured: where the target member already holds a live membership — which is the ordinary reason you re-point one, two members mixed up — `memberships_tenant_id_member_id_live_key` refuses with `23505` during `ExecUpdate`, before any AFTER trigger runs. No rule ordering can change that, because an index is not a rule. **Both suites were green on the sentence only because both authors had deliberately arranged targets holding nothing**, following the fixture warning the requirement itself gives them. The assertions were honest; the sentence above them was not. Now scoped, with the `23505` case written as its own scenario and asserted so nobody codes against `GL042` there.
  * **A numbered list is a claim of order.** ADR-100 numbered all four absolutes 1 to 4 and then said four lines later that the order among 2, 3 and 4 was not claimed. An implementer reading that file alone would take `GL044`-before-`GL045` as contract. They are a set, not a sequence, and the spec now says so.

**ADR-100 - The order in which two refusals answer was inherited from the order the clauses were typed, and a holdout author read my own sentence back to me.** ADR-099's fix restored `GL042` to before the `GL043` length check, and the spec sentence written alongside it says: *"it SHALL be this rule that answers, not another one the same statement also violates."* I wrote that with the length rule in view and illustrated it with the length rule. An author blind to the implementation and to the visible suite read the sentence as written, asserted the general form, and measured two counter-examples on the restored body: `set member_id = <other>, periods_granted = 3` answers **`GL044`**, and `set member_id = <other>, ends_on = <other>` answers **`GL045`**. Both refusals are correct and both name the wrong harm.

**There was no decision behind the order — that is the finding.** Ownership was checked third because `GL044` and `GL045` were typed into the function in earlier migrations and `GL042` was added in a later one. Six migrations each appended a clause where the previous one ended, and the resulting precedence was nobody's judgement.

**Decided: `GL042`, then `GL044`, then `GL045`, then `GL043`, with `GL046` last as before.** `GL042` is first because it is the only one of the four about **whose** membership this is; the other three are about what may be typed onto a membership already agreed to be yours. The repair is what makes the message matter: `GL044` and `GL045` are answered by taking a payment — "take the money and the dates follow" — and `GL042` only by a refund, a cancellation and a new sale. Telling somebody moving a membership to a different person to take a payment is advice for a different problem. `GL046` stays last on the rule already recorded: an absolute beats a permission, and answering the permission would imply a gym admin could do it.

**The order among `GL044`, `GL045` and `GL043` is deliberately left undecided.** No scenario needs it, and deciding it would be answering a question nobody asked — the same restraint ADR-084 applies to `expired`.

`20260913150000_the_rule_that_answers_is_the_one_about_whose_membership_it_is.sql` was **built by moving the block, not by retyping the function**, which is ADR-099's rule applied to the very next migration: the `GL042` clause and its comments were lifted verbatim and re-inserted at the top of the UPDATE branch, and the multiset of non-blank lines was asserted identical to `20260913120000`'s mechanically rather than reviewed by eye.

**Five contract findings the same author raised and did not paper over**, which is the more valuable half of the round — each is a pair of rules that can answer the same statement with no precedence written anywhere:

  * **`GL038` vs `GL039`** — a paid payment walked backwards *and* edited. The payment-side twin of exactly the pair this ADR decided for memberships, with opposite repair advice and nothing deciding it. The author called it the highest-value gap and it is.
  * **`GL034` vs `GL035`** — a cash payment naming a colleague *and* carrying a `provider_payment_id`.
  * **`GL036` (money-arrived) vs `GL040`** — a refund against a `created` payment naming a colleague.
  * **The refund amount-freeze has no SQLSTATE in the contract at all.** *"SHALL refuse any change to a refund's `payment_id` or `amount_paise` once recorded"* sits under the `GL036` requirement while the code-assignment paragraph sits under `GL041`. A statement demoting a `completed` refund *and* raising its amount is therefore **unassertable**, not merely undecided. That is a contract fix, not a test fix, and it is the only one of the five that blocks writing a test at all.
  * **The payment-side `GL042` shares a SQLSTATE with the membership-side rule but not its precedence sentence**, which is written under the membership requirement and about it.

Recorded as **OPEN-034**. None is a money hole — every path involved still refuses — and all five are the same shape: *where two rules can answer one statement, which one answers is behaviour, and this codebase has been deciding it by typing order.* The general grep, which is ADR-092's one table over: **for every pair of rules that can fire on the same row, either the contract decides which answers or an assertion pins that nobody may rely on it.**

**ADR-099 - I re-emitted a hundred-line function by hand to add four lines, one change after writing down that this is how this phase introduces defects.** ADR-098's migration needed nothing from `app.enforce_membership_terms_frozen()`; the transition rule went onto its own trigger, deliberately and for a good reason. The migration re-emitted that function anyway, and the re-emission was **not behaviour-preserving**. A fresh-context critic measured it in one statement: `update public.memberships set member_id = <other member>, duration_days = 3650` answered **`GL043`** where it had answered **`GL042`** — the `member_id` freeze had moved from before the duration check to after it. Two rules, both still present, in the wrong order, so the refusal names the wrong harm; a desk correcting a mis-sale reads "change the plan instead" where the true answer is "that membership belongs to somebody else".

**And ~60 lines of recorded reasoning went with it.** The live body was 108 lines against the authoritative 149. Everything the comments in `20260912210000` carried — why `GL042` takes no trusted-caller carve-out, the `memberships_tenant_id_member_id_live_key` trap that makes a careless test pass on an index instead of on the rule, why the claim rule is checked last — was gone from the database, from `pg_get_functiondef`, and from the file anyone would read next.

**The hazard is named verbatim in this repository, in a cell I wrote.** `app.enforce_payment_arrival_status()`'s registry entry says: *"re-emitting a hundred-line function by hand to add four lines is how this phase has introduced defects before."* I read that sentence while writing that cell and did the thing one change later. So the lesson is not "be careful" — that was already written down, in this repo, by me, and it did not work. The rule that follows is mechanical: **a migration that re-emits an existing function body must `diff` the emitted body against its last authoritative source before it is committed, and a migration that does not need to re-emit one must not.** `20260913120000_restore_the_terms_function_i_re_emitted_by_hand.sql` restores the body byte-for-byte from `20260912210000` and was verified that way rather than by reading.

**Every other re-emission in the repository was then checked, rather than assumed clean.** All 37 functions defined by a `create or replace` in `supabase/migrations/**` were parsed, consecutive definitions of the same function diffed, and every one that lost more than 15% of its lines examined. Five did: `app.enforce_follow_up`, `app.run_no_show_scan`, `app.impersonation_session_immutable`, `app.extend_membership_on_payment` and this one. **Only this one lost behaviour.** Three lost comments while their code-only line count held or grew (74→77, 76→76, 23→23), and `app.extend_membership_on_payment`'s 83→36 is the deliberate split that moved the arithmetic into `app.grant_periods()`. No `errcode` disappeared from any function at any point in the history — the terms function's set only ever grew, `GL043, GL044` to `GL042, GL043, GL044, GL045, GL046`. So the defect here is a **reorder**, not a deletion, which is why a code-count or error-code check would have missed it and a `diff` would not.

**Why the tests did not catch it.** Both suites assert that `member_id` is refused and that `duration_days` is refused; neither asserted which rule answers when a single statement violates both, because until this migration only one order had ever existed. That is a genuine gap in the contract's coverage, not a lucky escape: **where two rules can answer the same statement, which one answers is part of the behaviour**, and the registry cell already says so for the multi-row case ("a mixed multi-row statement is refused with whichever rule its first row reaches"). The visible suite now pins the single-row order.

**ADR-097 - The sweep harness is shared, unversioned, and was silently replaced.** `scratchpad/sweep.py` and `tapcount.py` are the only local verification this project has — the full 47-file run that must be green before every push. They live in the session scratchpad, are not in the repository, and **every subagent can write to them**. One did: an author needed a TAP-capturing wrapper, found `tapcount.py` missing from the repo (it was never there; it is scratchpad tooling), and wrote its own over the top.

The replacement worked for the file that author was testing and was narrower everywhere else. Its allowlist of pgTAP function names was twenty entries written from memory, so **every `is_empty`, `has_index`, `col_type_is` and `enum_has_labels` assertion in the suite stopped being captured**: the sweep reported 34 assertions of a planned 58 and called passing files FAIL, on 34 of 47 files at once. That is the loud failure. The quiet one is the same shape as the trap `sweep.py` was written to defeat — a checker that reports something other than what it measured.

Rebuilt by **deriving the allowlist from the corpus** rather than guessing it (`grep -ihoE '^\s*select\s+[a-z_]+\s*\(' supabase/tests*/*.sql | sort -u`, 42 distinct names, minus the seven that are fixture plumbing), and by handling `select case when <guard> then skip(…) else matches(…) end` — a conditionally-skipped assertion that the naive "first word after `select`" test reads as `case`, which was one whole assertion in `01_tenancy_structure` and would have gone on reading 57 of 58 for ever.

**And the patch that fixed it was itself broken by an invisible byte.** Writing the regex through a shell heredoc turned `` into a literal backspace, so the pattern was `^select\s+case` and could never match — the components all tested true in isolation while the function returned false, until `cat -A` showed the `^H`. Worth remembering the next time a regex is "obviously right" and does not fire.

The rule that follows: **an agent that needs a shared tool copies it under its own name.** That instruction is now in every brief that might touch one. The deeper point is that the verification harness deserves the same treatment as the code it verifies, and it does not have it — it is unversioned, so this damage was invisible to `git status` and would have been invisible to a reviewer.

**ADR-095 - "Press the button in a browser" writes to the live project, and `begin … rollback` cannot reach it.** Every containment rule this project has is written for `supabase db query`: wrap it in a transaction, roll it back, leave nothing behind. A critic found four payments and a membership in the demo gym that no seed wrote, matched one of them to a `.playwright-mcp` page snapshot **to within two seconds**, and thereby proved they came through the real console in a browser driven against the live Supabase project.

The cost was not hypothetical. Member 102's lapsed membership — the fixture `seed-scenarios.sql` declares and `docs/gates.md` Gate 11 counts — had a second `active` membership beside it with ₹1,500 paid against it, so the no-show scenario was gone. **And re-seeding could not repair it**: the seed upserts by fixed uuid and deletes nothing, so a row it never wrote is invisible to it. Four receipt numbers were spent, and `app.enforce_counter_monotonic()` is forward-only by design, so that part is simply gone.

**This is hard rule 2's shape without hard rule 2's guard.** That rule denies every mutating Supabase MCP tool outright, because the blast radius of hitting the wrong database is too large to rely on remembering. Browser automation pointed at a `localhost` app whose `.env.local` names the production project has the same blast radius and no equivalent denial — and the evidence of what it wrote lives in `.playwright-mcp/`, which is gitignored, so it is visible in exactly one working tree.

**The instruction is still right.** Two Phase 3/4 defects were found by pressing the button and by nothing else, and this phase found a third — the money screen never displayed the length the money was scored against. What was missing is the other half of the rule: **a browser session against the live project is a write, and the session that makes it owns cleaning it up in the same session.** Not "later", because the next reader cannot tell a deliberate fixture from a forgotten test, and neither can the seed.

**Cleaned up here — and the sweep was too narrow, which the next critic proved within a round.** The stray refund, the four payments, the stray membership and the two rows whose `periods_granted` and dates the browser payments had moved (the last through a deliberate `disable trigger` window, because `GL044` and `GL045` correctly refuse a hand repair and correctly take no trusted-context carve-out). **`attendance`, `qr_sessions` and `membership_pauses` were never looked at**, and the same browser sessions had left three `qr` check-ins, three QR sessions and an **approved pause**. The pause was the worst of them: NSH-002 excludes paused members from the red list, so the demo gym's first member had silently dropped out of churn detection for a week. And one check-in was on member 102 — **the very lapsed fixture this ADR claimed to have restored** — so she read as having visited today rather than never. Restored as a membership, not as a member.

The date-invariant claim was wrong too: `ends_on - starts_on = duration_days × periods_granted` holds for the 29 memberships carrying a granted period and fails for the 17 that carry none, which is OPEN-029's shape and was false as written.

**The lesson is the sweep, not the tables.** A browser session writes wherever the product writes, so the cleanup has to be enumerated from the schema rather than from the tables the writer happens to remember. Swept and verified since: zero strays in `payments`, `refunds`, `memberships`, `attendance`, `qr_sessions` and `membership_pauses`; `no_show_cases` and `follow_ups` deliberately kept, because those are the product's own scan output rather than residue.

**ADR-094 - Changing what a member owes is gym-admin work, like a refund.** `ends_on = duration_days x floor(money / price_paise)`. Round nine made `duration_days` underivable by hand because it is a multiplier on that product. **`price_paise` is the other factor, and it stayed freely typed.** Measured by a round-ten critic, two ordinary `front_desk` statements, no privilege beyond selling and taking money:

    update public.memberships set price_paise = 15000 where id = …;  -- permitted: no money yet
    insert into public.payments (… 150000, 'INR', 'paid', 'cash' …);  -- the ordinary Rs.1,500

    ends_on = 2027-07-06   periods_granted = 10   money = 150000

**300 days for one Rs.1,500 receipt**, and both audit invariants hold — `periods_granted = floor(money/price)` and `ends_on - starts_on = duration_days x periods_granted` — so it leaves exactly the "nothing to find" record that got `duration_days` closed. At `price_paise = 1` it is `periods_granted = 150000` and an `ends_on` in the year 14347.

**My argument in ADR-092 for treating the two factors differently was about intent, not effect**: *"`price_paise` is a negotiated number a desk legitimately mistypes; `duration_days` is not."* Both are multipliers on the same product. Intent does not bound a multiplier.

**And round ten made it permanent.** Before it, a gym could put `ends_on` back by hand; now `GL043` freezes the price once money has arrived and `GL045` freezes the dates, so the row cannot be repaired in place at all. A defect that was recoverable became unrecoverable **in the round that was supposed to be closing this family of holes**.

**Rejected: derive `price_paise` from the plan, as `duration_days` now is.** It is the tempting symmetry and it is wrong. A gym legitimately sells below list — that is what `discount_paise` exists for — and, at the time, the money path did not read that column (the OPEN-028 finding later resolved by the membership-net-price change), so making the price underivable would have left no way at all to sell a membership at a negotiated number.

**Rejected: refuse a price below the plan's.** Same objection, and it invents a bound nobody chose: any threshold is arbitrary, and a gym that discounts by 30% is ordinary while one that discounts by 90% is a decision, not an error.

**Rejected: bound the discount instead** — though **the reason first given for it was false, and a critic measured that too.** It said `discount = price - 1` is the same 150,000 periods. It was not: before OPEN-028 was resolved, `app.grant_periods()` did not read `discount_paise` at all. Measured then — a membership priced 100000 carrying a 90000 discount, paid 100000, granted **one** period. No value of that column produced any period at that time.

The rejection stands on the argument that survives: `discount_paise` is part of what a member owes the moment anything reads it, and a bound invented now would be a bound on a column with no arithmetic behind it. But ADR-093 is the ADR that says a wrong reason is worse than no reason because it stops the question being asked again — and this was one, written two paragraphs from that sentence.

**The control is WHO, not what**, and this codebase already made that call for the neighbouring case. `refunds_tenant_write` is `is_gym_admin()`, not `is_front_office()`: *"front_desk may record money but not refund it."* Deciding what a member owes is the same kind of act as deciding to give money back, and it belongs to the same people. The front desk sells at the plan's price and takes payment; **re-pricing is the manager's**. `GL046`.

That is proportionate rather than absolute, and it is worth being explicit about what it does not do: a `gym_owner` or `gym_manager` can still comp a membership to a paisa. **They should be able to** — comping is a real thing gyms do — and it leaves the price sitting on the row as evidence, which is the same bound ADR-093 accepted for OPEN-027. What it removes is the front desk doing it silently, which was the measured threat model for every `GL043` finding in this phase.

**Method note, because this is the third time.** ADR-092's grep — *when a requirement names a harm, no scenario under it may permit that harm's outcome by another route* — was written after round nine and would have caught this: the requirement names the ten-years harm, and its own scenario "Correcting a mistake before any money arrives … SHALL be allowed" permits the identical outcome through the other factor. **The rule was written down and not run.** Running it is now part of fixing the contract, not a thing to remember.

**ADR-093 - The dates a membership runs for are written by the rule that grants them.** Two rounds governed `duration_days` — the quantity that exists only to compute `ends_on` — while `ends_on` itself stayed writable by hand. Measured, from an ordinary front-desk session, no privilege beyond recording a payment:

    update public.memberships set ends_on = starts_on + 3650 where id = …;   -- ALLOWED
    update public.memberships set starts_on = starts_on - 3650 where id = …; -- ALLOWED

Ten years, one statement, no refusal, no receipt, nothing raised. **The whole `GL043` family protects the inputs to an arithmetic whose output anyone could type.**

This was visible the entire time and went unexamined because of the false premise corrected in ADR-092 above: the belief that a hand-written `ends_on` left a detectable trace ranked it below the length, and the number behind that belief had been borrowed from a different audit. **A wrong reason for a priority is worse than no reason, because it stops the question being asked again.**

So the dates join `periods_granted` under the rule that already works for it (ADR-089): they may change only at `pg_trigger_depth() >= 2`, which is where `app.grant_periods()`' own UPDATE runs and where no hand-written statement can reach. `GL045`. The create path sets them at INSERT and is untouched; nothing else in the product moves them — membership pauses write their own table, and a plan correction reaches them only through the granting rule.

**What this does not close, stated rather than left to the next critic.** A gym admin may re-length a plan, point a membership at it, point it back, and restore the plan; the membership keeps a length no plan now carries, with no artefact anywhere, and the next ordinary payment buys it. Recorded as OPEN-027 rather than fixed here, for three reasons: it needs `is_gym_admin()`, not the front desk; the same manager can reach the same outcome legitimately by creating a long plan and selling on it, which differs only in leaving evidence; and closing it means freezing `plans` once a membership has been sold on one, which contradicts assertions both suites currently hold — that a manager may re-length a plan and that the edit must LAND. **That is a contract change, and contract changes made in the same breath as a fix are how this phase produced rounds eight and nine.**

The honest instrument for it is an audit trail, which this product does not yet have and which is not Phase 5's to invent.

**ADR-092 - A period's length is derived, not negotiated: it is never caller-authored, and a plan correction carries the price with it.** ADR-090 recorded that a requirement contradicting itself defeats the blind arrangement. **The requirement written to record that lesson then did it again, in the same paragraph.**

It names the harm — *"a length named by the caller at creation is ignored in favour of the plan's — a blind author measured what accepting one costs: create a membership naming `duration_days = 3650`, pay the ordinary price, get ten years"* — and three lines later mandates it:

> #### Scenario: Correcting the length before any money arrives
> **THEN** it SHALL be allowed and SHALL land, and a payment SHALL be scored against the corrected length.

One statement refused, the identical outcome in two statements required. Measured by a critic: sell a 30-day membership at ₹1,500, `update memberships set duration_days = 3650`, take the ordinary ₹1,500 — **3,650 days**. Also through `MERGE` and `UPDATE … FROM`. `authenticated` holds UPDATE on `memberships` with no column restriction, so it is one `.update({duration_days})` from any front-desk browser session.

**And it is undetectable after the fact, which no other exploit in this phase has been.** A desk can already push `ends_on` directly, but that leaves `ends_on - starts_on` disagreeing with `duration_days × periods_granted`. This route leaves a *fully self-consistent* record: one ₹1,500 receipt, `periods_granted = 1`, `ends_on` exactly one recorded period, and `periods_granted = floor(money / price)` holding. **That claim was false, and it was mine.** Measured after the fact at the round-nine critic's prompting: `ends_on - starts_on <> duration_days * periods_granted` holds for **17 of 46** dated memberships on the demo gym before any fraud — 16 of them seeded rows carrying dates and no payments, one rewritten by `seed.sql` after its payments had granted. A 37% false-positive rate is not a detector, and one more hit does not stand out in it. I had conflated this invariant with a *different* audit the round-eight critic ran — `periods_granted = floor(money / price)`, which genuinely does hold at zero disagreements — and then used the borrowed number to rank one door above another. **The exploit is still undetectable and the fix still right; the argument for preferring it to the `ends_on` door was worthless**, and ADR-084's rule applies: a decision defended by a premise that turns out false has to be re-decided, which is what ADR-093 does.

Both blind suites certified it, because the scenario told them to: the visible suite has a front desk type `duration_days = 90` onto a membership whose plan says 365 and asserts the payment buys 90 days, "which is the whole point of recording the term". Neither suite has a single edit-then-pay assertion. **The authors were not wrong; the contract was, and they wrote to it.**

The resolution is the migration's own sentence, applied where it was already true: *"a term is what the plan says at the moment of sale; there is no legitimate caller that knows better."* If that holds at INSERT it holds at UPDATE. **`price_paise` is a negotiated number a desk legitimately mistypes; `duration_days` is not.** A wrong length is a wrong plan, and the instrument for that is re-pointing `plan_id`, which re-derives the length. The permitted door is all attack surface and no use.

So `duration_days` leaves the family of terms frozen by money and joins `periods_granted` (`GL044`) as a **derived** recorded fact: it may only change as part of a plan change, and only to that plan's length, at any time and whatever the money. **Refused, not ignored** — silently discarding a write is the failure this codebase asserts against, and the round-eight draft that carried the old value forward was caught by both suites for exactly that.

**The second half, measured in the same round: a plan correction carried the length and not the price.** `app.stamp_membership()` re-derived `duration_days` from the new plan; nothing re-derived `price_paise`, which `POST /api/memberships` had copied from the *original* plan. Correct a mis-sold Monthly to Annual, take one ₹12,000 Annual fee: `price` still ₹1,500, so `floor(1200000 / 150000)` is **eight periods of 365 days — 2,920 days** for one year's money. Predates round eight, which rewrote that branch and preserved it.

Price and length must come from the same plan or from neither. So a plan change re-derives **both**, unless the caller names a price in the same statement — which keeps a negotiated price possible, since that is the one of the two a desk legitimately sets by hand.

**The spec's own scenario for that case would have passed with the rule deleted** (ADR-078): it asserted only that the correction was *allowed*, never what the following payment bought. Every permitted-side assertion in both suites corrects price and plan *together*; the plan-only correction — what a desk actually does when it has sold the wrong plan — is tested nowhere.

**Three seams the blind authors staged rather than guessed, settled here.** Both suites bounded them and reported the observed state by `diag` instead of picking a side — the arrangement working exactly as ADR-089's round showed it can.

  * **Creating ignores a named length; editing refuses one.** Both authors called this the same family of inconsistency as the defect above, and the reason it is not is a fact about the schema: `duration_days` carries a default, so a `before insert` trigger cannot tell a caller who wrote `1` from one who wrote nothing. There is no "named length" to refuse at creation. On update `old` exists, so there it is refused — and refusing is the better answer wherever it can be given.
  * **A price equal to the one already recorded counts as naming none**, and the plan's price wins. A row trigger sees values, not which columns a statement listed. A desk that retypes the agreed number to protect it across a plan correction gets the list price; the remedy is a different number or a second statement, and that cost is now written down rather than discovered.
  * **The plan's currency travels with its price** (MNY-002). Taking a price without its currency scores the new number against the old denomination — wrong by an exchange rate and completely ordinary-looking, which is the worst combination this phase has learned to look for.

**And the refusal code is `GL043`**, which the requirement failed to name and both authors had to ask about mid-round. It was answered to both simultaneously so the two suites could not diverge on it, and it is in the requirement now. The length is a term and a reader will look where the other terms are.

**The method change this forces.** ADR-090 said requirements are to be read for internal contradiction before fan-out. That was done, and it still shipped, because the contradiction was between a paragraph and a scenario **that were written in the same edit to fix a previous contradiction**. The narrower rule: *when a requirement names a harm, no scenario under it may permit that harm's outcome by another route.* A named harm and a permitting scenario in one requirement is a contradiction whoever wrote them, and it is worth grepping for.

**ADR-091 - Hard rule 1 and hard rule 10 pull against each other, and rule 1 was quietly winning.** Raised by the round-eight visible test author, unprompted, about its own round — the most useful thing anyone said this phase.

Rule 1 obliges every session to read `docs/registry.md` before writing anything. Rule 10 obliges a test author to write from the requirement without seeing the implementation. **In round eight the implementer updated the registry in the same working tree while both blind authors were still writing**, so the registry told them the new trigger names, the new column name, `app.stamp_membership()`, which SQLSTATEs were raised, and two of the design decisions — "any currency, not just the membership's own", "refunded money still counts" — before either had designed an assertion. The author did not open the migration. It did not have to.

**Blindness was not lost to carelessness about the migration file; it was lost to a rule that requires reading a document the implementer had just written.** That is worth more than the round it was found in, because nothing in the loop would have surfaced it: the suites were green, the arrangement looked intact, and only the author's own report showed it was not.

So: **the registry entry for new code is written after the blind authors have handed back**, in the implementation commit where it already belongs (rule 10 already sequences tests before implementation; this only says the registry travels with the implementation, not ahead of it). Authors read the registry at `HEAD`, never the working tree — and an author that finds a working-tree registry describing unbuilt code should say so, as this one did.

It does not weaken rule 1. Nothing goes unregistered; it is registered in the commit that builds it, which is also the commit a critic reads.

**And it explains a pattern that had been read as competence.** Both round-eight suites asserted `GL043` on `duration_days` — a column that did not exist in any requirement text under that name, only in the registry row the implementer had written that hour. The convergence was not two independent authors reaching the same conclusion; it was two authors reading the same leak.

**ADR-090 - A term is frozen by money arriving, not by a period being granted — and the length of a period is a term.** Round seven made `periods_granted` unforgeable so that `old.periods_granted > 0` could be trusted as the gate. It never asked whether the gate says the right thing. **It does not**, and the exploit ADR-089 was written to close survived the round that closed it.

**A membership that has taken real money but not yet crossed one whole multiple of its price sits at `periods_granted = 0`, and every one of its terms is open.** That is not a contrived state — it is a part payment, ordinary practice in an Indian gym, and the console renders it as product copy: *"A full ₹X buys one period; part of it is recorded and receipted and buys none until the balance is paid."*

Measured by a critic against **a real row in the live demo gym** — Sneha Joshi's Annual, ADR-088's own worked example, ₹10,800 arrived against a ₹12,000 price:

    update memberships set price_paise = 100000 where id = …;   -- ALLOWED, the gate reads 0
    insert into payments … values (1, 'cash', 'paid', …);       -- one paisa

    before | ends_on=2026-09-12 price=1200000 periods_granted=0
    after  | ends_on=2036-09-09              periods_granted=10   -- +3650 days

**Ten years of an Annual membership for one paisa**, no error, no audit trail. On a clean ₹1,000/30-day membership the same three statements buy 150 days for ₹500.01. The plan and the currency are equally open in that window: a member who agreed to ₹1,000 for a month and paid it in two halves can be given **a year**, with two ordinary ₹500 receipts in the ledger.

**The gate is wrong because "granted" is not when scoring starts. `v_total` counts every paisa that has arrived, whether or not it has crossed a multiple** — so the money is already scored against the terms from the first payment, and the moment to freeze them is the moment the first payment lands.

**And the spec said so.** ADR-089's requirement reads *"Correcting a mistyped price or a wrong plan **before any money has arrived** stays free — nothing has been scored yet"*, while its scenario three lines below says *"a membership that has been granted nothing"*. **The prose stated the requirement and the scenario was written to the implementation**, so the gate looked correct against its own test and both blind suites asserted the weaker sentence. A spec contradicting itself inside one requirement is worse than a spec contradicting another file, because nothing in the loop compares a requirement to itself.

**The second half: a period's LENGTH was still read live from `plans`.** `plan_id` was frozen; `plans.duration_days` was not, and `plans_tenant_write` is `FOR ALL` on `is_gym_admin()`. One manager statement setting `duration_days = 3650`, then one ordinary ₹1,000 renewal, moved `ends_on` **3650 days** — and it does that silently to *every* membership on that plan whenever a gym legitimately edits it.

The fix is not to freeze the `plans` row, which would stop a gym repricing or re-lengthening a plan for future sales, something it must be able to do. **The membership already records the price and the currency it was sold at; the duration is the one term still read from somewhere else, and that asymmetry is the defect.** So `memberships.duration_days` is recorded at creation like the other two, filled from the plan by a `before insert` trigger (ADR-072: filling belongs early), and frozen with them. Editing a plan then changes what the *next* membership is sold at and nothing about one already sold — which is what a plan edit should mean.

**Rejected: freezing `plans.duration_days` while any membership references the plan.** It punishes the legitimate act to prevent the illegitimate one, and leaves the same defect for `plans.price_paise` the day anything scores against it.

**Also closed, found in the same round:** a payment could be INSERTED directly at `refunded` or `reversed`. Both count toward the grant total, neither extends anything and neither takes a receipt number, so ₹3,000 of grant credit could sit on the books with no receipt until a one-paisa payment cashed it in. A payment does not arrive already refunded — it is recorded, and then refunded (`GL039`).

**The method finding, which is the expensive one.** Round seven's gate was measured against five exploit shapes and passed all five, because every shape started from a membership that had been *granted* something. **The blind arrangement cannot catch a scenario that encodes the implementation's own assumption** — both authors wrote to that scenario, and the suites went green. What caught it was a critic reading the requirement's prose against its scenario. Requirements are now to be read for internal contradiction before they are fanned out, which is a cheaper check than another round.

**ADR-089 - The terms the money was scored against are frozen, and the count it was scored into is not hand-writable.** Round six froze a membership's `price_paise` and `currency` once `periods_granted > 0` (`GL043`). A round-six critic took the same exploit through four other doors, one ordinary statement each, and every one of them worked.

`app.grant_periods()` computes `floor(total / price)` periods of `plans.duration_days` each. **Three inputs decide what money buys — the price, the currency, and the plan whose duration measures a period — and round six froze two of them.**

  * **`plan_id` was not frozen.** `memberships_tenant_write` is `FOR ALL` on `is_front_office()` with no column restriction — the fact that migration's own justification cites. Repoint a 30-day membership at a 365-day plan and pay ₹1,000: measured `days_added=395`. One statement, nothing else manipulated, `periods_granted > 0` throughout. **Strictly easier than the price cut the round was written to close.**
  * **The gate on that freeze is itself front-desk writable.** `old.periods_granted > 0` is the whole condition, and `check (periods_granted >= 0)` permits `0`. `set periods_granted = 0`, then `set price_paise = 50000`, then pay **one paisa**: measured 75 days for ₹1,000.01. The one-statement form (`set periods_granted = 0, price_paise = 50000`) is correctly refused — the trigger reads `old` — so the freeze is not wrong, it is **gated on a value the attacker controls**.
  * **And no price edit is needed at all.** `set periods_granted = 0` then a 1-paisa payment: 30 days of gym for ₹0.01.
  * **Set it upward and it silently eats a real payment.** `set periods_granted = 500`, then an ordinary ₹1,000: `ends_on` unchanged, money receipted, zero days granted. **This is ADR-088's own named harm** — "she pays ₹12,000 for zero days, silently" — still reachable, because round six's CHECK bounds only the sign the previous critic happened to use.

The general form, and the reason this is one ADR and not four: **a freeze is worth exactly as much as the immutability of the thing it is keyed on.** Round six keyed a rule about recorded facts to a column the front desk can rewrite, which is the same mistake as deriving "already granted" by subtraction (ADR-087) — trusting a value chosen by the party the rule constrains.

So `periods_granted` is maintained by the granting rule and by nothing else (`GL044`), enforced by trigger depth: `app.grant_periods()` is only ever reached from the statement trigger on `payments`, so its write to `memberships` runs at `pg_trigger_depth() = 2`, while every hand-written `update memberships` runs at `1`. **Depth cannot be forged from a top-level statement** — unlike a session GUC, which the very session being constrained could set. With that in place `old.periods_granted > 0` means what it says, and `plan_id` joins `price_paise` and `currency` under `GL043`.

**A fifth door, found by the holdout author after the fix was designed: the INSERT.** Everything above is update-shaped, and so was the rule written against it. Measured live — a front-desk session creates a membership carrying `periods_granted = 5`, then takes ₹1,000: `ends_on` unmoved, receipt issued, no error. The third exploit again, reached without ever issuing an UPDATE. **Closing three doors and leaving the fourth is the mistake this ADR exists to correct**, so the count must be zero at creation; nothing in the product writes it there, so nothing legitimate is refused.

Both blind authors reached the same undecided question independently and both declined to pick a side rather than guess — the visible one shaping its whole section so that it stands whichever way the question settles, the holdout one writing a bounded assertion that holds either way and reporting the observed state by `diag`. **That is the arrangement working**, and it is worth recording because the round before this one let a single author write both suites and both missed the same two bypasses.

Two smaller questions decided with it. **Writing the same value back is allowed** (`is distinct from`): every exploit needs the value moved, and refusing a harmless write breaks ordinary column-listing updates. **`discount_paise` is deliberately not frozen** — the holdout author asked why it is absent, which was the right question, and the answer is that nothing in the money path reads it: a period is scored against `price_paise` alone, so freezing it would be a claim the system does not make. Whoever teaches the rule to score against `price_paise - discount_paise` adds it to `GL043` in the same change, because at that moment it grows the same door the other three had.

**Rejected: a consistency trigger** asserting `periods_granted = floor(money / price)` on every write, which would need no depth and no trust. `app.grant_periods()` returns early in five cases — zero price, currency mismatch, missing timezone, nothing owed, and the `ends_on is not null` filter — each of which legitimately leaves a membership whose recorded count and whose money disagree. OPEN-026 is such a row, and ADR-088's backfill created its mirror. A trigger enforcing agreement would refuse every future update to those rows, including the ones that would fix them. **The invariant is who writes it, not what it equals.**

**Rejected: `revoke update (periods_granted) on memberships from authenticated`**, the native answer. `app.grant_periods()` is `security invoker` and runs as the front-desk user, so the revoke would refuse the rule along with the attacker; making it `security definer` to get around that hands the whole extension path an RLS bypass in order to close a hole a trigger closes without one.

**Also rejected: raising the CHECK to `periods_granted > 0`.** It reads like a fix and closes nothing — a membership that has never been paid legitimately holds `0`, and every exploit above needs the count *lowered* or *raised*, never made negative.

**ADR-088 - `sum()` returns numeric, and assigning numeric to an integer column ROUNDS.** ADR-087's backfill and the rule it was written to agree with disagreed by one, for every membership whose money had reached at least half a period but not a whole one.

`sum()` over `bigint` returns `numeric`. `app.grant_periods()` declares both operands `bigint`, so it truncates: `1080000 / 1200000 = 0`. The backfill's expression stayed numeric, produced `0.9`, and PostgreSQL **rounded** it on assignment to an `integer` column. Stored: `1`.

Measured against the demo gym: **one membership in 47** — Sneha Joshi's Annual, priced ₹12,000, with ₹10,800 arrived, which is a coupon discount and not an exotic input. `memberships` has a `discount_paise` column and the seed uses it; every part payment lands in the same window. **The rounding did not mis-set an arbitrary row, it mis-set exactly the rows where the money and the price genuinely differ.**

The second consequence is worse than the first. She holds a period nobody paid for — and her next payment grants **nothing**: the total becomes ₹22,800, `owed` is `1`, `periods_granted` is already `1`, so the function returns. **She pays ₹12,000 for zero days**, silently, and the only record that anything is wrong is a column nobody looks at.

**This is what a recorded count is FOR.** ADR-087 argued that recording beats deriving because a record can be checked against the rule. Running that check found a defect in the very migration that introduced the column — one round after a critic listed "does the backfill agree exactly with its own rule?" as the first thing to attack. The answer was no, by one, on the one row in the demo gym carrying a discount.

Verified in both directions, which is the only kind of verification this project accepts (ADR-078): 1 disagreement across 47 memberships on live Cloud before the correction, 0 after it.

**And the check itself was wrong the first time.** My first comparison query divided in `numeric` and reported a disagreement that was its own arithmetic rather than the data's — a checker that cannot distinguish the thing it is checking from itself. It had to be rewritten to truncate exactly as the rule does before it could say anything at all.

**ADR-087 - How much has already been granted is a fact, not a subtraction.** Round three's own fix double-counted an upsert. I found it by running the shape rather than reading the code, and a round-four critic reproduced it independently in three forms: `INSERT … ON CONFLICT DO UPDATE`, `MERGE`, and a data-modifying CTE. ₹1,000 bought **60 days** on a 30-day plan, through `supabase-js .upsert()`, from an ordinary front-desk session.

Such a command fires BOTH statement triggers — the insert trigger's transition table holds the rows inserted, the update trigger's holds the rows that conflicted. Each then computed `before = total − its own added`, and the other invocation's rows were already in `total`, because by the time either fires the whole statement has landed. Both concluded they had crossed the multiple.

**This is one mistake in three costumes, and granularity was never the point.**

| round | derived per | what bought too much |
|---|---|---|
| one | payment | ten separate statements |
| two | row | ten rows in one statement |
| three | statement | one command firing two triggers |

Each fix removed the shape in front of it and left the next. The error underneath all three is **deriving "how much was already granted" by subtraction from a total that is not stable at the moment of asking.** Subtraction needs a "before" that no `AFTER` trigger has: every row of the current statement, every sibling trigger invocation, and every earlier statement of the transaction has already committed to the table being summed.

So `memberships.periods_granted` is recorded. A payment computes what the money now OWES, compares it against what has been granted, grants the difference and records the new total — idempotent, order independent, and indifferent to how many triggers fire or how many statements make up a transaction.

**Round two's migration claimed the cumulative sum "needs no marker column and cannot drift".** A critic falsified the second half; this falsifies the first. They were always the same sentence: *a running total that cannot be checked against a record of what it already paid for is not a count, it is a guess that happens to be right in the cases you tried.*

The backfill matters as much as the column: every existing membership is set to what its money already bought, counted exactly as the rule counts it. Without it the next payment on any existing membership would measure against zero and grant every period again — turning the fix into the largest instance of the bug it fixes.

**ADR-086 - A rule about a statement belongs in a statement trigger.** Round two's fixes carried two critical defects of their own, both measured by a blind critic rather than argued, and the first is the sharpest lesson of the phase.

**Ten payments in one statement each bought a full month.** ₹1,000 written as ten rows granted **300 days** on a 30-day plan, from an ordinary front-desk session, through one `supabase-js` call. `AFTER … FOR EACH ROW` triggers all fire *after every row of the statement is already in the table*, so each row re-derived the running total, found the FINAL total, subtracted only its own amount, and concluded that it was the one that crossed the multiple. Round two closed the ten-separate-statements case — `paid → created → paid`, which bought 90 days — and left the one-statement case, which buys 300.

**The arithmetic was right and the GRANULARITY was wrong**, which is why re-reading the sum would never have found it. A rule about "how much did this statement add" cannot be answered per row: a row cannot see which of its siblings preceded it, because none of them did. It belongs in a statement-level trigger with transition tables, where the statement's rows are a set that can be summed once. `app.grant_periods()` now holds the arithmetic and the two triggers differ only in how they decide which rows are in scope.

The contrast that makes this a miss rather than an unknown: `app.enforce_refund_total()`, moved to `after` in the *same migration*, is correct under exactly this shape — because it sums its siblings (`r.id <> new.id`) instead of re-deriving a delta. The same author got the neighbour right on the same day.

**A `receipt_number` typed onto an unpaid row jammed the gym's book for ever.** The overwrite ran only on the write that made a payment paid, so a `created` row kept whatever number it was given; the next real payment collided on `payments_tenant_id_receipt_number_key`; and because the failing insert rolls the counter's increment back with it, `next_number` never advanced, so every payment after that collided on the same number too. `GL037` closed the counter door and this reached the same room through the payment row — and the handler reported that collision as "already recorded", which is cash taken, nothing written, and a screen saying it is on file. **The number is now the counter's alone at every status.**

Three more, all measured, all folded in: the cumulative sum ignored **currency** (100000 USD-paise bought a month on a membership priced in INR); a dateless `pending` membership got its dates and **stayed `pending`**, so the member it had just been paid for was refused at the gate — two changes from one round contradicting each other in that round's own area; and the sum took **no lock**, so two transactions each recording half the price each granted nothing, permanently.

**And the registry asserted the opposite of the code in three cells.** `app.enforce_refund_total()` was documented as `before insert` while the catalogue said AFTER — *the registry describing the very defect ADR-085 records as this phase's third ADR-066 instance, as though it were the fix*. The counter cell quoted the justification of the alternative that was REJECTED. The extension cell still described round one's rule. And `app.stamp_refund()` was missing entirely: `grep -c stamp_refund docs/registry.md` returned 0, against hard rule 1. `registry-lint` gates exported TypeScript symbols, so no gate catches any of it — **the document AGENTS.md routes every reader to is checked by nothing but attention.**

Two claims inside the migrations were false as written and are corrected in place rather than deleted, so the next reader sees what a confident wrong claim looked like: "written into the spec" (it was not — the rule deciding how many months a member gets lived only in a SQL comment) and "cannot drift" (it drifted by a factor of ten).

**What this round says about the loop itself.** Round one: twenty findings. Round two: five more, three of them introduced by round one's fixes. Round three: two criticals introduced by round two's fixes. Phase 3 recorded that nine of its fourteen defects came from its own fixes; Phase 5 is running at a similar rate, and the arithmetic is not improving with familiarity. **The blind critic is not a formality at the end of a phase — it is the only thing that has caught any of these**, and every one was measured against the live database rather than reasoned from the diff.

**ADR-085 - A payment is a record: the INSERT was governed and the UPDATE was not.** Two blind critics returned NO-GO on Phase 5 round one with twenty findings between them, and almost every critical one was a single defect wearing different clothes. Measured against Cloud by the critics, not argued: a refund's `amount_paise` raised to ₹1,00,000 against a ₹1,000 payment **after** the ceiling had approved it; a payment walked `paid → created → paid` four times to land ninety days on a thirty-day plan, burning a receipt number each cycle; a desk typing its own `receipt_number` over an allocated one; `paid_at` supplied to file payments into `2031-32` and `2019-20`; and `document_counters.next_number` reset by hand until the next payment collided with a number already issued — which the Route Handler then reported to the front desk as a success, because it mapped **every** `23505` to a success redirect and `payments` has three unique indexes.

Round one's own migration quotes ADR-070 — *"the second statement is where a rule enforced only on insert gets undone"* — three functions above the rule that did not do it. **Quoting a rule is not applying it**, which is the second time this phase has had to say that about a rule quoted in the same file (ADR-082 said it about ADR-066).

The fix is one rule, not fourteen patches: **a payment that has ever been `paid` freezes its money facts** (`GL038`), **its status moves only along an explicit edge list** (`GL039`, and `paid` is unreachable from anywhere it has already been), the counter moves **forward only** (`GL037`), the refund ceiling applies **on update as well as insert** (`GL036`) with the refund's amount and payment frozen (`GL041`), **money leaving the gym names a human** (`GL040` — the attribution rule's fifth appearance and its first on the direction where a gym actually loses), and a payment may not name **another member's membership** (`GL042`).

Four things this round got wrong on the way, all worth more than the fix:

- **ADR-066 for the third time in one phase.** The refund attribution went into the same `before insert` as the ceiling and immediately broke two `05_membership_money_rls` assertions: a cross-tenant refund and a claimless refund started answering `GL040` where they had answered `42501`. Round one committed this for the payment claim rules; ADR-082 repaired half of it; this migration — *written to repair the other half* — committed it again one table over. The rule that finally states it usefully: **a trigger that REFUSES must not run before the policy that would have refused first; a trigger that only FILLS a column may.** Hence `app.stamp_refund()` before and `app.enforce_refund_total()` after.
- **The counter rule contradicted this project's own receipts spec.** It said "may only advance by exactly one"; the receipts spec two documents over says *"a gap in a receipt book is explainable, a reused number is not."* Only a decrease can issue a number twice. Requiring a step of one also made a legitimate holdout assertion unstageable — it pre-sets the counter to prove the allocator never reads before it writes. **A rule that forbids the test proving its neighbour is drawn in the wrong place.** Forward-only.
- **A refusal the caller cannot see is worse than the change it prevented.** The first draft restored `old.paid_at` in the `before` trigger, so a backdate became a silent no-op: quietly replaced, compared equal by the freeze, and reported as success while doing nothing the caller asked for. Left alone, it reaches the freeze and is refused visibly.
- **The cumulative sum counts money that ARRIVED, not money still held.** Summing only `paid` rows looks right and double-grants: half now and half next week grants one period on the second, then refunding the first half drops the total back below the line so the next half crosses it again. Counting `paid`, `refunded` and `reversed` makes the total monotonic, which is the property that makes "crossed a multiple" mean anything — and it follows from the spec's own choice that a refund does not reverse the extension.

**The seed does not rewrite a payment, and that was the alternative to a carve-out.** Freezing the money facts for every writer broke the seed's `on conflict do update`, because its membership dates slide with `today` and `paid_at` slid with them. Carving the freeze out for trusted contexts was the easy fix and the wrong one: the one Cloud project this seed runs against is the one that will later hold a real gym's rows (OPEN-006), and a carve-out is exactly the door through which a seed, a migration or a webhook bug rewrites recorded money. `on conflict (id) do nothing` needs no door. The cost is stated rather than discovered: a re-seeded demo keeps its original payment dates while membership dates move on — cosmetic, and the more honest of the two, because the payment did happen when it happened.

**And a gate I added caused a red CI run, which is the right way round.** A `seed-dry-run` job now runs the whole seed inside `BEGIN … ROLLBACK` on every push, because a change to `supabase/seed.sql` had shipped with a syntax error no gate could see — statements verified in isolation and not in place, spliced between a CTE and the INSERT it belonged to. It also catches the reverse and more dangerous direction: a MIGRATION that breaks the seed, silent until somebody tries to rebuild the demo gym. Its first version ran `needs: migrate`, in parallel with `pgtap`, and deadlocked `h14_impersonation_and_revocation_holdout` — which then reported "planned 58 but ran 28". `db.yml`'s `concurrency` group serialises WORKFLOWS; two jobs inside one workflow run concurrently by default, which is precisely what that group cannot protect against. `needs: [migrate, pgtap]`.

**ADR-084 - A lapsed membership is refused at the gate, and liveness is judged at the gym's today rather than at the timestamp the caller sends.** `app.enforce_check_in()` admitted a check-in whenever a membership row existed with `status in ('active','frozen')`. Verified from the catalogue rather than from the file: it contained the string `ends_on` **zero** times and `starts_on` zero times. And **nothing in this product ever writes `expired`** — ADR-064 says the status flip needs a scheduler that does not exist, and ADR-075 is the round where the no-show scan was rewritten to stop trusting that column and read the dates instead.

The scan got that fix. Check-in did not. **A member whose membership ended in March could check in today, and tomorrow, and for ever**, while the console reported "No active membership. Renew before checking in." for a control the database did not have. This product is sold on collecting renewals on time and there has never been a gate behind it.

That is ADR-075's own sentence arriving in a sibling function: an argument for deriving one fact from evidence is an argument for deriving every fact of that kind.

**Found by a blind critic checking ADR-083's justification rather than its code.** ADR-083 defended moving the grant of a period from the sale to the payment by saying a forgotten payment "leaves a member refused at the gate that evening — loud, and fixed in a minute". The critic tested that claim against the trigger and it was false: the member is admitted for ever, silently. **A decision defended by a premise that turns out to be false has to be re-decided, not quietly kept** — and re-deciding it means making the premise true, because the gate is owed regardless of which end of the sale grants the days.

**The one genuine trade-off, and why it went the way it did.** The QR expiry test two branches up judges at `new.checked_in_at`, deliberately, so Phase 7's offline queue can drain without invalidating a code that was valid when it was scanned. Matching that here is the obvious move and it is wrong: `checked_in_at` is caller-supplied and unbounded, because `attendance` grants `insert` to `authenticated`. Judging liveness at a timestamp the caller chooses lets anyone whose membership has lapsed send a date from when it had not, and this gate becomes decorative — silently, which is the failure mode this project weights heaviest. Judging at the gym's today costs a 23:50 scan that syncs at 00:10, refused visibly at a boundary somebody notices. **Prefer the failure that announces itself.** Phase 7's honest fix is `offline_recorded_at` and `replayed_at`, which the table already carries, not trusting the timestamp.

**Only the QR path changed.** The whole membership test sits inside `if new.qr_session_id is not null`, so an assisted front-desk check-in has never required a membership at all. Whether a gym may record a guest, a trial or a walk-in against no membership is a product question, not a trigger's to widen unasked — carried as an open decision below.

**ADR-083 - The payment grants the membership period, not the sale.** Phase 3's `POST /api/memberships` wrote `ends_on = starts_on + plan.duration_days`, and its comment said Phase 5's extension would be "a different act from this one". Phase 5 arrived and it is not: `app.extend_membership_on_payment()` moves `ends_on` forward by the plan's duration when a payment against that membership is paid. **A front desk selling a 30-day membership and then taking the money for it granted sixty days for one month's fee, in two clicks that both looked right.**

Found by reading the seed rather than by a test: thirty seeded memberships each carry the payment that bought them, so the trigger would have pushed every one of them a month out and quietly emptied the retention fixtures of their meaning — the member whose membership lapsed on a named day, the case that outlives it.

Resolved in favour of the payment, on two grounds:

- **PAY-007 and PAY-008 already say so.** A membership created and never paid for is an intention to pay, and this product may not treat one as a payment. Granting the full period at creation did exactly that, whatever the comment beside it said.
- **The two failures are not symmetrical, and that is what decided it.** A desk that creates a membership and forgets the payment leaves a member refused at the gate — loud, and fixed in a minute. **Corrected twice.** As first written this said "that evening", and it was false in two different ways: the check-in gate did not read dates at all (ADR-084 fixed that), and even now a membership sold today with `ends_on = starts_on = today` is admitted TODAY and refused from tomorrow. A critic measured both. The argument still holds — the failure announces itself within a day — but the sentence claimed a precision it never had. The reverse leaves a member training free for a month, and nobody finds out until a renewal that never comes. Every tie in this project is broken the same way: prefer the failure that announces itself.

It also leaves one owner for one piece of arithmetic. `starts_on + duration_days` was computed in a Route Handler and in a trigger, and two places computing one thing is the shape ADR-052 and ADR-064 are both about.

**The seed states history and does not take the carve-out.** It snapshots the membership dates before its payment rows go in and restores them after, rather than exempting the trigger for trusted callers — the trigger is right, and a seed is the one caller whose job is to say what already happened rather than to make something happen. Its `on conflict` also stopped writing `receipt_number = excluded.receipt_number`, because `excluded` is null on the razorpay rows the product now numbers, and a re-run would have withdrawn a receipt number that existed yesterday.
Also settled here, and worth stating because the absence of code documents nothing: **a refund does not reverse a membership extension.** A member who paid, attended and was refunded has not un-attended; what happens to their membership is a decision a human makes.

- **OPEN-008 (Phase 6 — the red list cannot schedule a follow-up).** `next_follow_up_at` exists on `no_show_cases`, in `followUpRequestSchema`, in `apps/web/app/api/follow-ups/route.ts`, and `app.enforce_follow_up()` derives `follow_up_due` from it correctly — **and no input on the red list supplies it.** So `follow_up_due` is unreachable through the product, `no_show_cases_tenant_id_next_follow_up_at_due_idx` is fed by nothing and read by nothing, there is no "due today" screen, and ADR-076's fix guards a state no user can create. Found by the round-three critic. Closing it needs a decision this deserves a spec for: `<input type="datetime-local">` submits `2026-09-12T09:00` with **no offset**, which `z.iso.datetime({ offset: true })` rejects into the `invalid` branch — so how a wall-clock time becomes an instant in the gym's timezone is the real question, and guessing at it at the end of a phase is how the `current_date` defect happened.

## Known enforcement gaps

Stated plainly so nobody mistakes a documented rule for an enforced one. A blind critic found each of these by testing what the gates actually catch rather than what they claim to.

- **`AGENTS.md` rule #5 (status vocabularies must be Postgres enums) and rule #8 (money as integer paise) have no CI job.** Nothing stops a future session adding `export const MEMBER_STATUSES = [...]` to `constants.ts` — `registry-lint` would only ask that it be registered. ADR-021 and review are the whole defence. If Phase 1 wants these enforced, the check is "no exported array of status-like string literals outside `packages/db`".
- **`supabase/functions/**` is linted, registry-checked, dependency-cruised and duplication-checked, but not typechecked** — `.jscpd.json` lists it, and `depcruise` cruises `supabase` (the parent dir, because dependency-cruiser errors on a path that does not exist yet) with `packages-not-to-apps` extended to cover it. Typecheck there is Deno, not tsc, so no tsconfig can include it. **CLOSED 2026-09-09** by the `deno-check` job in `ci.yml`, added at the start of Phase 5 — before the payment code, not after, because an untypechecked directory is a worse place to put money handling than to discover you already did.
- **`scripts/**/*.test.ts` is run by vitest but typechecked by nothing.**
- **The `process.env` ban is defeated by aliasing.** `const p = process; p.env.SECRET` is not caught; destructuring and computed access are. `no-restricted-properties` matches on the object name, so any rebinding escapes it.
- **`check-escape-hatches` — three bypasses a blind critic proved, now closed:** every knip config filename knip honours (`knip.jsonc`, `.knip.json`, `knip.ts`, `knip.config.mjs`, …), inline `/* eslint <rule>: off */` config comments, and a nested ESLint config file anywhere below the root (which silently replaces the constitutional config for that subtree). `registry-lint` likewise now sees indented, destructured, `export type {}` and `export * as` shapes, and only exempts the framework-mandated names in Next.js route files rather than the whole file. `check-test-immutability` treats `supabase/tests/**` as tests. Each has a test per hole in `scripts/__tests__/`.

## Open decisions (resolve deliberately when the phase starts)

These are unresolved by intent, not by oversight. Do not invent an answer while building the phase named — read this entry, make the call, and convert it into a numbered ADR above recording what was chosen and why.

- **OPEN-001 — RESOLVED by ADR-057**, a `workflow_dispatch` bootstrap whose `where not exists` makes it inert after one use. *Original text:* How is the very first `super_admin` account created? §5 requires gym self-signup to go through super-admin approval before going live, which means the platform cannot bootstrap its own first admin through that same signup flow. Needs a deliberate answer: a one-time seed script gated behind a secret, a `supabase` CLI/SQL runbook step documented in `docs/security.md`, or an invite-only bootstrap endpoint — whichever is chosen must not become a general-purpose backdoor for creating admins later.
- **OPEN-002 (deferred from Phase 2 to Phase 3, 2026-09-08).** Phase 2 built no Route Handlers, so there is still nothing to generate; `docs/architecture.md` deferred this package on exactly that reasoning and the reasoning has not changed. Moved deliberately rather than silently carried. What generates `packages/api-client`, "one generated client" consumed by both web and mobile (§4)? The master prompt names the requirement but not a tool. Candidates to evaluate against the zod-schemas-from-generated-types chain (§10): `openapi-typescript` + a hand-rolled fetch wrapper, `ts-rest`, `orval`, or a thin hand-written client if route handlers are few enough that generation adds more ceremony than it saves.
- **OPEN-003 (Phase 5 — Money).** GST invoice PDF generation approach. No library or service is named in the master prompt. Candidates: a PDF library run server-side (e.g. an HTML-to-PDF render in a Route Handler or Edge Function), a headless-browser render, or a third-party invoicing/PDF API. Must satisfy the per-gym invoice-numbering and financial-year-reset config already specified in `docs/data-model.md`, and must not require a client-side-only library that can't run in a Route Handler.
- **OPEN-004 (Phase 6 — Growth surfaces).** Transactional email provider. Not named in the master prompt. Needed at minimum for staff email-based auth flows (§12 Phase 2 names "email staff" as an identity mechanism distinct from member phone-OTP) and plausibly for receipt/invoice delivery and the owner's daily summary. Candidates: Resend, Postmark, AWS SES. Evaluate against India-region deliverability and Supabase Auth's custom-SMTP hook if staff auth ends up needing it earlier than Phase 6 — if so, pull this decision forward rather than leaving staff auth on Supabase's default (rate-limited, not for production) email sending.

---

- **OPEN-015 (Phase 6 or 7, when a member-facing and trainer-facing UI exists).** The role matrix is **table-granular**, so a role that needs any column of a table gets every column. Two consequences a blind critic measured rather than assumed: a `trainer` reads `memberships.price_paise`, `addon_orders.total_paise`, `plans.price_paise` and `organization_settings.gstin`, because all four tables are `is_staff()` reads; and a `member` reads their gym's `organizations` row entire, including `tier`, `status` and `trial_ends_at` — the platform's commercial relationship with the gym, which is none of the member's business. Neither is a policy bug and neither leaks another tenant. Narrowing them needs column-level privileges or a view per audience, which is a design decision with its own maintenance cost and should be made when there is a screen that needs it, not before. **Do not "fix" this with a fifth gate** — the vocabulary is closed on purpose (ADR-055).

- **OPEN-016 (Phase 4 - Retention engine).** `max_freeze_days_per_year` and `plans.max_freeze_days` are enforced by nothing. Both are read-then-write in application code, which is the same race the check-in guard was built to remove, so only a constraint trigger closes them properly. Neither's period is defined either: "per year" could be the calendar year or the gym's `financial_year_start_month`, and per member or per membership. Phase 3 chose calendar year, per member, and recorded it here rather than in code comments - decide it deliberately when the retention engine needs it.
- **OPEN-017 (Phase 3 or 7).** Calendar streaks take their window as a parameter because **no challenges table exists**. STK-001 names "calendar streak (challenge periods only)" and there is nowhere to store a challenge period, so the computation is correct and unreachable from any screen until one exists.

- **OPEN-018 (Phase 6 — gym onboarding).** A gym with no `organization_settings` row silently gets no check-in de-duplication, no no-show threshold, no renewal offsets and no pause approver. **Zero organizations are in that state today** and nothing in the product creates a gym yet, so it is latent — but two obvious fixes were tried in Phase 3 and both were worse than the gap. Detecting it in the check-in trigger is unsafe: under `security invoker` "no settings row" and "a gym you cannot read" are the same answer, so raising answers a question about another tenant *before* the policy refuses the row (ADR-066's pair, in the fix for something else — two blind holdout assertions caught it). Creating the row from an `after insert` trigger on `organizations` collides with the settings row that `seed.sql` and thirty pgTAP fixtures insert themselves. **The right place is gym onboarding**, which creates both rows in one transaction, and that is Phase 6.

### Open decisions added during Phase 5 (money)

- **OPEN-027 (a plan round-trip keeps a length no plan carries).** A gym admin may re-length a plan, point a membership at it, point it back, and restore the plan; the membership keeps the length it was pointed at, with no artefact anywhere. It needs `is_gym_admin()`, and the same admin reaches the same outcome legitimately by creating a long plan and selling on it — which differs only in leaving evidence. Closing it means freezing `plans` once a membership has been sold on one, which contradicts assertions both suites hold: that a manager may re-length a plan, and that the edit must land. The honest instrument is an audit trail, which this product does not have and which is not Phase 5's to invent. ADR-093.
- **OPEN-028 — RESOLVED by the membership-net-price change, 2026-09-10.** A period now costs `price_paise - discount_paise`: ₹12,000 less ₹1,200 is fully paid at ₹10,800. The change freezes `discount_paise` with the other `GL043` terms once money arrives, applies the same net amount to grants and product displays, and enforces `0 <= discount_paise <= price_paise`. Its guarded historical correction counted the already-paid annual period once without moving dates, status, or receipt history. Reminder calculations and owner metrics consume the same definition when those Phase 6 surfaces are implemented. Original finding: `memberships.discount_paise` existed and the seed wrote it, but the money path scored a period against `price_paise` alone; the discounted demo member who paid ₹10,800 was shown as owing ₹1,200. ADR-090, ADR-094.
- **OPEN-029 (creation is unpoliced for dates).** A front-desk session may create a membership dated `today … today + 3650` with no payment anywhere — ten years, and no UPDATE for `GL045` to refuse. Closing it means "a membership is created with no span it has not been paid for", which `supabase/seed.sql` contradicts on every demo row and fixtures contradict in both suites. It leaves a whole membership row as evidence, which the UPDATE route did not. **Round eleven narrowed it**: creating at a price the plan does not carry is now gym-admin work (`GL046`), so what remains open is the span, not the price. ADR-093, ADR-094. **ADR-098 widened it once, and the second thing it looked at turned out not to be there.** What is real: the remedy this phase prescribes for every mis-sold membership — **"refund, cancel and sell a new one"** — is not available to the party that makes the mistake. Measured from a front-desk session against a paid payment: `insert into public.refunds ...` is refused `42501`, *new row violates row-level security policy*, while the same insert from a `gym_owner` session passes the policy. Cancelling the membership, the other half of the remedy, the desk can do. So the desk can create a ten-year membership and can retire it, but cannot give the money back, and every rule in this phase that answers "correct it by refunding" is answering with something its most likely caller cannot do. That is not an escalation defect — a refund SHOULD be gym-admin work (ADR-082's form: the subject is a decision about money leaving, which a desk legitimately lacks) — but the span rule this open item will write must not phrase its remedy as though the desk could carry it out.

**What is NOT real, having been checked rather than assumed:** every frozen-terms rule refuses an UPDATE, so DELETE-then-INSERT at the same id would consult none of them — there is no `old` row to compare against. That route is closed by a table grant, not by a rule: `authenticated` holds `INSERT, SELECT, UPDATE` on `public.memberships` and **no `DELETE`**, which only `service_role` and `postgres` have, so a front desk attempting it gets `42501 permission denied for table memberships` before RLS or any trigger is reached. It survives as a trusted-caller route, which is the same population OPEN-029's span question already concerns, and it is recorded here so the next reader does not re-derive it as new.

- **OPEN-032 (a refund's denomination and kind stay writable).** `GL041` freezes a refund's `payment_id` and `amount_paise`, and round seventeen froze its status once `completed`. `currency`, `kind` and **`id`** stay writable for the life of the row — a holdout author measured both returning OK on a completed refund. Neither reaches the ceiling: `app.enforce_refund_total()` sums `amount_paise` across both kinds and reads neither column, so nothing is wrong today. But **a refund's denomination is editable while the ceiling bounding it is denominated in nothing**, which is the same shape as the membership currency that ADR-090 had to freeze. A later critic added `id` to the list — rewritable on a `completed` refund, harmless only because nothing references a refund, which is the same argument that was true of `payments.id` until `refunds`, `invoices` and `addon_orders` started pointing at it (ADR-087). Recorded rather than fixed because the columns are inert, and a freeze on an inert column is a rule nobody can test the harm of.
- **OPEN-033 (when is "has taken money" evaluated?).** `with p as (update payments set status='paid' …) insert into refunds select … from p` is accepted today: the refund's payment reaches `paid` in the same statement that writes the refund. The new requirement says a refund is refused unless its payment "has actually taken money" and never says *when* that is judged. The two readings differ only in this shape, and a round that changes the answer changes an API contract silently. Reported by a holdout author, staged rather than asserted.

- **OPEN-034 — RE-SCOPED by ADR-105 and CLOSED for `memberships` by ADR-108.** What remains open is the payment and refund side only; on `memberships` the question is answered as far as this project will answer it, and the reason is worth carrying: **the contract decides precedence only among rules this project owns, and records everything else as observation.** Nine critic rounds established that a normative sentence about which of Postgres's mechanisms answers first cannot be kept true by anybody — the set grows whenever a column, a constraint or a referencing table is added. Whoever takes the payment side should start from that shape rather than re-deriving it: five codes in one function is a closed set, "every way a database can say no" is not. Original entry follows.

- **OPEN-034 (original: which of two refusals answers is undecided in four more places, and unassertable in a fifth).** ADR-100 decided the precedence for the four membership-terms absolutes and a holdout author named five further pairs where two rules can answer one statement with nothing deciding which: `GL038`/`GL039` on a paid payment walked backwards and edited (the highest-value one — it is the payment-side twin of the pair just decided); `GL034`/`GL035` on a cash payment naming a colleague and carrying a provider id; `GL036`/`GL040` on a refund against a payment that never arrived and naming a colleague; and the payment-side `GL042` sharing a code with the membership-side rule but not its precedence sentence. **This list omitted `GL042`/`GL047` — the only pair the same session had itself introduced, one commit earlier — which a critic then found (ADR-101). The omission is the more useful half of this entry: a general rule written about other people's code is not applied to your own by being written down.** The fifth is different in kind and should be fixed first: **the refund amount-freeze is assigned no SQLSTATE anywhere in the contract**, its sentence sitting under the `GL036` requirement while the code-assignment paragraph sits under `GL041`, so a statement demoting a completed refund and raising its amount cannot be asserted at all. Not a money hole — every path still refuses — but the same shape that ADR-099 and ADR-100 each cost a round: precedence inherited from typing order. ADR-100.

  **Two more members of this family, both raised by a blind author rather than found by a critic**, which is the cheaper order. First: **a foreign key is not enforced during the UPDATE** — Postgres implements it as an `after` row constraint trigger, so it competes with `memberships_terms_frozen` on exactly the trigger-name ordering ADR-101 was bitten by, and wins only because the generated `RI_ConstraintTrigger_c_…` sorts before `memberships_…` under byte ordering. Measured `23503` today; under a collation-aware sort it would be `GL042`. The contract records it as an observation about Postgres rather than a decision. Second: **the order among the excluded mechanisms is itself undecided** — a statement violating the row-security policy and a composite foreign key at once answers `42501` today, and nothing says it must.

  **This table lists only the triggers this project WROTE, and that is its second defect rather than a scope note.** Measured on `public.memberships`: **22 triggers, of which 4 are ours and 18 are internal constraint triggers; 13 fire on UPDATE, 9 of them internal.** Every internal one sorts before `memberships_*` under byte ordering, so nine mechanisms answer ahead of both this project's after-row triggers and the table below shows none of them. ADR-101 calls this table "the one place ADR-101 tells a future session to look up trigger order" and ADR-103 says in the same breath that a foreign key IS an after row trigger on this table — so the table contradicts its own ADR. It is kept because the ordering *among this project's own rules* is the only ordering the contract makes a claim about (ADR-105), and it is accurate for that; it is **not** the full firing order of the table.

  **The fire order of this project's own triggers, measured with `for each row` / `for each statement` in the query — which the first version of this table omitted, and got `payments` wrong because of it.** Postgres orders triggers by name only *within* a timing AND level; every after-row trigger fires before every after-statement trigger regardless of name.

  | table | timing | level | fire order |
  |---|---|---|---|
  | `attendance` | before | row | `attendance_enforce_check_in` → `attendance_written_once` |
  | `document_counters` | before | row | `document_counters_monotonic` → `document_counters_touch_updated_at` |
  | `impersonation_sessions` | after | row | `impersonation_sessions_audit_end` → `impersonation_sessions_audit_start` |
  | `memberships` | after | row | `memberships_terms_frozen` → `memberships_transitions_after_terms` (**after ADR-101**; it was `memberships_status_transitions` → `memberships_terms_frozen`, the defect ADR-101 fixed) |
  | `memberships` | before | row | `memberships_stamp` → `memberships_touch_updated_at` |
  | `payments` | after | row | `payments_arrival_status` → `payments_enforce` → `payments_identity_frozen` |
  | `payments` | after | **statement** | `payments_extend_membership_insert` → `payments_extend_membership_update` |
  | `payments` / `refunds` | before | row | the `_stamp` trigger before the `_touch_updated_at` one |

  **`payments` is still the one to look at, and the corrected table changes what it says.** Three competing after-ROW triggers, with `payments_identity_frozen` — the `GL038` money freeze — last of the three and therefore losing to `payments_arrival_status` and `payments_enforce`. The two `extend` triggers are `for each statement` and fire after all three, not around them; they are also INSERT-only and UPDATE-only, so they never both fire. Every pair OPEN-034 lists on the payment side is decided today by names nobody chose for their alphabetical order. Whoever takes this should decide the contract first and then set the names, rather than reading the current order back as though it were a decision.


- **OPEN-031 (a refund can be double-submitted).** `POST /api/payments` carries a composite idempotency key and an essay about why; `POST /api/refunds` carries neither, `refunds` has no `idempotency_key` column, and the form has no nonce — so a double submit records two partial refunds. `GL036` still bounds the total to what was actually taken, so the gym cannot refund more than it received, and both rows show on the receipt page, so it is visible rather than silent. But the payments route's own reasoning applies word for word to the refunds route, and no requirement covers it: `manual-payment/spec.md` scopes idempotency to payments. Found by a critic in round fifteen and not fixed there, because adding a column and a contract in the same breath as a fix is what produced two rounds of this phase.

- **OPEN-030 — CLOSED by ADR-098** (memberships have no state machine, and payments do).** `app.payment_transition_allowed()` decides which payment status may follow which, and `GL039` enforces it. `memberships.status` has no equivalent: a blind author measured a **front-desk** session writing `cancelled → active`, reviving a retired membership in one statement, and writing `status = 'expired'` directly onto a row live for another ten days — **a status ADR-064/075 say nothing in this product ever writes**, because liveness is derived from dates. Neither is a money defect on its own: reviving a membership makes its money count for what it always bought, and the requirement above now says so. But "which status may follow which, and who may set it" is a contract **this phase did not write and wrongly assumed nobody had**: `docs/data-model.md` has listed the legal transitions since Phase 1 and calls `expired` and `cancelled` terminal. A critic found that within a round. The gap is not a missing contract — it is that **nothing enforces the one that exists**, and a routing-table document asserts a rule the database does not keep, and three of its rules now depend on the answer — the retired-membership rule, the check-in gate (ADR-084), and the one-live-membership index. Whoever writes it should start from `app.payment_transition_allowed()`, which is the same problem solved one table over.

**All three were cited by ADR-093 and ADR-094 for two rounds before being written down here**, which a critic found by grepping for them. The routing table sends readers to this file for "open decisions not yet resolved", so three holes this phase deliberately left open were unfindable by exactly the reader they were written for.

## Market context

Researched 2026-09-05, before the stack was locked. Recorded here because several ADRs above depend on it and it otherwise exists only outside this repo — a future session re-deciding pricing or messaging without it would reach a different, worse answer.

- **Competitors** (GymForce, Akton, GymFito, AdviceFit) ship WhatsApp Business API, UPI auto-debit and GST invoicing as *standard*, some priced around **₹89/month**.
- **Consequence for pricing (ADR-017):** competing on price is unwinnable and pointless. Gymloop prices as SaaS tiers by active-member count (~₹1,499 / ₹2,999 / ₹4,999) and differentiates on the retention loop — no-show red list, follow-up outcomes, measurable recovery — not on being cheaper.
- **Consequence for messaging (ADR-016):** WhatsApp Business API and UPI Autopay are **table stakes, not extras**. WhatsApp API lands in Phase 1.5 as the paid tier; Razorpay mandate tables go into the schema from day one (`docs/data-model.md`) so Phase 2 needs no migration to enable Autopay.
- **WhatsApp cost basis:** utility template messages run roughly **₹0.14/message** all-in. From **1 Oct 2026** Meta also bills *service* messages at the utility rate — which is what makes the per-gym credit wallet (ADR-016) necessary rather than optional.
- The original single-gym blueprint's pricing (₹10,000 one-time build + ₹500/month) is a per-gym project quote and does **not** apply to this product.

---

## Open decisions added after the Phase 0 blind critic

- **OPEN-005 — resolved by ADR-030** (CI applies migrations on serial merge to `main`; drift and pgTAP verify after the apply; `AGENTS.md` rule #7, ADR-024 and gate 9 updated together).
- **OPEN-006 (before real gym data lands — around Phase 5).** Split to a separate Supabase test project so the pgTAP suite and integration tests stop sharing a database with production rows. Until then the rollback rule (ADR-030) is the whole protection; once a paying gym's data exists it is not enough.
- **OPEN-007 (Phase 4 — Retention engine).** `pg_cron` tests need committed state — a scheduled job cannot observe rows inside a transaction that rolls back — so they cannot use the `BEGIN … ROLLBACK` pattern ADR-030 mandates. Solve it there: a dedicated schema or project for cron tests, or a cleanup-on-success pattern with the same "never leaves state behind" guarantee stated and enforced.
- **OPEN-008 — resolved by ADR-052** (every foreign key to a tenant-scoped parent is composite; 50 of 88 keys across 23 tables, with `audit_log` exempt because its nullable tenant column would make `match simple` stop enforcing). Original statement kept for the reasoning: **(Phase 2 - Identity & tenancy, before any product code writes these tables).** No foreign key in the schema re-checks the tenant: every FK references its parent's `id` alone, and Postgres runs referential-integrity probes with row security off, so a gym can write a row *into its own tenant* that references another gym's member, staff row, membership or payment. ADR-047 removed every consequence four blind critics could find, by tenant-scoping the constraints that stood behind it, but the write itself is still possible and it produces rows that are junk in one tenant and invisible in the other. The full fix is `unique (tenant_id, id)` on every parent and composite `(tenant_id, <fk>) references parent (tenant_id, id)` on every child - around forty constraints, one migration, no data to rewrite today and a great deal to rewrite once gyms exist. **Do it in Phase 2 or accept it explicitly.** The cheap alternative, if the owner would rather not carry forty composite keys, is a Phase 2 rule that every Route Handler validates parent tenancy before insert - which is a guard that can be forgotten in a code path, exactly what ADR-037 argues privileges beat.
- **OPEN-009 — RESOLVED by ADR-056** (the hook reads the per-table active definition, a trigger deletes `auth.sessions` on deactivation or role change, `jwt_expiry` is 900, and the platform write gate is `super_admin` only so support can no longer promote itself). *Original text:* `platform_users.is_active` and `staff.is_active` are written by nothing and read by nothing. `app.is_platform()` reads only the `app_role` JWT claim, so deactivating a compromised super admin does not revoke their live token - and while it lives they retain `for all` on `platform_users` and can set their own row back. Phase 2's access-token hook must read `is_active`, and must decide the session-revocation story (short expiry, a `session_id` check against `auth.sessions`, or both). Related, and in the same hands: `platform_support` and `super_admin` are indistinguishable to every Phase 1 policy, so a `platform_support` session can promote itself by updating `platform_users`. That is ADR-032's deferral of the role matrix working as intended, and it must not be inherited silently.
- **OPEN-010 (Phase 3 - Core domain).** Two integrity rules Phase 1 cannot express and Phase 3's write paths must own. `consents.recorded_at` is client-writable on an append-only table whose read model is "latest row per (member, purpose)", so a row stamped far in the future is latest forever and a later genuine withdrawal never takes effect (DPD-004); `check (recorded_at <= now())` is rejected as non-immutable, so the consent writer must stamp it server-side. And `notifications.dedupe_key` is unconstrained `text`, so PAY-002's "at most one message per stage" holds only as far as the sender composes a deterministic key - fold a run id or a timestamp into it and the unique index never fires. The precedent for constraining it in the schema is `audit_log_action_format_chk`, which exists precisely because the format carries the meaning.
- **OPEN-011 (contract-wide, whenever the next `spec:` change touches it).** Every "not empty" check in the schema is `<> ''`, which a single space satisfies - including `attendance_corrections.reason`, where DQA-003 says a correction without a reason must be *impossible*, and `membership_pauses.reason`, `refunds.reason`, `impersonation_sessions.reason`, `consents.version` and `consents.source`. `btrim(x) <> ''` is the strictly stronger form. Not changed here because it touches every cluster including three already applied, and because it is a uniform contract decision rather than a per-table one.
- **OPEN-014 (whenever the pgTAP harness is next touched).** The "no JWT claim at all" branch of `app.current_tenant_id()` is **not exercised anywhere in CI**, and the suite should not be read as though it were. A missing claim has two shapes — the GUC set to the empty string, and the GUC never set in the session — and `docs/data-model.md` requires both to yield null and zero rows without raising. Only the first is reachable: a custom GUC placeholder, once assigned, reverts to the empty string rather than to undefined, `reset` and `set … to default` both land on the empty string, and `DISCARD ALL` — the one statement that would restore the unset state — cannot run inside a transaction block, which every pgTAP file is by ADR-030. One holdout file used to cover the unset branch by accident, because it drew a fresh backend when the suite ran as separate invocations; ADR-051 collapsed those back to one and the accident ended. Both branches are fail-closed in the accessor's implementation, so this is missing coverage rather than a known hole. **The fix, if it is wanted, is a CI decision and not a test-file one**: run one file in its own invocation so it gets a fresh connection. Recorded rather than quietly lost, because the alternative is a suite that looks like it covers a branch it does not.
- **OPEN-012 (Phase 5 — Money).** Invoice arithmetic is unconstrained and unspecified: `total_paise` is unrelated to `taxable_paise + cgst + sgst + igst`, CGST/SGST and IGST are not mutually exclusive, `cgst_paise = sgst_paise` is not enforced, a refund may exceed the payment it refunds, and a discount may exceed the price. No requirement in any Phase 1 spec states these rules, so this is a specification hole rather than a missed implementation — recorded rather than archived in silence, on the blind final critic's finding. Phase 5 either writes the checks or states why the arithmetic is the application's to own.
- **OPEN-013 — RESOLVED by ADR-055** (a 36-row role matrix, read and write as separate policies, and a member-scoped read policy on the fourteen tables a member may see). *Original text:* There is no intra-tenant authorization. Every tenant policy is `tenant_id = app.current_tenant_id()` and nothing more, so any `authenticated` session carrying a gym's `tenant_id` can read and write **every row in that gym** — every other member's phone number, every payment, `staff.role`, `razorpay_accounts`. That is a deliberate Phase 2 deferral (`docs/data-model.md` says the role matrix lands there) and no Phase 1 spec promises otherwise, but `member` is already an `app_role` label and no open item said out loud that the current default is full-gym access. It is DPDP-relevant: a member session under this shape can read the whole gym's personal data. Phase 2 must confront it before it issues a member token, not after.
- **OPEN-019 (Phase 6 — an assisted check-in requires no membership at all).** The whole `GL013` live-membership test sits inside `if new.qr_session_id is not null`, so a front-desk assisted check-in has never required the member to hold a membership — before ADR-084 or after it. That may be right: a gym records trials, guests and day passes, and `attendance.source = 'front_desk'` already carries an acting staff member and a non-empty reason (ATT-005, ATT-006), which is exactly the accountability a discretionary entry needs. It may equally be a hole the size of the one ADR-084 just closed. **It is a product question and a trigger is not the place to answer it unasked**, so it is named here rather than widened silently. Whoever resolves it should decide what a trial visit IS before deciding whether to refuse one.
- **OPEN-020 (Phase 5 Razorpay — a desk session cannot touch an online payment at all).** `app.enforce_payment()` refuses any row where `method = 'razorpay'` or any provider column is non-null, on INSERT **and** UPDATE, for every session row security applies to (`GL035`). That is correct for authoring — online state is the provider's to report (PAY-006) — and it also means a gym manager cannot add a note to an online payment, or correct anything about one, ever. Harmless today because no online payment exists. **It is a constraint the Razorpay half will meet on its first day**, and it should be a decision then rather than a discovery: either the writable columns (`notes`, `failed_reason`) are exempted from the provider rule, or the console simply never offers them for online rows.
- **OPEN-021 (Phase 6 — no upper bound on a typed amount).** `paiseFromRupees` accepts up to ₹999,999,999.99 and the desk form has no confirmation step, so a front desk fat-fingering an extra zero records ₹15,000 for ₹1,500 — and now consumes a receipt number doing it, which ADR-085 makes permanent. No requirement anywhere names a bound, which is why this is an open decision and not a defect. The cheap answers are a confirmation step above some multiple of the membership's own price, or a per-gym ceiling in `organization_settings`; the honest observation is that the second is configuration nobody will set and the first is the one a front desk would actually notice.
- **OPEN-022 (Phase 7 — an offline check-in cannot be replayed after its membership lapses).** ADR-084 judges membership liveness at the gym's today rather than at `checked_in_at`, because that column is caller-supplied and unbounded and judging there would make the gate decorative. The price is a 23:50 scan that syncs at 00:10, or any offline queue drained after a membership ends: refused, visibly. **The fix is not to trust the timestamp** but to trust the evidence — `attendance.offline_recorded_at` and `replayed_at` already exist for exactly this, and a replay carrying them can be judged at the instant it claims while a plain insert cannot. Phase 7 owns the offline queue and should own this with it.
- **OPEN-023 (Phase 6 — nothing activates a `pending` membership later).** A paid payment now grants a dateless `pending` membership its dates and makes it `active` — but only if nothing else of that member's is already live, because `memberships_tenant_id_member_id_live_key` permits one. Where another IS live, the second keeps its dates and stays `pending`, which is right today (the member is covered) and incomplete tomorrow: **nothing ever activates it when the first lapses.** This is the same shape as nothing writing `expired` (ADR-064, ADR-075) seen from the other end, and it wants the same answer — a scheduler, or a rule that derives liveness from dates rather than from a column somebody has to write. The console cannot reach the state (`POST /api/memberships` refuses to sell a second membership while one is live), so it is reachable only by direct write today.
- **OPEN-024 (Phase 6 — a lapsed member cannot be sold a REPLACEMENT membership).** Round five restored the renewal path: the payment form now names a lapsed membership, so taking money renews it from today. What is still refused is selling that member a *different* plan — `POST /api/memberships` inserts `status: 'active'` and `memberships_tenant_id_member_id_live_key` sees the lapsed row as still `active`, because nothing ever writes `expired` (ADR-064). So a member who lapsed on a Monthly plan can renew Monthly and cannot upgrade to Annual without somebody editing the old row by hand. The clean answer is the same one OPEN-023 and ADR-075 point at — liveness derived from dates rather than from a column somebody must write — and the partial index would need to become one over dates, which is not an index Postgres can build (`now()` is not immutable). Worth a deliberate decision rather than an accident.
- **OPEN-025 (Phase 6 — two concurrent activations, and a deadlock nobody maps).** `app.grant_periods()` locks the membership it is granting, so two transactions paying two DIFFERENT dateless memberships of one member each see no live sibling and both activate; the second commit hits the one-live index and the handler reports it as `already_recorded`. Same root as OPEN-023 and vanishingly rare, since the console cannot create the state. Separately, `grant_periods` locks memberships in group-by order, so two concurrent commands paying the same two memberships can deadlock, and `40P01` is unmapped — a transient failure presented to the desk as a permanent one. Both want the same fix: lock in a deterministic order and map the retryable SQLSTATEs.
- **OPEN-026 (Phase 6 — a half-dated `pending` membership takes money and stays refused).** `memberships_dated_unless_pending_chk` permits a `pending` row with one null date. Paying it extends `ends_on` and records `periods_granted`, but it cannot be activated (activating a row with a null `starts_on` violates that same CHECK), so the member stays refused at the gate with no product path to fix it. Round three aborted the payment loudly with an unmapped `23514`; round five's guard made it silent instead, which is the wrong direction by this project's own tie-breaker — but the right fix is not a louder error, it is deciding what a half-dated membership MEANS, and nothing in the product can currently create one. Whoever adds a membership-request or onboarding flow owns this. A critic noted the mirror case in the same breath: ADR-088's backfill records `periods_granted` for the OTHER half-dated shape (`starts_on` set, `ends_on` null) where the extension's `ends_on is not null` guard means no days were ever granted — so that row carries a count of periods it never received, and would grant fewer than its money bought if somebody later completed it. Same malformed state, same direct-write-only reachability, same owner. **ADR-098 added a third member of this family**, and said it was recorded here while this entry said nothing about it: `pending → cancelled` is legal by the membership lifecycle and unreachable for a dateless row, because that same CHECK answers `23514` first. A sale called off before any money arrived is exactly that row — the transition is permitted by the lifecycle and refused by the shape. Same constraint, same question of what a half-dated membership means, same owner.

---

## Taken without the product owner during the Phase 1 overnight run — confirm or reverse

The owner authorised the Phase 1 run end to end and was asleep for it, with a standing instruction: where a decision is genuinely the owner's, pick the safest default, record it, and carry on. Each of these is that. None is load-bearing enough that reversing it later costs more than one migration or one commit, and each names what reversing it would take.

- **Roles move to the database (ADR-031).** `ROLES`/`Role` deleted from `packages/shared`; `app_role` is the enum. Reverse cost: re-adding a constant and a registry row — but then two vocabularies exist again.
- **Claim names `tenant_id` and `app_role`; accessors in a private `app` schema (ADR-032).** Phase 2's access-token hook must set exactly these two claim names, or every policy in the schema silently matches nothing. Reverse cost: one migration replacing two function bodies, before Phase 2 ships the hook. **This is the decision with the shortest window to change cheaply.**
- **Two tables outside tenant scoping (ADR-033).** `platform_users`, `audit_log`. Reverse cost: a migration, plus special-casing a synthetic platform tenant in every isolation test.
- **The seed is a manually dispatched workflow, not a migration and not a session command (ADR-034).** Reverse cost: none, it is one workflow file.
- **UUID v4 primary keys (ADR-035).** Reverse cost: high once data exists — this is the one to disagree with early if at all.
- **DPD-007 split: retention policy now, breach runbook in Phase 8 (ADR-036).** Reverse cost: none, it is a doc.
- **Calendar-day defaults move off `current_date` to IST-local (ADR-039).** A contract change, so a `spec:` commit. Reverse cost: one migration per affected column, and reversing reintroduces a five-and-a-half-hour window each day in which a date is wrong.
- **The check-constraint rule word is now fixed (ADR-040).** Reverse cost: none, it is a naming convention — but it must be fixed *before* the fan-out, not after.
- **The per-table privilege contract, and no `force row level security` (ADR-037).** Previously decided and written into `docs/data-model.md` with no ADR number; the `pg_default_acl` finding behind it was re-verified against Cloud in this run before numbering it. Reverse cost: a migration per table, and a signed-in user regains `delete` and `truncate` on everything.
- **Tests and implementation land as two commits in one push, with the red pgTAP run taken once on the contract cluster (ADR-038).** Reverse cost: none going forward, but a cluster already landed cannot be re-run red.
- **A `member_devices` table was added to the `comms` cluster, which the in-flight table list did not have.** ADR-016 makes push notifications v1's primary channel, and a `notifications` row with no device token to deliver to is a dead end; the alternative was a Phase 3 migration to add it. It is deliberately minimal — member, platform, push token, last-seen, active flag — and carries no delivery-receipt state, which belongs on `notifications`. Reverse cost: dropping one table before any code reads it. *The first attempt at this recorded the decision here and in `docs/security.md`'s retention table and the `comms` EARS spec, but never added the table to `docs/data-model.md`'s table list — so the cluster agent implementing `comms` would have had a requirement with no table to build. Caught on the resumed run's critical read of the in-flight work; the table list now carries it.*
- **The contract was drafted by the orchestrator's sub-agent and reviewed by the orchestrator, then by a fresh-context blind critic**, rather than by a second independent author. The property that matters — the contract is fixed and independently checked before any fan-out — holds either way.
