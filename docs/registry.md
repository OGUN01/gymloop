# Registry — the anti-duplication index

**If it is not in this table, it does not exist.** Before writing any helper, constant, type, hook, or component, search this file and grep the codebase. Reuse what's here, or record why you couldn't in `docs/decisions.md`. Adding an exported symbol without registering it here fails the `registry-lint` CI gate.

Update this file in the same commit that adds the export. One row per symbol, kept alphabetical within its table.

## Constants

| Name | File | Purpose | Used by |
|---|---|---|---|
| `DEFAULT_CURRENCY` | `packages/shared/src/config/constants.ts` | ISO currency code for money amounts | Phase 5 (money) |
| `DEFAULT_TIMEZONE` | `packages/shared/src/config/constants.ts` | Fallback IANA timezone; gyms override per-tenant | Phase 1 (data model), Phase 4 (retention scans) |
| `GATE_CODE_BYTES` | `packages/shared/src/config/constants.ts` | Randomness in a check-in gate code: 8 bytes rendered as 16 uppercase hex characters — unguessable, and still typeable when `BarcodeDetector` is absent (ATT-003). Only its SHA-256 hash is ever stored | `apps/web/lib/gate-code.ts` |
| `GATE_CODE_TTL_MS` | `packages/shared/src/config/constants.ts` | How long an issued gate code stays scannable — fifteen minutes, which is what makes ATT-003's "a screenshot does not remain scannable" true. **Not** the de-duplication window, which is per gym (`organization_settings.checkin_dedupe_seconds`) and must never be given a constant to fall back on | `apps/web/app/api/gate-code/route.ts` |
| `GYM_CODE_LENGTH` | `packages/shared/src/config/constants.ts` | Length of the 6-char gym invite code (§5) | Phase 2 (identity), member app gym-finder |
| `PG_INSUFFICIENT_PRIVILEGE` | `apps/web/lib/api.ts` | SQLSTATE `42501` — what a row-security refusal of an `INSERT` looks like from the other side of PostgREST. Every handler that inserts recognises it and answers 403 rather than 500, so the role matrix stays the only statement of who may write what | `apps/web/app/api/check-in/route.ts`, `apps/web/app/api/gate-code/route.ts` |
| `PG_UNIQUE_VIOLATION` | `apps/web/lib/api.ts` | SQLSTATE `23505` — on `attendance_tenant_id_client_event_id_key` it means one check-in attempt arrived twice, which the handler answers with the row that already exists rather than with an error | `apps/web/app/api/check-in/route.ts` |
| `PLAN_TIER_PRICES_PAISE` | `packages/shared/src/config/constants.ts` | Monthly SaaS tier prices in integer paise (§5) | Phase 6 (platform billing) |
| `PRODUCT_NAME` | `packages/shared/src/config/constants.ts` | The one place the placeholder product name lives | Everywhere the name is displayed |
| `RENEWAL_REMINDER_WINDOWS` | `packages/shared/src/config/constants.ts` | Renewal reminder windows, each with an explicit `daysFromExpiry`: **negative = before expiry, 0 = expiry date, positive = after** (so the spec's "+3" is `+3`). Ids (`expiry_minus_14` … `expiry_plus_3`) are unambiguous without reading the sign (PAY-001) | Phase 4 (reminders), Phase 5 (renewals) |
| `SUPABASE_REGION` | `packages/shared/src/config/constants.ts` | ap-south-1 (Mumbai) — latency-driven choice, see `docs/decisions.md` | Infra docs, onboarding checks |
| `SUPPORTED_LOCALES` | `packages/shared/src/config/constants.ts` | English + Hindi (§4) | i18n setup, all UI phases |
| `TRIAL_DAYS` | `packages/shared/src/config/constants.ts` | 14-day full-feature trial length (§5) | Phase 2 (gym signup/onboarding) |
| `MS_PER_DAY` | `packages/shared/src/config/constants.ts` | Milliseconds in a day. Used only to step a *calendar* day forward after an instant has already been collapsed to a gym-local `YYYY-MM-DD` — never to convert an instant to a day, which is what makes a 23- or 25-hour DST day harmless here (MNY-004) | `packages/shared/src/streaks/streaks.ts` |
| `RED_LIST_PAGE_SIZE_DEFAULT` | `packages/shared/src/config/constants.ts` | Cases per page of the red list — 25, smaller than the roster's 50 on purpose: the roster is scanned for a name, the red list is a work queue read top to bottom before the 6am rush, and a page longer than the calls anybody can make is a page that hides its own bottom | `apps/web/lib/red-list.ts` |
| `RED_LIST_PAGE_SIZE_MAX` | `packages/shared/src/config/constants.ts` | The clamp for {@link RED_LIST_PAGE_SIZE_DEFAULT} — 100. A page size is a hint from a caller, so an over-large one is clamped rather than refused | `apps/web/lib/red-list.ts` |
| `PAYMENT_PAGE_SIZE_DEFAULT` | `packages/shared/src/config/constants.ts` | 25 — a day's takings, not a month's. The screen answers "what did we take today, and does the drawer agree", and a longer page is a longer scroll past yesterday | `apps/web/lib/payments.ts` |
| `PAYMENT_PAGE_SIZE_MAX` | `packages/shared/src/config/constants.ts` | The clamp for {@link PAYMENT_PAGE_SIZE_DEFAULT} — 100 | `apps/web/lib/payments.ts` |
| `membershipNetPrice` | `packages/shared/src/api/membership-price.ts` | Exact nonnegative integer net period price; rejects unsafe, fractional and out-of-bound inputs with RangeError (OPEN-028, ADR-111) | Membership detail and payment default; Phase 6 money consumers |
| `PAISE_PER_RUPEE` | `packages/shared/src/config/constants.ts` | 100, named because `* 100` on money is exactly the literal AGENTS.md rule 4 exists for. **Paid a debt**: it lived as a local `PAISE.perRupee` in `app/(console)/memberships/[memberId]/page.tsx` with a comment saying it belonged in `packages/shared`, and now does | `paiseFromRupees`, `rupeesFromPaise` |
| `PAISE_DIGITS` | `packages/shared/src/config/constants.ts` | 2 — how many digits of paise a rupee amount may carry, and a third is refused rather than rounded (MNY-003). Read by both halves of the conversion so they cannot disagree about what `50` after a point means | `paiseFromRupees`, `rupeesFromPaise` |
| `IDEMPOTENCY_KEY_MAX_LENGTH` | `packages/shared/src/config/constants.ts` | 200 — long enough for a uuid and a prefix, short enough that the partial unique index on `(tenant_id, idempotency_key)` stays a b-tree entry | `paymentRequestSchema` |
| `MEMBER_PAGE_SIZE_DEFAULT` | `packages/shared/src/config/constants.ts` | Members returned when a caller asks for no particular page size — 50. Here rather than in `apps/web` because mobile pages the same roster in Phase 7 | `apps/web/lib/members.ts` |
| `MEMBER_PAGE_SIZE_MAX` | `packages/shared/src/config/constants.ts` | The clamp on a requested page size — 200. A **clamp and not a rejection**: a page size is a hint from a caller, and refusing an over-large one turns a screen that would have worked into an error for no gain (gate 26) | `apps/web/lib/members.ts` |
| `DAYS_PER_WEEK` | `packages/shared/src/config/constants.ts` | Seven. Named because `no-magic-numbers` catches it inline and because a weekly-goal window is a business rule, not arithmetic | `packages/shared/src/streaks/streaks.ts` |


## Enums

Postgres enums (ADR-021), created by the migration named below and generated into `packages/db/types/database.ts` — never hand-written as a TypeScript constant. `docs/data-model.md` § Enums holds every label set and its legal-transition graph, and its ownership table says which cluster creates which type: **each type is created exactly once**, and a second `create type` in a parallel migration is an apply failure on merge. All 24 v1 enums are registered below.

| Name | File | Purpose | Used by |
|---|---|---|---|
| `addon_kind` | `supabase/migrations/20260906115153_catalogue.sql` | The three add-on kinds sold to members: `pt_package`, `diet_plan`, `product` — the discriminator behind the kind-conditional requireds: a PT package must state `session_count`, a product must state `stock_quantity`, a diet plan needs neither (ADD-002) | `addon_products.kind`; the `addon_products_pt_package_has_session_count_chk` and `addon_products_product_has_stock_quantity_chk` check constraints; `addon_products_tenant_id_kind_is_active_idx` |
| `addon_order_status` | `supabase/migrations/20260906115153_catalogue.sql` | An add-on order's lifecycle: `pending`, `paid`, `active`, `completed`, `cancelled`, `refunded`. Past `pending`/`cancelled` the order must carry the payment that bought it unless its total is zero | `addon_orders.status`; the `addon_orders_paid_has_payment_chk` check constraint; `addon_orders_tenant_id_status_idx` |
| `app_role` | `supabase/migrations/20260906115131_tenancy.sql` | The one role vocabulary: `super_admin`, `platform_support`, `gym_owner`, `gym_manager`, `front_desk`, `trainer`, `member` (ADR-031 — replaces the retired `ROLES`/`Role`) | `staff.role` and `organization_settings.pause_approver_role` (both checked down to the four gym-side labels); the `app_role` JWT claim `app.is_platform()` reads; `platform_users.role` and `audit_log.actor_role` |
| `attendance_source` | `supabase/migrations/20260906115149_attendance.sql` | How a visit was recorded: `qr`, `front_desk` — the discriminator behind the assisted-check-in constraint that requires the acting staff member and a non-empty reason (ATT-005/006) | `attendance.source` |
| `consent_purpose` | `supabase/migrations/20260906115159_comms.sql` | The two independently-controlled consent categories: `marketing`, `service` — withdrawing one leaves the other unaffected (INT-002/DPD-002/DPD-003) | `consents.purpose`; the `consents_member_id_purpose_recorded_at_idx` current-state index |
| `contact_channel` | `supabase/migrations/20260906115156_retention.sql` | How a no-show case's follow-up contact was made: `call`, `whatsapp`, `in_person`, `sms` | `follow_ups.channel` |
| `follow_up_outcome` | `supabase/migrations/20260906115156_retention.sql` | The result of a follow-up contact attempt: `will_return`, `injured`, `travelling`, `timing_issue`, `unhappy`, `no_response`, `cancelled` — logged on every append-only contact-log entry (NSH-007) | `follow_ups.outcome` |
| `gym_preset` | `supabase/migrations/20260906115131_tenancy.sql` | The three sellable gym presets: `neighbourhood_gym`, `premium_studio`, `functional_box` | `organization_settings.preset` |
| `import_status` | `supabase/migrations/20260906115203_platform.sql` | A member CSV/Excel import run's lifecycle: `pending`, `processing`, `completed`, `failed` | `member_imports.status` |
| `lead_source` | `supabase/migrations/20260906115203_platform.sql` | How an enquiry reached the gym: `walk_in`, `referral`, `instagram`, `google`, `website`, `phone`, `other` | `leads.source` |
| `lead_stage` | `supabase/migrations/20260906115203_platform.sql` | An enquiry's pipeline stage: `new`, `contacted`, `trial_scheduled`, `trial_done`, `converted`, `lost`. A `converted` lead must name the member it became | `leads.stage`; the `leads_converted_has_member_chk` check constraint; `leads_tenant_id_stage_idx` |
| `mandate_status` | `supabase/migrations/20260906115146_membership_money.sql` | Razorpay subscription mandate states, mirroring the provider's: `created`, `authenticated`, `active`, `paused`, `halted`, `cancelled`, `completed`, `expired` | `razorpay_mandates.status` — schema reserved, unused until Phase 2 wires UPI Autopay |
| `member_status` | `supabase/migrations/20260906115131_tenancy.sql` | Member lifecycle: `active`, `paused`, `expired`, `cancelled`, `blocked` | `members.status` |
| `membership_status` | `supabase/migrations/20260906115146_membership_money.sql` | A membership period's lifecycle: `pending`, `active`, `frozen`, `expired`, `cancelled`. `pending`→`active` only on a verified payment (PAY-008); a renewal is a new row, never an edit | `memberships.status`; the `memberships_tenant_id_member_id_live_key` partial unique index, which reads `active` and `frozen` as "live" |
| `no_show_case_status` | `supabase/migrations/20260906115156_retention.sql` | A no-show case's lifecycle from opening to resolution: `open`, `contacted`, `follow_up_due`, `returned`, `closed`. At most one live case (`open`/`contacted`/`follow_up_due`) per member; a check-in auto-transitions a live case to `returned` then `closed`, preserving its follow-up history rather than deleting it (NSH-003/004/005) | `no_show_cases.status`; the `no_show_cases_tenant_id_member_id_open_key` partial unique index (live statuses); `no_show_cases_tenant_id_status_idx`; `no_show_cases_tenant_id_next_follow_up_at_due_idx` (`follow_up_due` rows) |
| `notification_channel` | `supabase/migrations/20260906115159_comms.sql` | How a notification is delivered: `push`, `whatsapp_link`, `in_app`, `sms`, `email`. Push is v1's primary channel (ADR-016) | `message_templates.channel`; `notifications.channel` |
| `notification_status` | `supabase/migrations/20260906115159_comms.sql` | A notification's delivery lifecycle: `scheduled`, `sent`, `delivered`, `failed`, `clicked`, `converted`, `opted_out` | `notifications.status`; the `notifications_tenant_id_status_scheduled_for_idx` send-queue index |
| `organization_status` | `supabase/migrations/20260906115131_tenancy.sql` | Gym account lifecycle: `pending_approval`, `trial`, `active`, `suspended`, `closed` | `organizations.status`; Phase 6 (platform console) |
| `payment_method` | `supabase/migrations/20260906115146_membership_money.sql` | How the money was collected: `razorpay`, `cash`, `upi`, `card`, `bank_transfer`. Anything but `razorpay` carries staff attribution (PAY-011) | `payments.method` |
| `payment_status` | `supabase/migrations/20260906115146_membership_money.sql` | Payment lifecycle, provider-owned (PAY-006/007): `created`, `pending`, `paid`, `failed`, `refunded`, `reversed` | `payments.status` |
| `pt_session_status` | `supabase/migrations/20260906115153_catalogue.sql` | A PT session's lifecycle: `scheduled`, `completed`, `cancelled`, `no_show`. Only `scheduled` and `completed` sessions hold a trainer's calendar slot — a cancelled or no-show session frees it (DQA-005) | `pt_sessions.status`; the `pt_sessions_trainer_overlap_excl` exclusion constraint's `where` predicate |
| `refund_kind` | `supabase/migrations/20260906115146_membership_money.sql` | Whether money went back as a `refund` or a `reversal` (PAY-010 — always its own row, never a mutation of the payment) | `refunds.kind` |
| `refund_status` | `supabase/migrations/20260906115146_membership_money.sql` | Refund lifecycle: `requested`, `processing`, `completed`, `failed` | `refunds.status` |
| `streak_rule_type` | `supabase/migrations/20260906115131_tenancy.sql` | The three configurable streak rules (STK-001): `visit_streak`, `weekly_goal`, `calendar_streak` | `organization_settings.streak_rule_type` |

## Database functions

Functions in the private `app` schema — not exposed by `supabase/config.toml`, so none is an RPC and none appears in `packages/db/types/database.ts`. Every one carries `set search_path = ''`, and every claim reader and role gate is `stable` + `security invoker`: they read the `request.jwt.claims` GUC and never a table, so definer rights would buy nothing and hand a caller elevated context for free. The `security definer` functions are the ones that must reach past the caller's privileges — the access-token hook (reads the three identity tables and `auth.users`) and the two trigger functions (write `audit_log`, which is read-only to `authenticated`, and delete from `auth.sessions`, which `authenticated` cannot touch at all).

Created once, by the migration named; a second copy is the duplication this table exists to prevent. Phase 1's "there is no third accessor" rule ended where it said it would: **the gate vocabulary is now closed at four** — `current_app_role()`, `is_staff()`, `is_gym_admin()`, `is_front_office()` — and a table needing a fifth distinct gate is a signal that the table is wrong, not that the vocabulary is too small (Phase 2 `design.md` § 8.2). Do not add `app.can_do_x()`; raise it instead.

| Name | File | Purpose | Used by |
|---|---|---|---|
| `app.audit_impersonation_session()` | `supabase/migrations/20260907184316_phase2_identity_triggers.sql` | `security definer` trigger function writing the INT-003 audit row when an impersonation session starts (`impersonation_session.started`) and when its `ended_at` is set (`impersonation_session.ended`). The database writes both, because `audit_log` is read-only to `authenticated` and a caller who must remember is a caller who will forget | The `impersonation_sessions_audit_start` and `impersonation_sessions_audit_end` triggers |
| `app.current_app_role()` | `supabase/migrations/20260907184313_phase2_identity_accessors_and_hook.sql` | Reads the `app_role` JWT claim **as `text`, casting nothing**; `null` when absent. An unrecognised role is compared as text like any other, so it grants nothing and raises nowhere — a cast to `public.app_role` would make one forged claim silent on the gate functions and `22P02` on the policies that compare a label directly | Both `<table>_platform_write` clauses (`= 'super_admin'`) on all thirty-six tables, and the `= 'gym_owner'` write gate on `organizations`, `staff` and `razorpay_accounts` |
| `app.current_impersonation_id()` | `supabase/migrations/20260907184313_phase2_identity_accessors_and_hook.sql` | Reads the `impersonation_session_id` JWT claim as a uuid; `null` on any non-impersonating token | Revocation audit attribution, preview write guard and own-session end |
| `app.current_member_id()` | `supabase/migrations/20260907184313_phase2_identity_accessors_and_hook.sql` | Reads the `member_id` JWT claim as a uuid; `null` on any non-member token | Every `<table>_member_select` policy — as `member_id = (select …)`, as `id = (select …)` on `members`, and as `(select …) is not null` on the tables a member reads gym-wide |
| `app.current_staff_id()` | `supabase/migrations/20260907184313_phase2_identity_accessors_and_hook.sql` | Reads the `staff_id` JWT claim as a uuid; `null` on any non-staff token | Phase 3+ (staff attribution on attendance, payments and follow-ups). No Phase 2 policy uses it — the role matrix gates on the role, not on the row |
| `app.current_tenant_id()` | `supabase/migrations/20260906115131_tenancy.sql` | Reads the `tenant_id` JWT claim as a uuid; `null` when the claim is absent or empty, raises `22P02` when it is present but malformed (ADR-032) | The `<table>_tenant_select`, `<table>_tenant_write` and `<table>_member_select` policy on every tenant-scoped table, always as `(select app.current_tenant_id())` |
| `app.custom_access_token_hook(event jsonb)` | `supabase/migrations/20260907184313_phase2_identity_accessors_and_hook.sql`, stale-claim cleanup completed by `supabase/migrations/20260915100002_phase6_identity_navigation.sql` | The Supabase access-token hook. `security definer`, in `app` and never `public` (a hook in `public` is a PostgREST RPC and a `schema-drift` diff). Before any cast, lookup, early return or exception path, it removes all five Gymloop claims from the copied claims object. It then resolves `platform_users` → `staff` → `members`, stops at the first table the user has a row in, and adds only the complete resolved identity shape. **Its entire body sits in `exception when others then return event` — a raising hook is a project-wide sign-in outage, not a failed request** | Supabase Auth as `supabase_auth_admin` (the only role holding `execute`), via `pg-functions://postgres/app/custom_access_token_hook` in `supabase/config.toml` |
| `app.enforce_check_in()` | `supabase/migrations/20260908071401_check_in_exactly_once.sql`, taught that a membership's DATES are what make it live by `supabase/migrations/20260910120000_check_in_needs_a_live_membership.sql` (ADR-084) | **`security invoker`**, **`volatile`** `before insert` trigger function on `attendance`: the scanned session's validity at the instant of the scan (ATT-001/002), the live membership behind a scan, the acting staff member taken from the `staff_id` claim and never from the request — a *supplied* `assisted_by_staff_id` that is not the acting one is refused with `GL016`, which defaulting-when-null did not do (ATT-005), and — the reason it exists — **exactly-once**. It takes `pg_advisory_xact_lock` on (tenant, member) *before* reading the gym's de-duplication window, so two concurrent check-ins serialise; the volatility is load-bearing, because a `stable` function would reuse the snapshot the INSERT took before it blocked and look straight past the row it has to find. **`invoker` is load-bearing in the same way**: a `before insert` trigger fires ahead of the `with check`, so every `authenticated` session reaches it, and definer rights (owner `postgres`, `rolbypassrls`) turned its refusal messages into an oracle handing a member `expires_at`, `revoked_at` and `checkin_dedupe_seconds` from tables the matrix denies them. Under invoker the lookups run under RLS and a denied probe gets the same `GL010` as one citing an id that never existed. The happy path survives because the matrix nests: `is_front_office()` ⊆ `is_staff()`, and `qr_sessions_tenant_select` is `is_front_office()` exactly. On the table and not in a caller: `attendance` grants `insert` to `authenticated`, so a rule living in a Route Handler has a supported way round it | The `attendance_enforce_check_in` trigger; `apps/web/app/api/check-in/route.ts`, which maps its five `GL0xx` SQLSTATEs onto the error envelope |
| `app.enforce_attendance_written_once()` | `supabase/migrations/20260908170000_attendance_is_written_once_and_a_decider_is_identified.sql` | **`security invoker`**, `before update` trigger function on `attendance`. Freezes the columns that carry what a visit MEANT — member, branch, membership, `checked_in_at`, source, QR session, the assisted pair, `client_event_id` — and leaves writable the ones recording what happened afterwards, `checked_out_at` above all: a check-out is a legitimate later write, which is why revoking the UPDATE grant was the wrong fix and three suites said so (ADR-068). **`tenant_id` is deliberately NOT frozen**: `attendance_tenant_write`'s `with check` already refuses moving a row to another gym, and a `before` trigger raising first would answer ahead of the policy (ADR-066). Reads nothing, so it has no RLS interaction to get wrong. Exempts sessions row security does not apply to (`row_security_active`), because `seed.sql` records attendance with `insert … on conflict do update` | The `attendance_written_once` trigger; `supabase/tests/16_checkin.sql` |
| `app.enforce_follow_up()` | `supabase/migrations/20260909110000_follow_ups.sql` | **`security invoker`**, `after insert` trigger on `follow_ups`. Stamps nothing and refuses four things: a contact not naming the acting staff member (`GL030`, the third appearance of one attribution rule after `GL016` and `GL026`, with the null actor named as its own clause because `null is distinct from null` is false); a follow-up on a closed case (`GL031`); a concurrent contact (`GL032`, via `pg_try_advisory_xact_lock` — `try` not `wait`, because a waiting lock serialises two callers and then lets both write); and a correction naming an entry on another case (`GL033`). Then derives the case's status from what happened, since a caller who could write `contacted` could mark a case contacted without contacting anybody | The `follow_ups_enforce` trigger; `supabase/tests/19_follow_ups.sql` |
| `app.stamp_payment()` | `supabase/migrations/20260910090000_manual_payment.sql`, given `paid_at` and the front-office guard by `supabase/migrations/20260910130000_a_payment_is_a_record.sql` (ADR-085) | Body last replaced by `supabase/migrations/20260910150000_a_period_is_what_the_money_bought.sql`, where the "a number typed onto an unpaid payment is nulled" rule lives — this cell named only the two earlier migrations. **`security invoker`**, `before insert or update` trigger on `payments`. Writes two things to the row and refuses nothing: the acting staff member into a null `recorded_by_staff_id` (filled, not refused — the id can only come from the token, so there is nothing to forge, and the *supplied-but-wrong* case is `GL034`'s in the `after` trigger), and the receipt number. **The receipt number takes no `row_security_active()` carve-out, and that asymmetry with `app.enforce_payment()` is the design**: a receipt is not a judgement about who is asking, and the Razorpay webhook runs as `service_role`, so carving it out would leave a hole in the gym's book for every online payment. What a trusted writer keeps is the right to supply its *own* number; inside row security a supplied one is overwritten. Allocation is `insert … on conflict … do update … returning next_number - 1` — one statement, so a counter another transaction has already advanced to 50 hands out 50, and nothing ever reads before it writes. `financial_year` is India's, derived from `coalesce(paid_at, created_at)` in the gym's own timezone (MNY-004) and never taken as an argument, so a caller cannot file a March payment into the next year by asking; the year is *in* the rendered number (`2026-27/000001`) because `payments_tenant_id_receipt_number_key` is unique per gym and a bare restarting ordinal collides every April. **Round two: `paid_at` is the system's to stamp** — three documents claimed the financial year was derived rather than passed in, and a critic filed payments into `2031-32` and `2019-20` by supplying it. Under row security it is the instant this write made the payment paid, and a trusted writer keeps its own because the webhook's is the provider's timestamp. A changed `paid_at` on an already-paid row is left ALONE rather than restored, so the freeze refuses it visibly — restoring it made a backdate a silent no-op, and a refusal a caller cannot see is worse than the change it prevented. **Allocating is front-office work**: without that guard a member's insert reached `document_counters` from a BEFORE trigger and was refused by THAT table's policy, the right SQLSTATE by accident, naming a table they have no business knowing exists (the half of ADR-082's repair that was missed) | The `payments_stamp` trigger; `supabase/tests/21_manual_payment.sql`, `supabase/tests-holdout/h21_manual_payment_holdout.sql` |
| `app.enforce_payment()` | `supabase/migrations/20260915100001_refund_retries_and_money_audit.sql` | Security-invoker AFTER INSERT/UPDATE invariant enforcement. Named row rules run GL038 (all-status id and received-money freeze), GL042 (member/membership identity), GL039 (legal transitions and arrival status), GL034 (staff attribution), GL035 (provider/manual separation). Native constraints retain their own timing. Only claim-based GL034/GL035 skip trusted contexts. Consolidates the former identity and arrival-status helpers; original clause semantics are preserved. | `payments_enforce` trigger |
| `app.enforce_preview_read_only()` | `supabase/migrations/20260915100002_phase6_identity_navigation.sql` | Private, empty-search-path, security-invoker row-trigger guard. When RLS is active and the JWT carries a non-null impersonation-session claim, every insert/update/delete on every public table granted authenticated DML raises `42501`. The only exception is the exact update that ends the caller's own matching impersonation session; the session immutability function checks the submitted row before normalization so retarget-and-end is refused too | The generated `<table>_preview_read_only` BEFORE trigger on every authenticated-writable public table |
| `app.extend_membership_on_payment()` | `supabase/migrations/20260910090000_manual_payment.sql`, taught to count money rather than payments by `supabase/migrations/20260910130000_a_payment_is_a_record.sql` (ADR-085) | **`security invoker`**, `after insert or update` trigger on `payments`: **`for each statement`, with transition tables** (`newly_paid`, and `previously` on update) — not per row, and that granularity IS the rule. Per row, ten payments in one `insert … select` each saw the final total, subtracted only their own amount, and each granted a full period: ₹1,000 bought **300 days** on a 30-day plan. It groups the statement's newly-paid rows per (membership, currency) and hands each group to `app.grant_periods()`, which owns the arithmetic — **this cell stated round three's superseded `floor(after) − floor(before)` formula as current for a whole round after ADR-087 replaced it**, two rows above the cell that had it right. The rule is now `floor(total / price) − periods_granted`, recorded rather than derived, because subtraction needs a "before" no `AFTER` trigger has. The total counts money that ARRIVED — `paid`, `refunded`, `reversed` — so a refund does not drop it below a line a later payment would re-cross. Dates move by `greatest(ends_on, today) + duration × periods` in the **gym's own day**, never `current_date` (ADR-039), and a `pending` membership becomes `active` when it is paid for — **only if `starts_on` will be non-null and nothing else of that member is live**, both conditions being `app.grant_periods()`'s and neither of which this cell used to mention. The `m.ends_on is not null` guard is also `grant_periods`' and not this function's. Renewing early keeps the days already bought; renewing late starts from today rather than handing back the lapsed weeks. No carve-out — this is what the product *does* when money arrives, so it must happen on the webhook's path too or the gateway takes payment and extends nothing. Exactly once: on UPDATE it returns early when the row was already `paid`, so a replay extends nothing, and on INSERT `payments_tenant_id_idempotency_key_key` means the second row never exists. **This trigger is why `POST /api/memberships` stopped writing `ends_on = starts_on + duration_days` (ADR-083)** — with both in place a desk that sold a membership and then took the money for it granted sixty days for one month's fee. `m.ends_on is not null` sits in that UPDATE's `where`, so it **is** a filter and this cell used to claim the opposite. Measured: a `pending` membership with `starts_on` set and `ends_on` null, paid in full, comes out `granted=0 ends=NULL` — the whole row is skipped, so not even `periods_granted` is recorded, and its money has bought a count it does not hold (the mirror of OPEN-026, and the reason ADR-089 rejected a consistency trigger). What is true is the REASON for the predicate: `greatest` ignores nulls in Postgres, so without it an open-ended membership would acquire a finite end date thirty days out, a downgrade dressed as a renewal. Skipping the row is the safe half of that trade and is not the whole answer; what a half-dated membership means is OPEN-026's | The `payments_extend_membership_insert` and `payments_extend_membership_update` triggers (statement-level, with transition tables — the row-level `payments_extend_membership` was dropped by `20260910150000`); `supabase/tests/21_manual_payment.sql`, `supabase/tests-holdout/h21_manual_payment_holdout.sql` |
| `app.stamp_refund()` | `supabase/migrations/20260910130000_a_payment_is_a_record.sql` | **`security invoker`**, `before insert` trigger on `refunds`. Fills `initiated_by_staff_id` from the verified claim and does nothing else — no refusal, no read of another table. **The split from `app.enforce_refund_total()` is the point**: a trigger that REFUSES must not run before the policy that would have refused first, and a trigger that only FILLS a column may. Putting both in one `before insert` made a cross-tenant refund answer `GL040` where it had answered `42501` — ADR-066's third instance in one phase, in the migration written to fix its second. **It was missing from this registry entirely** until a critic ran `grep -c stamp_refund docs/registry.md` and got 0 (hard rule 1) | The `refunds_stamp` trigger; `supabase/tests/22_payment_record.sql`, `supabase/tests-holdout/h22_payment_record_holdout.sql` |
| `app.grant_periods(tenant, membership, currency, added)` | `supabase/migrations/20260910150000_a_period_is_what_the_money_bought.sql`, made to measure a period by what the membership was SOLD at rather than by the plan's current length in `supabase/migrations/20260911090000_money_arriving_is_what_freezes_a_term.sql` (ADR-090); current body: `supabase/migrations/20260915100000_membership_periods_cost_the_net_price.sql` (ADR-111), scoring against `price_paise - discount_paise` | **First grants on fully dated memberships SET the span:** when `periods_granted = 0`, start at the later of the recorded start and gym-local today and end after the recorded sold duration times newly owed periods. Partial payments return before moving dates; subsequent renewals retain the existing extension arithmetic. Fully dateless and both half-dated paths preserve their prior behavior. **Reads `status` and grants nothing to a `cancelled` or `expired` membership** (ADR-096): it read price, currency, dates, count and length and never the status, so a payment naming a retired membership extended it — measured to the year 26667 — silently, because the gate reads status and refuses the member anyway. Reachable exactly on the repair path this phase prescribes, which is the moment a member holds a retired membership and a live one. `pending` and `frozen` are not retired: the first is activated by its own first payment, the second is a paused live membership. The payment stays recorded, receipted, attributed, refundable and bounded by `GL036`. | The extension arithmetic, in one place, called once per (membership, currency) group by the statement-level trigger. **Its fourth parameter `p_added` is now dead and deliberately kept**: ADR-087 stopped subtracting the statement's own contribution, which is the whole fix, so the argument is passed and ignored. Removing it would change the signature that `has_function_privilege` checks and the holdout's elevation allowlist names; it is scheduled for removal with the next signature change and recorded here so nobody reads it as live. Takes `for update` on the membership before summing — without it two transactions each recording half the price each see only their own row, each grant nothing, and **the deficit is permanent**, because every later payment measures against the same total. Refuses to add money in a currency the membership is not priced in (MNY-002), returns rather than dividing by a zero price, and moves a `pending` membership to `active` when it grants it dates — only if nothing else of that member is live, and only if `starts_on` will be non-null, because `memberships_dated_unless_pending_chk` forbids a half-dated `active` row and an ordinary payment aborted on it with an unmapped `23514`. **Grants what the money OWES minus `memberships.periods_granted`, never a subtraction from a running total** (ADR-087). `authenticated` holds EXECUTE on it and that is unavoidable rather than an oversight: the calling trigger is `security invoker`, so the session must hold it, and `security definer` would add the first unjustified elevated function in `app` since Phase 2. It grants nothing a front-office session lacks — `memberships_tenant_write` is `FOR ALL` on `is_front_office()`, and until round ten such a session could set `ends_on` directly — **which is no longer true, and this cell was the stated REASON for the grant rather than a description of it**. `GL045` (ADR-093) now refuses a hand-written date from any session at any privilege, so the justification has to stand on its own: the function reads and writes only rows the caller's own policies already reach, it is `security invoker`, and a direct top-level call grants nothing — `p_added` is dead and `v_periods = v_owed - v_granted` is `<= 0` whenever the money has already been scored, which a critic verified by forging `p_added = 999999999`. Fourteenth wrong Phase-5 registry cell and the third whose *reasoning* inverted rather than its description — and `app` is not PostgREST-exposed | `app.extend_membership_on_payment()` |
| `app.enforce_refund_total()` | `supabase/migrations/20260915100001_refund_retries_and_money_audit.sql` | Security-invoker AFTER INSERT/UPDATE rules, in order: GL041 freezes id/key/payment/amount/currency/kind and completed status; GL040 preserves attributed staff; GL036 requires received money and bounds all non-failed refunds under a payment row lock. The immutable and money rules apply to trusted writers too; only attribution uses the claim-context guard. | `refunds_enforce_total` trigger |
| `public.record_refund` | `supabase/migrations/20260915100001_refund_retries_and_money_audit.sql` | Security-invoker RPC: payment, integer paise, currency, kind, parsed reason and UUID key. Requires canonical gym-admin and real staff claims before lookup; derives tenant/actor. Inserts requested refund or proves exact tenant/key replay without UPDATE. Different facts raise GL048; unrelated failures propagate. Returns refund_id and replayed. | `POST /api/refunds` |
| `app.audit_money_change()` | `supabase/migrations/20260915100001_refund_retries_and_money_audit.sql` | Private security-definer AFTER INSERT/UPDATE trigger writer for payments/refunds. Appends exactly one payment.created/payment.updated/refund.created/refund.updated event with frozen-contract financial summaries and JWT actor; no-subject trusted writes have null actors. Empty search_path, no direct PUBLIC/anon/authenticated execution, no backfill. | `payments_money_audit`, `refunds_money_audit` |
| `app.payment_transition_allowed(from, to)` | `supabase/migrations/20260910130000_a_payment_is_a_record.sql` | The payment state machine, stated once as an explicit EDGE LIST so it can be read: `created` → pending/paid/failed, `pending` → paid/failed, `paid` → refunded/reversed, `failed` → created/pending, and `refunded`/`reversed` terminal. **`paid` is unreachable from anywhere it has already been**, which is what stops the extension and the receipt counter being farmed by pressing a button — a critic walked one payment `paid → created → paid` four times and landed 90 days on a 30-day plan, burning a receipt number each cycle. Written as edges rather than as "not one of these" because a label added to `payment_status` later should be unreachable until somebody decides where it belongs; a default-permit table grows holes by itself. `immutable`, so the planner may fold it | `app.enforce_payment()` |
| `app.enforce_counter_monotonic()` | `supabase/migrations/20260910130000_a_payment_is_a_record.sql` | **`security invoker`**, `before update or delete` trigger on `document_counters`: `next_number` may only rise — forward only, any increase — the row's identity may not change, and it may not be deleted (`GL037`). **This cell previously said "by exactly one" and quoted the justification of the alternative that was REJECTED**, which a critic measured against the live rule (`next_number + 1000` succeeds, and the spec's own scenario requires it to). Forward-only is right because only a DECREASE can issue a number twice, and because "a gap in a receipt book is explainable, a reused number is not" — this project's own receipts spec, two documents over. `document_counters_tenant_write` is `FOR ALL` on `is_front_office()` — the same predicate that lets a person take money lets them rewrite the book's numbering, and a critic reset it so the next payment collided with a number already issued. **No trusted-context carve-out** (ADR-082's line: an invariant about a book, not a judgement about a claim) and **no elevation** — making the allocator `definer` would add the first unjustified elevated function in `app` since Phase 2, which a holdout meta-test asserts against by name | The `document_counters_monotonic` trigger; `supabase/tests/22_payment_record.sql`, `supabase/tests-holdout/h22_payment_record_holdout.sql` |
| `app.membership_transition_allowed(from, to)` / `app.enforce_membership_status_transition()` | `supabase/migrations/20260913090000_a_membership_status_moves_only_where_it_can_go.sql` (ADR-098) | **That migration also re-emitted `app.enforce_membership_terms_frozen()`, which it did not need to and got wrong — see that function's cell and ADR-099; `20260913120000` undoes it.** The two functions in this cell were correct and are untouched by the restore. **`security invoker`**; the first `immutable` and pure, the second an `after update for each row` trigger on `memberships`. Which membership status may follow which (`GL047`): `pending → active|cancelled`, `active → frozen|cancelled|expired`, `frozen → active|cancelled|expired`, and **nothing out of `expired` or `cancelled`**. A status written back to itself is allowed — `app.grant_periods()` writes `status = m.status` on every grant, so a rule refusing a status WRITE rather than a CHANGE would break every renewal. An explicit edge list, not a deny-list, so a label added to the enum later is unreachable until somebody places it. Measured across all 25 ordered pairs: 13 permitted, 12 refused, every refusal `GL047`, with one membership per member so the live partial unique index can never be what answers. **Its own trigger, not folded into `memberships_terms_frozen`** — both seed files disable that one around their own statements, a window argued for dates and never for transitions, so the rule would have been silently off for the whole seed. Verified refusing a revival with the terms trigger disabled exactly as CI disables it. No trusted-context carve-out (ADR-082: the subject is which state may follow which, which a webhook does not lack); the seed needs none, reaching retired statuses at INSERT and writing the same value back on a re-run | The `memberships_transitions_after_terms` trigger — **renamed from `memberships_status_transitions` by `supabase/migrations/20260913180000_a_trigger_name_decided_which_rule_answered.sql` (ADR-101), because the name was deciding which rule answered**: Postgres fires same-timing row triggers in trigger-name order, `'s' < 't'`, so `GL047` preceded all five rules in `memberships_terms_frozen` and a statement that both re-pointed a membership and revived it answered `GL047` where the contract says `GL042`. The new name sorts after (`'tr' > 'te'`) and keeps the separate-trigger property the seed's disable window requires. **When a change adds a trigger, enumerate every other trigger on that table of the same timing AND the same level, and sort them by name** — a query, not a judgement. The `level` half is not optional: Postgres orders by name only within a timing and level, and every `after` row trigger fires before every `after` statement trigger whatever it is called (ADR-103). This cell kept the level-less version for a round after `docs/decisions.md` corrected it, which is the fourth time this phase that two files disagreed and the one hard rule 1 makes canonical was the stale one; `supabase/tests/22_payment_record.sql` sections 29-31, `supabase/tests-holdout/h22_payment_record_holdout.sql` section 25, `h16_checkin_holdout.sql` |
| `app.enforce_membership_terms_frozen()` | `supabase/migrations/20260910230000_the_freeze_was_keyed_on_a_column_the_desk_can_write.sql` (ADR-089), **re-gated onto money by `supabase/migrations/20260911090000_money_arriving_is_what_freezes_a_term.sql` (ADR-090)**, **authoritative body now `supabase/migrations/20260913150000_the_rule_that_answers_is_the_one_about_whose_membership_it_is.sql` (ADR-100), restored one migration earlier by `supabase/migrations/20260913120000_restore_the_terms_function_i_re_emitted_by_hand.sql` (ADR-099)** — `20260913090000` re-emitted this function to add something that went onto its own trigger and did not need it, dropped ~60 lines of the reasoning below (108 live lines against 149) and moved **`GL042` from before the `GL043` length check to after it**, so a statement violating both answered with the wrong harm. Restored byte-for-byte from `20260912210000` and verified by `diff`, not by reading. **The rule this leaves behind: a migration that re-emits an existing function body must `diff` the emitted body against its last authoritative source before it is committed, and a migration that does not need to re-emit one must not** | **`security invoker`**, `after insert or update` trigger on `memberships`, **seven rules** — and this cell said *six* while enumerating seven, in the commit whose stated purpose was fixing hard rule 1, which is the identical mistake it narrates twice in its own text below. Eighteenth wrong Phase-5 cell, fifth written alongside its own code. Six are invariants about the data and take no carve-out; **`GL046` alone is a claim rule**, and it is checked LAST, not first — the cell called it "the first of them" while stating three sentences later that it runs after `GL043` — and this cell said *three* while enumerating four, in the same commit that added the fourth. Thirteenth wrong Phase-5 registry cell; `registry-lint` gates TypeScript symbols, not prose, so nothing but a reader catches these — and the cell that stood here last round said `after update` and *two*, omitting the INSERT rule a blind author had just found, which made it the tenth wrong Phase-5 registry cell and the second written in the same commit as the code it described. **`GL044` on INSERT:** a membership is created with `periods_granted = 0`; a front-desk session could otherwise CREATE one carrying `5` and take the money, no UPDATE anywhere. **`GL044` on UPDATE:** `periods_granted` may change only at `pg_catalog.pg_trigger_depth() >= 2`, where `app.grant_periods()`' own write runs — every hand-written `update memberships` runs at 1, and depth cannot be forged from a top-level statement the way a session GUC could. **`GL042`:** `member_id` is frozen — a membership belongs to the member it was sold to. `GL042`'s own requirement is evaluated on the PAYMENT row, and this column was frozen by nothing: a critic moved a membership carrying a granted period to another member from a **front-desk** session in one statement, while the paid payment still named the original member and carried their receipt. Verbatim the harm that requirement names, by the least-privileged writer who can reach the table. No trusted-context carve-out (ADR-082: the subject is which member a membership was sold to, which a webhook has as much of as anybody). **A trap for whoever tests it**: the move must be attempted against a **different** member who holds nothing, with a statement that writes no status and leaves a row the table's constraints accept — otherwise something other than this rule answers and the check passes on an accident. `memberships_tenant_id_member_id_live_key` gives `23505` when the STORED tuple is `active`/`frozen` **and** the target holds another such row (both, not either); a null-dated `pending` row gives `23514` if a status is written without dates; re-pointing at the member who already owns it is permitted outright, since `GL042` fires on `is distinct from`. Which mechanism answers is **observed, not contracted** (ADR-105): the normative claim is only that `GL042` precedes `GL043`, `GL044`, `GL045`, `GL046` and `GL047`, which are the five codes this project controls. **`GL046`:** changing a membership's `price_paise`, `currency`, `plan_id`, `discount_paise` or **`coupon_id`** — or CREATING one whose price or currency differs from its plan's, or which carries a discount or a coupon — requires `app.is_gym_admin()`. **This cell omitted `coupon_id` in the commit that added it**, which is the seventeenth wrong Phase-5 cell and the fourth written alongside its own code. The coupon names WHY a member owes less and the discount is how much less; a front desk could attach a 10%-off coupon while being refused the discount it implies, leaving rows reading "coupon applied, discount zero". `ends_on = duration_days * floor(money / price_paise)`; round nine made the length underivable and **the price stayed freely typed**, so a front desk set a ₹1,500 membership to ₹150, took the ordinary ₹1,500 and got **300 days**, both audit invariants intact and, after round ten's freezes, unrepairable. Both blind authors then measured the same thing through CREATION, in one statement fewer. The control is who, not what — `refunds_tenant_write` is already `is_gym_admin()` on the same reasoning, "front_desk may record money but not refund it". A gym admin may still comp to a paisa; the price stays on the row as evidence. **This rule alone carries a trusted-context carve-out** (`row_security_active`), on ADR-082's general form: its subject is which staff role you are, and a webhook, a migration and the seed have none — the same line `app.enforce_payment()` draws. The other five are invariants about the data and are deliberately role-agnostic. A bare `super_admin` is refused; an impersonating token carries `app_role = gym_owner` and is allowed as one. **Checked AFTER `GL043`**, because an absolute beats a permission and answering the permission would imply an admin could do it. **`GL042` is checked FIRST of the four absolutes** — before `GL044`, `GL045` and `GL043`, and that order is asserted rather than incidental: a statement re-pointing a membership and re-lengthening it violates both, the caller acts on whichever answers, and `GL043`'s "change the plan instead" is advice about a plan when the true answer is that the membership belongs to somebody else. It stopped being true for one migration and every assertion in both suites stayed green, because none named a statement that violates two rules at once (ADR-099). **And before that it had never been decided at all**: ownership was checked third because `GL044` and `GL045` were typed in by earlier migrations and `GL042` by a later one, which a holdout author measured by reading the requirement's unqualified sentence as written (ADR-100). The order among `GL044`, `GL045` and `GL043` is deliberately still undecided — no scenario needs it. **A migration that re-emits this function must move clauses, never retype them, and assert the multiset of its non-blank lines against the previous definition.** Consequence, recorded rather than discovered: a mixed multi-row statement from a non-admin is refused with whichever rule its first row reaches — Postgres does not order a statement's row triggers — so an assertion mixing frozen and unfrozen rows must pin the refusal and the values, not the code. **`GL045`:** `starts_on` and `ends_on` may change only at `pg_trigger_depth() >= 2`, where `app.grant_periods()`' own write runs — the dates are what the money bought (ADR-093). Nine rounds had governed the price, the length and the count while the arithmetic's OUTPUT stayed writable by hand: `update memberships set ends_on = starts_on + 3650` was ALLOWED from an ordinary front-desk session, and `starts_on` pulled backwards is a day at the turnstile too, since `app.enforce_check_in()` judges liveness from these dates (ADR-084). `is distinct from`, so a date written back unchanged is allowed. **Filling a null date is refused like any other change** — briefly exempted to keep an older holdout assertion green, then reverted, because both round-ten authors had independently asserted the refusal and the requirement accepts that cost; an exemption carved to save one assertion is a rule acquiring an edge nobody decided. Four malformed shapes are unrepairable in place as a result, all named in the requirement. **`GL043` on the length:** `duration_days` may change only as part of a plan change, **at any time and whatever the money** — a critic typed `3650` onto a 30-day membership sold at ₹1,500, took the ordinary ₹1,500, and got **3,650 days**, from one `.update()` in a front-desk browser session. It is the only exploit this phase has found that leaves **nothing to audit**: one receipt, one period granted, `ends_on` exactly one recorded period, `periods_granted = floor(money/price)` holding, and a length differing from the plan's being legitimate after a plan is re-lengthened. **`GL043` on the terms:** once **any money has arrived** — `paid`, `refunded` or `reversed`, in ANY currency — `price_paise`, `discount_paise`, `currency` and `plan_id` are frozen. Current body: `20260915100000_membership_periods_cost_the_net_price.sql` (ADR-111). The gate was `periods_granted > 0` for one round, which left a **part-paid** membership wide open: measured on a real demo row with ₹10,800 against a ₹12,000 price, cutting the price and paying **one paisa** moved `ends_on` **ten years**. Any currency, not just the membership's own, because re-denominating is exactly what makes foreign money start scoring; refunded money still counts, because thawing on refund would sell the re-pricing door for the price of a refund. Measured: all four terms `GL043` on that row; a membership with no money stays fully editable; renewals unaffected. No carve-out for trusted callers (ADR-082's general form). `after`, not `before`, so no rule answers ahead of the policy (ADR-066) | The `memberships_terms_frozen` trigger; `supabase/tests/22_payment_record.sql`, `supabase/tests-holdout/h22_payment_record_holdout.sql` |
| `app.stamp_membership()` | `supabase/migrations/20260911090000_money_arriving_is_what_freezes_a_term.sql` (ADR-090), rewritten by `supabase/migrations/20260911140000_a_length_is_derived_not_typed.sql` (ADR-092) | **`security invoker`**, **`before insert or update`** trigger on `memberships` — and the cell that stood here said `before insert` and "fills `duration_days` … **when the caller has not supplied one**", which was the behaviour of a draft the same migration's comments describe as REJECTED. It documented the rejected version as shipped, omitted the UPDATE branch entirely, and a critic then found the round's worst defect living in the branch this cell did not mention. Twelfth wrong Phase-5 registry cell, third written in the same commit as its code, second stating the opposite of measurement. **On INSERT:** `duration_days` is taken from the plan whatever the caller named — a caller-supplied `3650` on a 30-day plan lands `30`, measured. The price is deliberately NOT forced: a desk legitimately sells at a discount, and `POST /api/memberships` sets it. **On UPDATE:** the length follows the plan and only the plan. When `plan_id` changes, `duration_days` is re-derived from the new plan, and `price_paise` **and `currency` together** are re-derived too unless the same statement names one — leaving the price behind is the 2,920-day defect, a membership pointing at an Annual plan while priced as a Monthly turning one year's money into eight, and leaving the currency behind would score the new number against the old denomination (MNY-002). A length written any other way is **left exactly as the caller wrote it** so that `app.enforce_membership_terms_frozen()` can REFUSE it — putting the old value back here would make the write vanish silently, which is what both suites caught in round eight. A plan that cannot be read derives nothing and the composite foreign key answers the row (ADR-066) | The `memberships_stamp` trigger; `memberships.duration_days`; `app.grant_periods()`, which reads the recorded length instead of joining `plans` |
| `app.close_no_show_case_on_return()` | `supabase/migrations/20260909090000_no_show_scan.sql` | **`security invoker`**, `after insert` trigger on `attendance` (NSH-005): a returning member closes their own open case, preserving its follow-up history — the recovery is the product's only evidence the loop worked, and a delete destroys the row that proves it. AFTER by ADR-072: it modifies no attendance column, so running before would close a case for a visit row security was about to refuse | The `attendance_close_no_show_case` trigger; `supabase/tests-holdout/h18_no_show_scan_holdout.sql` |
| `app.run_no_show_scan(p_tenant_id uuid, p_today date)` | `supabase/migrations/20260909090000_no_show_scan.sql` | **`security invoker`**, and that is load-bearing: `p_tenant_id` is a tenant id arriving from a caller, and under invoker a staff session passing another gym's id reads zero rows through that gym's policies and opens nothing. Opens one case per member absent longer than the gym's own `no_show_threshold_days`, measured in the gym's own timezone, excluding members with an approved pause covering the day — **derived from `membership_pauses`, never from a status column** (ADR-064). A gym with no settings row is scanned and no case opens, without raising, because under invoker that is indistinguishable from a gym you may not read. Returns rows that landed, so a second run reports 0 | `public.run_no_show_scan_all()`; `supabase/tests/18_no_show_scan.sql` |
| `public.run_no_show_scan_all()` | `supabase/migrations/20260909150000_run_no_show_scan_all.sql` | Runs the scan for every gym, one row per gym. **The loop lives here because "which gyms get scanned" is a rule** — a loop that silently skips one produces the exact failure this capability exists to prevent — and in SQL it is reachable by pgTAP. Returns per-gym counts rather than a total, since a total cannot tell "forty gyms, nothing to do" from "one scanned, thirty-nine skipped". **`execute` revoked from `public`, `anon` AND `authenticated` by name** — `revoke ... from public` alone did NOT work and this entry previously said it had (ADR-074). Supabase grants EXECUTE to `anon` and `authenticated` explicitly, so revoking PUBLIC leaves both standing: a member, a trainer and a front desk could all run the nightly job by hand until this was fixed. Verify with `has_function_privilege`, never with the migration's own prose | `cron.job` `no-show-scan-nightly` (the schedule); `supabase/functions/no-show-scan/index.ts` (an operator's manual re-run) |
| `no-show-scan-nightly` (`cron.job`) | `supabase/migrations/20260909170000_the_critic_was_right_four_times.sql` | The `pg_cron` job that actually runs the retention loop — `0 1 * * *` (06:30 IST), `select public.run_no_show_scan_all()`, as `postgres`, which holds `BYPASSRLS` and is therefore what lets a `security invoker` scan see every gym. **Closes OPEN-007's operational half**: the scan was correct, reachable and never started for four commits. Chosen over a scheduled Edge Function because it needs no public endpoint — ADR-074 is what one mis-written grant on such an endpoint cost — no deploy step and no secret, so there is no state in which the migration is applied and the job is not running (ADR-077) | The retention loop; nothing calls it, it calls |
| `public.red_list_cases` | `supabase/migrations/20260909130000_red_list_view.sql` | The red list, as a view: open cases with `days_absent` computed at read time and the latest follow-up attached. **`security_invoker = true` is the whole risk surface** — a view runs with its owner's rights by default, the owner holds `BYPASSRLS`, and omitting the option would hand every gym's cases to every caller with no policy in the path. `04_contract_meta` filters `relkind in ('r','p')`, so views are invisible to every other meta-assertion in this repo; `20_red_list.sql` pins the option for that reason | `apps/web/lib/red-list.ts` |
| `app.enforce_pause_decision()` | `supabase/migrations/20260908090000_phase3_approver_boundary_and_missing_settings.sql`, widened to `before insert or update` and taught that a decision is final by `supabase/migrations/20260908150000_pause_decision_on_insert_and_stays_decided.sql` | **`security invoker`**, **`volatile`** `before insert or update` trigger function on `membership_pauses`. Governs one transition only — recording an approval — and refuses it unless the approver is the acting staff member (`GL020`), is not the person who requested the pause (`GL021`), and holds the gym's configured `organization_settings.pause_approver_role` (`GL023`; `GL022` when the gym has no settings row). **`GL026` names the null actor as its own clause** and not as one side of a comparison: `null is distinct from null` is false, so the comparison alone let a session with no `staff_id` claim create a pause with no requester at all — and `GL021` then compared `<uuid> is not distinct from null`, false for the same reason, so the two-person rule was satisfied against nobody (ADR-071). `membership_pauses_approver_pairs_with_approval_chk` — a CHECK and deliberately not a trigger branch — holds `(approved_at is null) = (approved_by_staff_id is null)`, because every rule governing the approver sits below a `return` taken when there is no approval, and because an invariant must reach the writers this function exempts. A pause already approved or rejected is settled: neither the decision columns nor what the decision covers — `membership_id`, `starts_on`, `ends_on`, `reason` — may change (`GL024`), because moving what an authorisation covers forges it as surely as rewriting who gave it — falling out of a branch in a `before` trigger permits the write, so "already decided" has to refuse rather than return. **`insert` is in the event list and that is load-bearing**: the write policy is `is_front_office()`, so a pause could otherwise be born approved in one statement and meet no rule (ADR-067). `invoker` costs nothing here — every role that may write a pause already reads `organization_settings` and `staff` under `is_staff()`. The requester is immutable (`GL026`) and an insert may not arrive already approved (`GL027`), because a rule of the form "A must differ from B" is a control only where one side is a recorded fact — on an insert neither is (ADR-068). The trusted-context carve-out is `row_security_active('public.membership_pauses')` — "does any policy apply to this session", false for the owner and for `BYPASSRLS` roles — and NOT a null `staff_id`, which also matched an impersonating token holding `gym_owner` with no staff identity; such a session met no rule at all and could rewrite a decision (`GL025` refuses it now) | The `membership_pauses_enforce_decision` trigger; `supabase/tests/17_pause_decision.sql`, `supabase/tests-holdout/h17_pause_decision_holdout.sql` |
| `app.impersonation_session_immutable()` | `supabase/migrations/20260908035810_phase2_impersonation_session_immutable.sql`, `ended_at` clamp added by `supabase/migrations/20260908052129_phase2_hook_lookup_indexes_and_ended_at_clamp.sql`, preview-input refusal added by `supabase/migrations/20260915100002_phase6_identity_navigation.sql` | `security invoker` trigger function stating one rule: **a session is written once and then only ended.** On insert it clamps the anchor forward so a session cannot start in the future. On update it takes the stored row whole (`new := old`), puts the submitted `ended_at` back only while it was null, and clamps that end forward. When the caller is in preview, it first inspects the raw submitted row and permits only the exact end of that caller's own matching session; an update that also attempts to retarget or rewrite another field raises `42501` instead of being silently normalized | The `impersonation_sessions_immutable` trigger. `before`, so it runs ahead of both `after` audit triggers and the table-wide preview guard regardless of name |
| `app.impersonation_is_live(p_ended_at timestamptz, p_expires_at timestamptz)` | `supabase/migrations/20260907184313_phase2_identity_accessors_and_hook.sql` | The **one** place the liveness expression `ended_at is null and expires_at > now()` exists. Never re-spell it inline | `app.custom_access_token_hook()`. Not the partial unique index, which can only carry `ended_at is null` — `now()` is not immutable |
| `app.is_front_office()` | `supabase/migrations/20260907184313_phase2_identity_accessors_and_hook.sql` | True when the `app_role` claim is `gym_owner`, `gym_manager` or `front_desk` — i.e. staff who are not trainers. Compares the claim as text, so a role outside the enum is false rather than an exception | The `<t>_tenant_select` and/or `<t>_tenant_write` gate on the money, contact and QR tables (`payments`, `invoices`, `coupons`, `consents`, `qr_sessions`, `leads`, …) — Phase 2 `design.md` § 8.3 |
| `app.is_gym_admin()` | `supabase/migrations/20260907184313_phase2_identity_accessors_and_hook.sql` | True when the `app_role` claim is `gym_owner` or `gym_manager` | The `<t>_tenant_write` gate on the gym's configuration tables and the `<t>_tenant_select` gate on the five tables that get no write policy at all (`audit_log`, `webhook_events`, `messaging_wallets`, `messaging_wallet_ledger`, `impersonation_sessions`), and **the gate inside `app.enforce_membership_terms_frozen()`** — `GL046`, the one claim rule among that trigger's six, deciding who may change what a member owes (ADR-094). Added there in the round that rewrote the neighbouring cell and unrecorded here until a critic checked |
| `app.is_platform()` | `supabase/migrations/20260906115131_tenancy.sql` | True when the `app_role` JWT claim is `super_admin` or `platform_support`; false when the claim is absent (ADR-032) | The `using` of every `<table>_platform_select` policy, always as `(select app.is_platform())`. Phase 2 split the platform policy in two and gated `<table>_platform_write` on `super_admin` — this is what lets support read everywhere and write nowhere |
| `app.is_staff()` | `supabase/migrations/20260907184313_phase2_identity_accessors_and_hook.sql` | True when the `app_role` claim is one of the four gym-side staff roles (`gym_owner`, `gym_manager`, `front_desk`, `trainer`) | The default `<t>_tenant_select` gate on the gym-side tables, and the `<t>_tenant_write` gate on `no_show_cases`, `follow_ups` and `pt_sessions` |
| `app.revoke_sessions_on_identity_change()` | `supabase/migrations/20260907184316_phase2_identity_triggers.sql` | `security definer` trigger function: deletes the user's `auth.sessions` rows when an identity is deactivated or its role changes, and writes both audit rows — the INT-003 role-change row and the deactivation row § 7 adds deliberately. Deactivation on `members` is `status` becoming `cancelled`/`blocked` or `erased_at` being set (DPD-006) — that table has no `is_active` and no `role`, so it writes no role-change row | The `platform_users_identity_change`, `staff_identity_change` and `members_identity_change` triggers |
| `app.touch_updated_at()` | `supabase/migrations/20260906115131_tenancy.sql` | Sets `new.updated_at := now()` on update | One `<table>_touch_updated_at` trigger per table that has an `updated_at` column |

## Types

| Name | File | Purpose | Used by |
|---|---|---|---|
| `ApiFailStatus` | `apps/web/lib/api.ts` | The failure statuses a Route Handler may answer with, named (`'forbidden'`, `'conflict'`, ...) rather than numbered — a bare `403` at a call site is exactly what `no-magic-numbers` exists to move somewhere named | `apps/web/app/api/check-in/route.ts`, `apps/web/app/api/gate-code/route.ts` |
| `FollowUpRequest` | `packages/shared/src/api/follow-ups.ts` | `z.infer` of `followUpRequestSchema` | `apps/web/app/api/follow-ups/route.ts` |
| `PaymentRequest` | `packages/shared/src/api/payments.ts` | `z.infer` of `paymentRequestSchema` | `apps/web/app/api/payments/route.ts` |
| `RefundRequest` | `packages/shared/src/api/refunds.ts` | `z.infer` of `refundRequestSchema` | `apps/web/app/api/refunds/route.ts` |
| `MembershipCreate` | `packages/shared/src/api/memberships.ts` | `z.infer` of `membershipCreateSchema` | `apps/web/app/api/memberships/route.ts` |
| `PauseDecision` | `packages/shared/src/api/memberships.ts` | `z.infer` of `pauseDecisionSchema` | `apps/web/app/api/memberships/pauses/route.ts` |
| `PauseRequest` | `packages/shared/src/api/memberships.ts` | `z.infer` of `pauseRequestSchema` | `apps/web/app/api/memberships/pauses/route.ts` |
| `CheckInRequest` | `packages/shared/src/api/check-in.ts` | `z.infer` of `checkInRequestSchema` — the check-in request body as a type | Web and (Phase 7) mobile callers of `POST /api/check-in` |
| `PlanTier` | `packages/shared/src/config/constants.ts` | `keyof typeof PLAN_TIER_PRICES_PAISE` | Phase 6 (platform billing) |
| `RenewalReminderWindowId` | `packages/shared/src/config/constants.ts` | `'expiry_minus_14' \| … \| 'expiry_plus_3'` — the window id, so reminder state can be keyed by name rather than by a signed offset | Phase 4 (reminders), Phase 5 (renewals) |
| `StaffSession` | `apps/web/lib/api.ts` | A complete verified real-staff caller: Supabase client plus `userId`, `tenantId`, `staffId` and generated `StaffRole`, all read from signature-verified JWT claims | Console Route Handlers |
| `SupportedLocale` | `packages/shared/src/config/constants.ts` | `(typeof SUPPORTED_LOCALES)[number]` | i18n setup |
| `LocalDate` | `packages/shared/src/streaks/streaks.ts` | A gym-local calendar day as `YYYY-MM-DD`. A distinct type because the whole streak module's correctness rests on never mixing an instant with a day — a visit at 00:30 IST is *today* in Asia/Kolkata and *yesterday* in UTC | Streak callers |
| `StreakPause` | `packages/shared/src/streaks/streaks.ts` | A raw `membership_pauses` row shape. Takes the row unfiltered, so the "approved" test (`approved_at` set, `rejected_at` null) happens inside the module and no caller can forget the qualifier (STK-002) | Streak callers |
| `StreakInput` | `packages/shared/src/streaks/streaks.ts` | Everything `visitStreak` needs, supplied by the caller — no I/O in the module | `visitStreak` |
| `WeeklyGoalStreakInput` | `packages/shared/src/streaks/streaks.ts` | As above, plus the weekly goal and week start day | `weeklyGoalStreak` |
| `CalendarStreakInput` | `packages/shared/src/streaks/streaks.ts` | As above, plus an explicit window. The window is a parameter because **no challenges table exists** — see OPEN-017 | `calendarStreak` |
| `StreakResult` | `packages/shared/src/streaks/streaks.ts` | Current streak, longest streak, missed days, and a `unit` of `'day'` or `'week'` so a screen cannot label a weekly-goal streak in days | Streak callers |


## Utilities

| Name | File | Purpose | Used by |
|---|---|---|---|
| `apiFail` | `apps/web/lib/api.ts` | The typed failure envelope `{ ok: false, error: { code, message } }`, taking a named status rather than a number. `code` is stable and machine-readable; `message` is what somebody at a front desk reads, so it says what to do next | `apps/web/app/api/check-in/route.ts`, `apps/web/app/api/gate-code/route.ts` |
| `apiOk` | `apps/web/lib/api.ts` | The success envelope `{ ok: true, data }` | `apps/web/app/api/check-in/route.ts`, `apps/web/app/api/gate-code/route.ts` |
| `assertEnv` | `packages/shared/src/config/env.ts` | Fail-fast env validation for server entrypoints (boot-time, not lazy) | Server app/Edge Function entrypoints |
| `followUpRequestSchema` | `packages/shared/src/api/follow-ups.ts` | zod schema for logging what happened when somebody rang a member: `caseId`, `channel`, `outcome`, optional `notes`, `nextAction`, `nextFollowUpAt` and `correctsFollowUpId`. **No `staffId`** — it is stamped from the verified claim and `GL030` refuses a row naming anybody else, the third column in this product to work that way. **No `status`** — the case's status is derived from what happened, and a caller who could set `contacted` could mark a case contacted without contacting anybody | `apps/web/app/api/follow-ups/route.ts` |
| `paymentRequestSchema` | `packages/shared/src/api/payments.ts` | zod schema for money taken at the desk: `memberId`, optional `membershipId`, `amountRupees` as typed, `method`, optional `notes` and `idempotencyKey`. **No `staffId`** — stamped from the verified claim, `GL034` refuses a row naming anybody else, the fourth column in this product to work that way. **No `receiptNumber`** — the gym's book is numbered atomically by `document_counters`, and a caller that could choose its own number could put two receipts on one. **No `provider`/`providerOrderId`/`providerPaymentId`** — a desk row carrying them would be cash wearing an online payment's evidence (`GL035`) | `apps/web/app/api/payments/route.ts` |
| `refundRequestSchema` | `packages/shared/src/api/refunds.ts` | zod schema for sending money back: `paymentId`, required UUID `idempotencyKey` (the exact retry identity), `amountRupees` as typed, `kind` (`refund` or `reversal`, checked against the generated enum at the edge), and a required non-empty `reason`. **No `initiatedByStaffId`** — stamped from the verified claim, `GL040` refuses a row naming anybody else, and this is the fifth column in the product to work that way and **the first on money going OUT**. **No `status`** — a caller who could write `completed` could mark money returned without returning it. **No `notes`**, because `refunds` has no such column and a schema field with nowhere to go is one a caller can send and the system silently drops | `apps/web/app/api/refunds/route.ts` |
| `paiseFromRupees` | `packages/shared/src/api/payments.ts` | `"1500.50"` → `150050`, by string arithmetic — never `Math.round(Number(r) * 100)`, which is the floating-point bug MNY-001 forbids (`Number('1050.60') * 100` is `105059.99999999999`). Returns `null` for anything outside `^\d{1,9}(\.\d{1,2})?$`, so more than two decimal places is **refused, not rounded**: this product's documented input rounding rule (MNY-003), chosen because Postgres silently rounds a `numeric` literal into a `bigint` column and a receipt for ₹1050.60 recording ₹1051 is wrong by an amount nobody finds | `apps/web/app/api/payments/route.ts` |
| `rupeesFromPaise` | `packages/shared/src/api/payments.ts` | Exact two-decimal display from safe integer numbers or canonical signed decimal strings. BigInt arithmetic preserves values beyond JS safe integer range. Invalid strings raise TypeError; unsafe/noninteger numbers raise RangeError. No rounding, and paiseFromRupees remains unchanged | Receipts, payments ledger, memberships and Phase 6 money read surfaces |
| `isoDaySchema` | `packages/shared/src/api/memberships.ts` | A `YYYY-MM-DD` that is actually a day. The regex fixes the shape; **round-tripping the parsed date back against the input** rejects what passes the shape and is still not a date — `2026-02-31` parses as 3 March, and `2026-13-01` makes an Invalid Date whose `toISOString()` throws rather than returning something that compares unequal | `membershipCreateSchema`, `pauseRequestSchema` |
| `membershipCreateSchema` | `packages/shared/src/api/memberships.ts` | zod schema for selling a plan: `memberId`, `planId`, `startsOn`. **No price field and no tenant field** — the price is copied from the plan row (MNY-001) and the tenant comes from the verified claim, so neither is a validation rule that could be forgotten | `apps/web/app/api/memberships/route.ts` |
| `pauseDecisionSchema` | `packages/shared/src/api/memberships.ts` | zod schema for deciding a freeze: `memberId`, `pauseId`, `decision` (`approve` \| `reject`). No approver field: the approver is the acting staff member and `app.enforce_pause_decision()` refuses a row saying otherwise | `apps/web/app/api/memberships/pauses/route.ts` |
| `pauseRequestSchema` | `packages/shared/src/api/memberships.ts` | zod schema for asking for a freeze: `memberId`, `membershipId`, `startsOn`, `endsOn`, `reason` (trimmed before `min(1)`, so three spaces are refused here as well as by `membership_pauses_reason_chk`), refined so a freeze cannot end before it starts. No `requestedBy`: the requester is stamped from the claim and the table refuses to let it change afterwards (ADR-068) | `apps/web/app/api/memberships/pauses/route.ts` |
| `checkInRequestSchema` | `packages/shared/src/api/check-in.ts` | zod schema for the check-in request body: `memberId`, optional `token` (a scan), optional `reason` (a visit recorded at the desk), optional `clientEventId` (the idempotency key). It lives in `shared` because mobile posts the same shape in Phase 7 — and because **it has no tenant field and no acting-staff field**, both of which come from the verified token instead | `apps/web/app/api/check-in/route.ts` |
| `clientEnv` | `packages/shared/src/config/env.ts` | Validated `NEXT_PUBLIC_*` vars — **server-side call only**, see file header comment on the Next.js client-bundling constraint | Server components/Route Handlers that pass public config to client components |
| `createServerSupabase` | `apps/web/lib/supabase/server.ts` | The request-scoped `supabase-js` client carrying the caller's own session, built on `@supabase/ssr`'s `createServerClient` with the current `getAll`/`setAll` cookie pair. **The only Supabase client in the app** — every read is a Server Component render and every write a Server Action, so no browser client (and so no `"use client"` read of `process.env`) is needed. Created fresh per request; never cache it in a module variable | `apps/web/app/sign-in/page.tsx`, `apps/web/app/(console)/layout.tsx`, `apps/web/app/(console)/console/page.tsx`, `apps/web/app/(console)/console/check-in/page.tsx`, `apps/web/app/not-linked/page.tsx`, `apps/web/lib/auth-actions.ts`, `apps/web/lib/api.ts` |
| `env` | `packages/shared/src/config/env.ts` | Combined client+server validated env, lazily parsed and cached | Server-side code needing both |
| `hashGateCode` | `apps/web/lib/gate-code.ts` | SHA-256 of a normalised gate code — the only way a scanned or typed code is matched to a `qr_sessions` row, ATT-003 having left nowhere to store the code itself. Normalises (strips spaces, uppercases) so a typed code finds the session a scanned one would | `apps/web/app/api/check-in/route.ts`, `apps/web/app/api/gate-code/route.ts` |
| `loadMemberSearch` | `apps/web/lib/members.ts` | The member list both console screens read, optionally narrowed by a phone substring so a front desk can type the last four digits of a number read aloud. Carries **no** `tenant_id` predicate on purpose — `members_tenant_select` does the filtering, and an application-side copy would return the right rows even with the policy broken, hiding the defect the pgTAP suite exists to catch. Takes the page's `searchParams` promise so the trimming, the empty search and the error shape are decided once too | `apps/web/app/(console)/console/page.tsx`, `apps/web/app/(console)/console/check-in/page.tsx` |
| `newGateCode` | `apps/web/lib/gate-code.ts` | Mints a gate code from `GATE_CODE_BYTES` of `node:crypto` randomness. Beside `hashGateCode` because minting and matching have to agree, and outside `packages/shared` because that package forbids Node builtins | `apps/web/app/api/gate-code/route.ts` |
| `serverEnv` | `packages/shared/src/config/env.ts` | Validated server-only secrets | Route Handlers, Edge Functions |
| `signIn` | `apps/web/lib/auth-actions.ts` | Server Action: `signInWithPassword` from a native form, refreshes signature-verified claims, classifies the complete identity and redirects to its one canonical home. On failure it returns the single non-enumerating sign-in message | `apps/web/app/sign-in/page.tsx` |
| `signOut` | `apps/web/lib/auth-actions.ts` | Server Action: clears the local session and redirects to sign-in | `AccountFrame`, `apps/web/app/not-linked/page.tsx` |
| `quoteFilterValue` | `apps/web/lib/keyset.ts` | Makes a value opaque to PostgREST's filter grammar, where `,` `.` `(` `)` are syntax — an unquoted value is not a value, it is a clause. A blind critic hand-crafted a cursor against the live API to prove it | `apps/web/lib/members.ts`, `apps/web/lib/red-list.ts` |
| `encodeCursor` | `apps/web/lib/keyset.ts` | Base64 of the sort key, opaque so nobody depends on the shape — **and that is all it buys. Base64 is not a signature** | `apps/web/lib/members.ts`, `apps/web/lib/red-list.ts` |
| `decodeCursor` | `apps/web/lib/keyset.ts` | Decodes and then hands the result to a guard that must prove it is what it claims; anything unusable is null, and every caller answers null with the first page rather than an error or a Postgres message on the screen | `apps/web/lib/members.ts`, `apps/web/lib/red-list.ts` |
| `isInt4` | `apps/web/lib/keyset.ts` | A value a Postgres `integer` column can actually hold. **`Number.isInteger` is not this check** — `Number.isInteger(1e21)` is `true`, its string form `"1e+21"` is what reaches PostgREST, and Postgres rejects it as `22P02`; `2147483648` passes too and gives `22003`. The guard that let both through carried a comment asserting neither was an integer (ADR-079) | `apps/web/lib/red-list.ts` |
| `UUID_PATTERN` | `apps/web/lib/keyset.ts` | A uuid checked to BE one rather than merely to be a string — the check whose absence let a crafted cursor append its own filter clause | `apps/web/lib/members.ts`, `apps/web/lib/red-list.ts` |
| `pageSizeFrom` | `apps/web/lib/keyset.ts` | The requested page size, clamped rather than refused: a page size is a hint from a caller, and an unreadable one becomes the default because a mistyped query string should not make a list unloadable | `apps/web/lib/members.ts`, `apps/web/lib/red-list.ts` |
| `loadRedList` | `apps/web/lib/red-list.ts` | The red list's query: open cases from `red_list_cases`, longest-absent first, keyset paginated. **No `.eq('tenant_id', …)`** — the view is `security_invoker`, so the underlying tables' policies filter, and an application-side predicate would return the right rows even with one of them broken | `apps/web/app/(console)/red-list/page.tsx` |
| `loadPayments` | `apps/web/lib/payments.ts` | The payments ledger's query: the gym's payments newest first, keyset paginated on `(created_at desc, id asc)` — total, because two payments taken in the same second at two counters is the ordinary case here. Embeds the member and the staff member across ADR-052's composite foreign keys. **No `.eq('tenant_id', …)`** — `payments_tenant_select` filters | `apps/web/app/(console)/payments/page.tsx` |
| `loadReceipt` | `apps/web/lib/payments.ts` | One payment plus the gym it was taken at, for the receipt document. A payment in another gym comes back `null` because the policy returns no row, so "not this gym's" and "no such payment" are the same answer — which is the honest one, since distinguishing them would confirm the payment exists | `apps/web/app/(console)/payments/[paymentId]/page.tsx` |
| `deskTime` | `apps/web/lib/payments.ts` | An instant as the GYM reads it — `deskTime(iso, timezone)` — never the server's. **Found wrong in a browser**: the receipt showed `2026-09-08 21:30` for a payment whose own number said `2026-27/000001`, the number derived in the gym's day and the date printed in UTC. Between 00:00 and 05:30 IST every receipt carried yesterday's date, and at the 1 April boundary a receipt would be filed under one financial year while showing a date in the other. ADR-039 and MNY-004 in the view layer. `hourCycle: 'h23'` and not `hour12: false`, which renders midnight as `24` under some ICU versions; falls back to `DEFAULT_TIMEZONE` because `organizations.timezone` is free text | The payments ledger and the receipt page |
| `seeOther` | `apps/web/lib/api.ts` | The 303 redirect every console form handler answers with. **303 and not 302**: a refresh must not re-post the form, which is the difference between a member being charged once and twice. Takes a short stable error code, never a sentence — the screen owns the wording, and a message in the query string would let anyone hand a member of staff a link that displays whatever they like | `apps/web/app/api/follow-ups/route.ts`, `apps/web/app/api/payments/route.ts` |
| `staffFormParsed` | `apps/web/lib/api.ts` | A complete real-staff caller, their form, and the parsed data. It can restrict callers to an explicit generated-role allowlist before reading the form. Three outcomes remain distinct: `failure`, `invalid`, or the verified session plus typed data | Console form Route Handlers |
| `optionalField` | `packages/shared/src/api/forms.ts` | A blank optional form input is **absent, not empty** — an untouched text field submits `''`, not nothing, and without this a follow-up with no note was refused as unreadable. Found by pressing the button in a browser; no unit test would have, because a test constructs the body it means and a form constructs the body it has. Shared rather than copied, because `jscpd` found the second copy had been made by pasting the first, comment and all | `followUpRequestSchema`, `paymentRequestSchema` |
| `staffForm` | `apps/web/lib/api.ts` | A complete real-staff caller and their form fields in one step, with an optional generated-role allowlist. It keeps identity/role refusal separate from an unreadable body and returns a flattened verified session for handlers | Console form Route Handlers |
| `formFields` | `apps/web/lib/api.ts` | One `<form method="post">` submission as plain string fields ready for a zod schema, or the typed envelope refusing a body that is not a form at all. The two membership handlers called `await request.formData()` unguarded, so that case was a **500 for a request that was merely wrong** — and it is the one form failure a redirect cannot answer, because there is no member id in it to redirect with. `File` values are dropped rather than stringified | `apps/web/app/api/memberships/route.ts`, `apps/web/app/api/memberships/pauses/route.ts` |
| `staffSession` | `apps/web/lib/api.ts` | Reads signature-verified claims through the complete identity classifier and accepts only a valid real-staff identity. An optional generated-role allowlist returns 403 for a real staff role outside that endpoint's audience; other identity shapes return 401 | Console Route Handlers |
| `supabaseCredentials` | `apps/web/lib/supabase/credentials.ts` | The `(url, anonKey)` tuple both `createServerClient` call sites spread, read through `clientEnv()` and never `process.env` (hard rule 3) | `apps/web/lib/supabase/server.ts`, `apps/web/lib/supabase/proxy-session.ts` |
| `updateSession` | `apps/web/lib/supabase/proxy-session.ts` | Refreshes the Supabase session on every matched request and returns the response carrying the refreshed cookie — the only place a refresh can be persisted, since a Server Component may not write cookies. Performs **no** redirect: the layout decides who may see a page, and a proxy-level redirect would swallow the not-linked state | `apps/web/proxy.ts` |
| `toLocalDate` | `packages/shared/src/streaks/streaks.ts` | Collapses an instant to a gym-local `YYYY-MM-DD` via `Intl.DateTimeFormat.formatToParts`. **Parts, not `en-CA`** — that locale's format is unspecified on React Native's Intl, which would have broken the mobile app in Phase 7 and nowhere earlier | Streak functions |
| `visitStreak` | `packages/shared/src/streaks/streaks.ts` | STK-001's consecutive-visit rule. A rest day, an approved pause or a gym holiday bridges rather than breaks (STK-002) | Member screens, Phase 4 retention |
| `weeklyGoalStreak` | `packages/shared/src/streaks/streaks.ts` | STK-001's N-of-M rule, counted in **weeks**, over distinct days attended. A week left with fewer than the goal's worth of non-exempt days is neutral — it bridges rather than meeting or breaking, since counting it met would manufacture streak and breaking it would violate STK-002 | Member screens |
| `calendarStreak` | `packages/shared/src/streaks/streaks.ts` | STK-001's challenge-period rule over an explicit window | Member screens, once challenges exist (OPEN-017) |
| `takeMemberEcho` | `apps/web/app/(console)/members/echo.ts` | Reads back a refused submission's typed values **and clears the cookie**. Destructive by design: a refresh shows an empty form rather than resurrecting a submission the user moved on from | Member create and edit pages |
| `loadMember` | `apps/web/app/(console)/members/member-data.ts` | One member plus their recent attendance, read through the caller's own session so RLS filters. Carries no tenant predicate — adding one would mask the defect the pgTAP suite exists to catch | Member detail and edit pages |
| `memberSubmission` | `apps/web/app/api/members/member-input.ts` | Everything both member handlers do before touching `members`: identify the caller, read the form, reject a non-row. Returns **the caller's own session**, never `service_role` | Member create and update handlers |
| `refusalMessage` | `apps/web/app/api/members/member-input.ts` | Turns a Postgres refusal into a sentence a front desk can act on, mapped by SQLSTATE and constraint name rather than by a role list copied into the app | Member handlers |
| `redirectTo` | `apps/web/app/api/members/member-input.ts` | POST/Redirect/GET on success — a form handler answering JSON strands the front desk | Member handlers |
| `redirectWithError` | `apps/web/app/api/members/member-input.ts` | Failure path. Carries the typed values back in a one-shot `HttpOnly` cookie, **not the query string**: these fields are a member's phone, email and date of birth, and a query string is written into the access log, the browser's history and the `Referer` of everything the page then loads (DPDP, `docs/security.md`) | Member handlers |
| `backToMember` | `apps/web/app/api/memberships/shared.ts` | POST/Redirect/GET back to a member's membership screen, carrying a stable `?error=` code rather than a sentence | Membership and pause handlers |
| `formField` | `apps/web/app/api/memberships/shared.ts` | One trimmed string field out of a `FormData` | Membership handlers |
| `dateField` | `apps/web/app/api/memberships/shared.ts` | One `YYYY-MM-DD` field, validated as a shape before Postgres sees it | Membership handlers |
| `addDays` | `apps/web/app/api/memberships/shared.ts` | Calendar-day arithmetic on a `YYYY-MM-DD` string. **No longer what computes a plan sale's `ends_on`** — since ADR-083 the membership create handler grants no period at all and the payment does, in the database. Its remaining caller is the freeze-day counting loop in the pauses handler | `apps/web/app/api/memberships/pauses/route.ts` |


## Next.js file conventions

Exports Next.js itself names. Page, layout and Route Handler exports are exempt from `registry-lint` by convention; `proxy.ts` sits at the app root rather than under `app/`, so its exports are not — and both are listed here anyway, because a session looking for "where does the session get refreshed" should find it in this file.

| Name | File | Purpose | Used by |
|---|---|---|---|
| `config` | `apps/web/proxy.ts` | The proxy `matcher` — every path except Next's static assets. Not a magic value, a route pattern | Next.js |
| `proxy` | `apps/web/proxy.ts` | The Next 16 proxy entry point (renamed from `middleware` in 16.0.0, and defaulting to the Node.js runtime — a copied `middleware.ts` does not run at all). Delegates to `updateSession` | Next.js, on every matched request |

## Hooks

_None yet — Phase 2+._

## Components

Phase 7 owns the design system. What is here is the one screen whose behaviour cannot be a Server Component — a camera and a live confirmation need a client — built from native elements and Tailwind, with no UI dependency.

| Name | File | Purpose | Used by |
|---|---|---|---|
| `Alert` | `apps/web/app/(console)/alert.tsx` | The one red banner every console screen uses to say something went wrong. `role="alert"` is not decoration: a front desk that has just pressed a button needs the refusal read out without focus moving, and that attribute is the whole of it. Extracted because two screens had spelled the same ten lines out and `jscpd` called it a clone — the second copy was made by pasting the first, which is how one of them ends up without the `role` | `apps/web/app/(console)/red-list/page.tsx`, `apps/web/app/(console)/payments/page.tsx`, `apps/web/app/(console)/payments/[paymentId]/page.tsx` |
| `CheckInGate` | `apps/web/app/(console)/console/check-in/check-in-gate.tsx` | The check-in gate: holds a gate code (scanned with the platform's own `BarcodeDetector`, or typed where that is absent — **no QR library**), posts to `POST /api/check-in`, and renders the confirmation in type legible across a room. It clears on no timer, because a gate that erases itself leaves the person who looked away not knowing whether they are in. A send that fails offers a retry reusing the same `clientEventId`, which is what makes retrying safe rather than a second visit | `apps/web/app/(console)/console/check-in/page.tsx` |
| `MemberSearchPage` | `apps/web/app/(console)/console/member-search-page.tsx` | The frame the roster and the check-in gate share: title, the link to the other screen, the native `<form method="get">` phone search, and where a failed read says so. A Server Component with no interactivity of its own, so the search works before any JavaScript has loaded. Extracted because the two screens are the same page with a different body, and the second copy is where they drift apart | `apps/web/app/(console)/console/page.tsx`, `apps/web/app/(console)/console/check-in/page.tsx` |
| `MemberForm` | `apps/web/app/(console)/members/member-form.tsx` | One native `<form>` serving both create and edit. Status options come from the generated `member_status` enum, never a hand-written list (ADR-021). No client-side phone regex — the E.164 rule lives in `members_phone_format_chk` and a copy here would be a second source of truth that stops that path ever running | Member new and edit pages |
| `MEMBER_ECHO_COOKIE` | `apps/web/app/api/members/member-input.ts` | Name of the one-shot echo cookie, shared by the handler that sets it and the page that clears it | `takeMemberEcho` |


## Env vars

| Name | Purpose | Set in |
|---|---|---|
| `CLOUDFLARE_ACCOUNT_ID` | Cloudflare account for R2 access | `.env.local`, CI secrets, Vercel |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase publishable key (client-safe) | `.env.local`, CI secrets, Vercel |
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase project URL (client-safe) | `.env.local`, CI secrets, Vercel |
| `R2_ACCESS_KEY_ID` | R2 credential | `.env.local`, CI secrets, Vercel |
| `R2_BUCKET` | R2 bucket name (`gymloop-media`) | `.env.local`, CI secrets, Vercel |
| `R2_ENDPOINT` | R2 S3-compatible endpoint URL | `.env.local`, CI secrets, Vercel |
| `R2_SECRET_ACCESS_KEY` | R2 credential (secret) | `.env.local`, CI secrets, Vercel |
| `SUPABASE_DB_PASSWORD` | Direct Postgres connection password | `.env.local`, CI secrets |
| `SUPABASE_PROJECT_REF` | `pecxrpskmfeuyzngvewq` | `.env.local`, CI secrets |
| `SUPABASE_SERVICE_ROLE_KEY` | Server-only Supabase secret key — never `NEXT_PUBLIC_`-prefixed | `.env.local`, CI secrets, Vercel (server env only) |

**CI-only secrets** (not app env vars, not in `.env.example`): `SUPABASE_ACCESS_TOKEN` (Supabase CLI auth for CI), `HOLDOUT_DEPLOY_KEY` (read-only deploy key for cloning `gymloop-holdout`).

## Phase 6 identity and navigation

| Symbol | Location | Contract | Consumers |
|---|---|---|---|
| `StaffRole` | `apps/web/lib/identity.ts` | Four real staff roles extracted from generated app_role | Identity classifier and API role guards |
| `PlatformRole` | `apps/web/lib/identity.ts` | Super admin/support subset of generated app_role | Platform identity and API guards |
| `GymloopIdentity` | `apps/web/lib/identity.ts` | Discriminated complete staff/member/platform/impersonation/unlinked shape | Verified session and navigation callers |
| `classifyIdentity` | `apps/web/lib/identity.ts` | Pure UUID/role/contradictory-claim validation; does not verify signatures | API and navigation after getClaims |
| `identityHome` | `apps/web/lib/identity.ts` | One role-home mapping, with unlinked and preview outcomes | Sign-in, root, not-linked and audience layouts |
| `readIdentity` | `apps/web/lib/identity-session.ts` | Signature-verified claims plus pure classification; Auth failures expose no identity | Navigation, API helpers and end-preview route |
| `requireAudience` | `apps/web/lib/identity-session.ts` | Typed console/member/platform read guard with central home redirects | Layouts and role home pages |
| `AccountFrame` | `apps/web/app/account-frame.tsx` | Shared account header and sign-out | Console, member and platform layouts |
| `PreviewProvider` | `apps/web/app/preview-context.tsx` | Read-only preview state for descendants, rendered on the server too | Console layout |
| `usePreviewReadOnly` | `apps/web/app/preview-context.tsx` | Reads the UI preview state, never authorizes database access | MutationForm and CheckInGate |
| `MutationForm` | `apps/web/app/preview-context.tsx` | Hides product POST forms during preview; preserves ordinary native forms | Member, membership, refund and follow-up screens |
| `MemberSession` | `apps/web/lib/api.ts` | Verified subject, tenant, member id and Supabase client | memberSession callers |
| `PlatformSession` | `apps/web/lib/api.ts` | Verified subject, generated platform role and Supabase client | platformSession callers |
| `memberSession` | `apps/web/lib/api.ts` | Member-specific session/failure envelope with complete identity validation | Member API callers |
| `platformSession` | `apps/web/lib/api.ts` | Platform session/failure envelope; requireAdmin refuses support | Platform API callers |
| `POST /api/impersonation/end` | `apps/web/app/api/impersonation/end/route.ts` | Claim-only own-session end through RLS; refresh then platform, local logout/sign-in on refresh failure | Persistent preview banner |
| `gymTimeLabel` | `apps/web/lib/payments.ts` | Reuses deskTime with a validated zone label; invalid zones report unavailable instead of labeling fallback time as gym-local | Fleet trial expiry and preview banner |
